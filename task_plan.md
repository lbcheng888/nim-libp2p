# task_plan

| files | action | verify | done |
| --- | --- | --- | --- |
| `/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim` `/Users/lbcheng/nim-libp2p/libp2p/services/wanbootstrapservice.nim` `/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` `/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` `/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_sync_imports.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把移动端 LSMR 自举的 handoff 边界收正到 `Identified`。当前真问题已经前推清楚：`Connected` 太早，bootstrap handoff 必须先确保现有 muxer 上拿到 identify snapshot，再做 `refreshCoordinateRecord -> queueControlSync/publish -> overlay`。同时验证 publish 到 anchor 的时间窗，不再把“handoff 返回后立刻读一次”误当成 publish 失败。 | `/tmp/repro_lsmr_bootstrap_handoff_wait.nim` 已坐实：同一条 bootstrap handoff 在 `await sleepAsync(2.seconds)` 后 `anchorHasClient=true`；`nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` 为 `1 OK`；`nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_sync_imports.nim` 为 `1 OK`。`nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_routing_status.nim` 仍被工作区现存的 `finalizeReceivedDirectMessageAsync GC-safe` 编译问题挡住，不是这轮 handoff 改动引入。下一步只剩拿到 `tests/testwanbootstrap_service.nim` 的最终 summary，再 fresh 部署 root anchor 做 Android/鸿蒙 LSMR 互见。 | in_progress |
| `/Users/lbcheng/nim-libp2p/examples/dual_stack_bootstrap_node.nim` `/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim` `/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把移动端 LSMR 自举的根锚点前提收正。当前 Android fresh 已坐实 `lsmrMinWitnessQuorum=1` 生效，但 `lsmrLocalCertReady=false / lsmrWitnessSuccess=0 / lsmrLastRefreshReason=\"\"`，真问题不是 asset 没下发，而是公网 bootstrap host 只跑了 `WanBootstrapRole.anchor`，没挂 `withLsmr`，因此不是 root witness/control anchor。修法是把 `dual_stack_bootstrap_node` 升成 `dualStack + primary=lsmr` 的 LSMR root anchor，并把 `refreshCoordinateRecord()` 失败诊断收成绝不空白；同时补一条 `bootstrapConnectedHandoff` 网络回归，钉住“连上单 root anchor 后本地 cert 能装上并 publish 回 anchor”。 | `nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` 新增 handoff 回归通过；`nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` 保持通过；`nim c -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/examples/dual_stack_bootstrap_node.nim` 通过。下一步只需把这版 bootstrap root host 真部署出去，再 fresh 验证 Android/鸿蒙 `lsmrLocalCertReady/lsmrActiveCertificates` 起量。 | in_progress |
| `/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim` `/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_sync_imports.nim` `/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` `/Users/lbcheng/nim-libp2p/tests/fabric_network_lsmr.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 LSMR control protocol 收成真 session 语义。根因是 client 侧 `LsmrControlSession` 已按“同一条 stream 连续 sync/publish”设计，但 server 侧 `handleControl` 还是一请求一流、回完就关，导致 `sync -> publish` drain 在同流上必然 read timeout，再补起第二个 control dial，把 `testlsmrservice_network_slow_sync_imports` 打成 `Opened SecureConn: 4 / Closed SecureConn: 3`。修法是把 control handler 改成在同一条 control stream 上循环处理多次 request/response，直到 EOF/close，再退出。 | `nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_sync_imports.nim` 为 `1 OK`；`nim c -r -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` 为 `1 OK`；`nim c -d:ssl --verbosity:0 /Users/lbcheng/nim-libp2p/tests/fabric_network_lsmr.nim` 通过 | yes |
| `/Users/lbcheng/nim-libp2p/libp2p/dial.nim` `/Users/lbcheng/nim-libp2p/libp2p/dialer.nim` `/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim` `/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` `/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 LSMR fresh control-plane 从“outgoing auto identify + refresh 完就自动 publish 回连接 peer”的假耦合里拆开。`DialOptions` 新增 `skipAutoIdentify`，LSMR witness/control/overlay connect 显式走 control-plane；connected 热路在本地还没 cert 时只发 refresh hint，不再让 anchor 因一次 witness 安装本地 cert 后立刻反向抢第一波 control stream。 | `nim c -d:ssl /Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_witness_refresh.nim` 通过；`nim c -d:ssl /Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` 通过；`nim c -d:ssl /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_routing_status.nim` 仍被工作区现存 `libnimlibp2p finalizeReceivedDirectMessageAsync GC-safe` 旧问题卡住，不是这轮新引入；定向 `lsmr_diag` 复现实验已坐实：anchor 侧旧的反向 control publish storm 消失，当前唯一 blocker 前推成 incoming `/ipfs/id/1.0.0` identify 仍和 fresh witness 首流竞争 | in_progress |
| `/Users/lbcheng/nim-libp2p/libp2p/peerinfo.nim` `/Users/lbcheng/nim-libp2p/libp2p/protocols/identify.nim` `/Users/lbcheng/nim-libp2p/libp2p/protocols/rendezvous/rendezvous.nim` `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testpeerinfo.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把移动端 WAN advertise/export 的 `SignedPeerRecord` 真相收正，不再直接拿热路里的缓存 envelope 做外发编码。`peerInfo` 新增 `currentSignedPeerRecord/currentSignedPeerRecordBytes`：优先复用稳定缓存的 `payload/signature/seqNo`，导出时只用 live `privateKey/publicKey` 修正 envelope 的 `publicKey`，缓存空或坏时才重建；identify/rendezvous/syncAdvertisedPeerInfo` 统一走这条导出路径，避免 Android `lastRendezvousAdvertiseError=Signed Peer Record encode failed: KeyError`。 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testpeerinfo.nim` 为 `4 OK`，新增回归已坐实：即使把 `peerInfo.signedPeerRecord` 清成 stale/default，`currentSignedPeerRecordBytes()` 仍能导出正确 `peerId/addrs/publicKey`；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testidentify.nim` 为 `7 OK`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/discovery/testrendezvous.nim` 为 `21 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过。当前 live blocker 已前推成“把这版重新打进 Android/Harmony 后 fresh 复跑双端 WAN bootstrap，确认 `lastRendezvousAdvertiseError` 消失、`rendezvousAdvertised` 起量、双端开始互见” | in_progress |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/app/src/main/assets/mobile-bootstrap.json` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/resources/rawfile/mobile-bootstrap.json` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 Android WAN automation 的 host network 真相收成同步下发，不再赌后台时序；同时把双端 `relayBootstrapMultiaddrs` 改成真 `/p2p-circuit/p2p/<bootstrapPeer>` seed，继续前推 Android/Harmony WAN 节点发现 | fresh Android `android-transport-223010` 已坐实：`transportAddrs.hostNetworkStatus` 不再是 `{}`，`multiaddrs` 不再是 wildcard，而是真 `240e:43d:682c:222:a0b5:23ff:fe8e:a9d3` 与 `10.67.35.137`；fresh `android-btick-223015` 也已带同一份 `hostNetworkStatus`，`selectedCandidates[*].addrs` 与 `providerSeedAddrs/anchorSeedAddrs` 里都出现了真 `/p2p-circuit/p2p/12D3KooWMJmRGksDpUMeaDSsJikJNgBs9M5fsJihHf8kPptSaUun`，`relayCapable=true`，说明 relay-backed bootstrap seed 已经生效。Android `assembleDebug`、`adb install -r -g` 与鸿蒙 `release_hongmeng.sh --target 3KN0224C18003262 --skip-nfc-smoke` 都通过。当前唯一 live blocker 已前推成 Android authoritative bootstrap 真连通：`connectedCount=0 joinRuns=0` 仍未实际 join，显式 `bootstrap_seed_connect` token `android-seedconnect-223058` 已结束 `ok=false`，log 里只坐实 `native_lock_snapshot timeout after 100ms`，下一轮只该继续收 Android bootstrap exact connect 本体，不再回头收 hostNetwork/wildcard | in_progress |
| `/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim` `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/cpp/libp2p_harmony_napi.cpp` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/services/Libp2pRuntimeService.ets` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/entryability/EntryAbility.ets` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把鸿蒙 `tailnet_status` 从“查询本身超时”前推到“真实 not ready”。NAPI 先补 `tailnetStartKickoff/getTailnetStartStatus*`，`EntryAbility` 改成先看 `tailnet start` 真状态，不再把启动中误判成失败；同时去掉 `ensureStarted()/bootstrap poll` 里的 host network refresh 抢占，再把 native `tailnet_status` 收成非 ready 快照：provider 或 relay listener 还没 ready 时，直接返回轻量值快照，不再走 `submitCommand(cmdTailnetStatus)` 的重路径。期间试过把 ETS async 状态读取硬改同步，已坐实会把鸿蒙主进程打成 `signal:11`，现已全部回退。 | fresh 鸿蒙 `tailnet_status` automation token `harmony-tailnet-check-210533` 现在会在 `21:05:38 -> 21:05:58` 直接返回 `cmd_result {"ok":false,"error":"tailnet_not_ready",...,"snapshotPayload":"{\"ok\":false}"}`；最新 hilog 里已不再出现旧的 `invokeAsync:tailnetStatusJsonAsync:timeout=2500`、`invokeAsync:getTailnetStartStatusAsync:timeout=1500` 和 `refreshHostNetworkStatus:bootstrap_status:*` 连环超时，只剩真实 `cmd_finish ... error=tailnet_not_ready`。这说明 Harmony 当前 blocker 已从“状态查询链自己卡死”前推成“tailnet 还没 ready”，后续可以直接继续收 WAN bootstrap/discovery，不必再回头修 `tailnet_status` 查询链 | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_wan_provider_seed_addrs.nim` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/services/Libp2pRuntimeService.ets` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把移动端 WAN bootstrap 的配置边界收正，不再让 `relayMultiaddrs` 一鱼两吃。native 新增 `relayBootstrapMultiaddrs/relay_bootstrap_multiaddrs` ingestion，authoritative bootstrap 与 `anchorSeedAddrs` 只认 bootstrap seed 字段；`relayMultiaddrs` 继续只给 relay reservation。Android/Harmony loader 同步支持新字段，Android 的 bootstrap override 也改成喂 `static_bootstrap + relayBootstrapMultiaddrs`，不再误写 reservation 字段。 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_wan_provider_seed_addrs.nim` 为 `2 OK`，新增回归已坐实：只给 `relayBootstrapMultiaddrs` 也会进入 authoritative bootstrap，`bootstrap status` 里出现 `selectedCandidates[*].addrs=/p2p-circuit/p2p/<bootstrapPeer>`、`provider.anchorSeedAddrs` 与 `provider.authoritativeBootstrapPeerIds`；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testwanbootstrap_authoritative_autojoin.nim` 仍为 `1 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过。Android 与鸿蒙当前 bundled `mobile-bootstrap.json` 都已经带上真实 `relayBootstrapMultiaddrs`，代码边界和资产边界都已收正；当前 live blocker 已经前推到 Harmony tailnet/bootstrap readiness，本身不再是 bootstrap seed 字段缺失 | yes |
| `/Users/lbcheng/nim-libp2p/libp2p/services/wanbootstrapservice.nim` `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_authoritative_autojoin.nim` `/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` `/Users/lbcheng/UniMaker/android/app/src/main/assets/mobile-bootstrap.json` `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/resources/rawfile/mobile-bootstrap.json` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 收 WAN authoritative bootstrap 发现主因：1) `wanbootstrapservice.syncLoop()` 去掉 `when defined(ohos)` 的 auto-join 禁用，authoritative peer 自动 join 改成统一走完整 `authoritativeExactBootstrapAddrs()`，不再只拿 `selected.addrs`；2) `auto_authoritative_seed*` source 不再被当成 `direct_hint`；3) fresh 重打安卓和鸿蒙，直接用 `runtime_snapshot/bootstrap_tick/bootstrap_seed_connect` 验证移动端 WAN 发现真相，不再靠旧日志猜 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testwanbootstrap_authoritative_autojoin.nim` 为 `1 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim` 通过；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` 仍为 `7 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过。Android 新包 `lastUpdateTime=2026-04-07 18:54:17`，鸿蒙新包 `updateTime=1775559333345`。fresh 验证已前推到新真相：Android `android_runtime_fix_1900` 为 `connected=0 discovered=0`，`android_bootstrap_fix_1901` 为 `joinAttempts=1 joinConnected=0`，`selectedCandidates[0].addrs` 已扩成 4 条 authoritative IPv6 exact addrs，旧的“只剩单 UDP 候选”已收掉；但 `android_anchor_manual_1905` 仍是 `bootstrap_seed_connect_exact_timeout`。Harmony `harmony_runtime_fix_1859` 也已看到 `selectedCandidatePreview[0].addrCount=4`，说明 OHOS 不再跳过 authoritative auto-join；但 `harmony_bootstrap_fix_1901` 仍是 `bootstrapTickAsync_timeout`，`harmony_anchor_manual_1905` 已坐实同一 authoritative peer 的 `UDP -> TCP` 两条 exact addr 全部 `connect timeout`。同时安卓和鸿蒙资产里的 `relayMultiaddrs` 仍与 `static_bootstrap` 完全相同，当前还没有真 relay-backed bootstrap seed，所以移动端 WAN 发现 blocker 已收窄成“authoritative bootstrap 基础设施对公网蜂窝 exact connect 不可达”，不是旧代码误路由或 OHOS skip | yes |
| `/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/pages/NodePageNative.ets` `/Users/lbcheng/UniMaker/hongmeng/scripts/release_hongmeng.sh` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把鸿蒙节点页按安卓 Nodes 页面 1:1 收成同一套节点真相。核心是把 `NodeEntry` 扩成安卓同构字段，节点列表改成 `connectedPeersInfo + connectedPeers + discoveredPeers + discoverySnapshot` 按 `peerId` 合并，不再把 `hostPeers` 直灌列表；过滤改成安卓同款 source tag 推断，不再做字符串 contains；列表行改成“标题 + 域名/PeerId + transport/discovery/wake/path chips”，空态文案也改成安卓同款 BLE/QUIC 移动发现链说明 | 鸿蒙 `release_hongmeng.sh --target 3KN0224C18003262 --skip-nfc-smoke` 已成功重编、打 HAP 并 `hdc install` 到设备；设备侧 `bm dump -n com.example.unimaker` 已更新到 `updateTime=1775546524822`。本轮中途 ArkTS 真报错只剩 `Object.prototype.hasOwnProperty.call` 不被支持，已改成字段判空后重新打包通过；最终安装成功，ability 因设备锁屏未自动拉起 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` `/Users/lbcheng/nim-libp2p/build/adb_retained_heat2_extract.log` `/Users/lbcheng/nim-libp2p/build/adb_retained_heat_10s_extract.log` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 给 Android production UI instrumentation 的同进程双发入口补上 `repeatDelayMs`，把 retained relay heat 的验证从“马上二连发”扩成“带短空窗仍可复用”。基于同一条 fresh `/tsnet` seed，先实跑一次默认短空窗 same-process 双发，再实跑一次 `repeatDelayMs=10000` 的 same-process 双发，专门钉住 route 清掉后 peer relay retained lease 依然能把第二发送到现成 `sessionConnKey`，不回退 fresh warm | `ConversationRouteSendTest` 现已支持 `-e repeatDelayMs <ms>`；测试包重编并重装后，fresh 本地 route `/ip4/100.64.148.187/udp/56556/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上两轮 same-process 双发均为 `OK (1 test)`。第一轮 token `ui_dm_20260407_1421_retain2_a/ui_dm_20260407_1421_retain2_b` 在本地 `dm_rows` 全实收，log `[adb_retained_heat2_extract.log](/Users/lbcheng/nim-libp2p/build/adb_retained_heat2_extract.log)` 已坐实同一 app PID `18293` 下二发前 `status=ready selectedPath=relay selectedConnKey=533165791696`，并命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，第二发 `durationMs=715`。第二轮 token `ui_dm_20260407_1426_retain10s_a/ui_dm_20260407_1426_retain10s_b` 也全实收，log `[adb_retained_heat_10s_extract.log](/Users/lbcheng/nim-libp2p/build/adb_retained_heat_10s_extract.log)` 已坐实同一 app PID `20302` 下隔 `10s` 仍是 `status=ready selectedPath=relay selectedConnKey=533164702160`，第二发继续命中 `relay_ready_session`，`sendThreadMessage success durationMs=818`。赛后本地 `tailnet_status.proxyListenersReady=true`，`adb logcat -b crash` 仍只有华为输入法噪音 | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 peer relay session 从 route 生命周期里再解耦一层：`RelaySessionTargetSpec` 现在带 retained lease，`clearRouteTransportIntent()` 不再因为 route 清掉就立刻删非 sticky relay target；只要 lease 还活着，owner 就继续把这个 peer 当 retained relay heat。`reconcilePeerRouteSession()` 也改成只有 lease 真过期才 `release_retained`，并在 live relay session 存活时持续刷新 lease，避免同进程短空窗直接掉回 fresh warm | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 为 `7 tests run (8.51s): 7 OK, 0 FAILED, 0 SKIPPED`，新增用例 `clearing a failed relay route still retains peer relay heat` 已通过；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim` 仍为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过 | yes |
| `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 给“删掉固定 `3s` ACK 预留”补一条稳定 native 回归，但不再依赖 live direct/relay 热身。新用例直接走 `libp2p_send_with_ack_seeded_kickoff()` + 假 `/tsnet` seed，钉住低预算终态里 `reservedAckBudgetMs` 必须始终是 `0`，避免以后又把假 ACK reserve 加回 seeded DM kickoff 状态机 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 为 `6 tests run (7.36s): 6 OK, 0 FAILED, 0 SKIPPED`；新增用例 `seeded kickoff never reintroduces fake ack reserve` 已通过，确认 kickoff terminal payload 始终带 `reservedAckBudgetMs=0`。同时 `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim` 仍为 `18 OK` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/build/adb_sameproc_fix_extract.log` `/Users/lbcheng/nim-libp2p/build/wait_dm_sameproc_fix_a.json` `/Users/lbcheng/nim-libp2p/build/wait_dm_sameproc_fix_b.json` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 seeded `/tsnet` DM send 主链里错误的固定 budget 切分删掉。`executeSendDirectCommand()` 现在不再先拍脑袋预留 `3s` ACK budget；`waitForRouteTransportReady()`、`ensurePeerReadyViaExplicitAddresses()`、后续 send/ack 都直接吃同一个总 deadline。配合前一轮的长活 relay warm worker，send 不会再比 worker 更早因为 `route transport not ready` 提前失败 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` 通过；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`adb install -r -g` 全通过，主包时间 `2026-04-07 13:30:18`。fresh 本地 route `/ip4/100.64.148.187/udp/60897/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，same-process 双发 token `ui_dm_20260407_1332_sameproc_fix_a/ui_dm_20260407_1332_sameproc_fix_b` 为 `OK (1 test)`，本地两次 `wait_dm` 全实收。Android logcat `[adb_sameproc_fix_extract.log](/Users/lbcheng/nim-libp2p/build/adb_sameproc_fix_extract.log)` 已坐实 repeat 发前 snapshot 已是 `status=ready selectedPath=relay selectedConnKey=532830587344`，发送链命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，第二发 `sendThreadMessage success durationMs=818`；旧的 `route transport not ready` 首发提前失败已不再出现，赛后本地 `tailnet_status.proxyListenersReady=true` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` `/Users/lbcheng/nim-libp2p/build/adb_providerfix_s1.log` `/Users/lbcheng/nim-libp2p/build/adb_providerfix_s2.log` `/Users/lbcheng/nim-libp2p/build/adb_sameproc_fail.log` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 single-heart route wait 漏掉的 `provider_wait -> ensureLocalTsnetReady()` 补回主链；`waitForRouteTransportReady()` 现在在 `provider_wait` 时会直接拉起本地 tailnet provider，并在 provider ready 后立刻 `reconcileDesiredRouteSessionsNow()`。同时给 Android instrumentation 补了可选 `repeatToken/repeatRoute`，专门做同进程双发验证 retained peer relay heat | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`./gradlew :app:assembleDebugAndroidTest -x lint --no-daemon`、`adb install -r -g` 全通过，主包时间 `2026-04-07 12:55:56`。fresh 本地 route `/ip4/100.64.148.187/udp/49611/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`、赛时 `tailnet_status.proxyListenersReady=true`。cold app token `ui_dm_20260407_1256_providerfix_s1` 为 `OK (1 test)`，本地 `wait_dm` 实收，Android logcat 坐实 `waitForRouteTransportReady ... relayStage=running -> ready`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，最终 `sendThreadMessage success durationMs=1629`，旧的 `local tailnet provider pending` 已消失。第二条独立 instrumentation token `ui_dm_20260407_1257_providerfix_s2` 也是 `OK (1 test)`、本地 `wait_dm` 实收，但 app PID 已从 `25754` 变成 `26274`，不是同进程 retained-heat 样本；这轮仍重新跑了 `dm_protocol_select_begin -> warm_done`，最终 `durationMs=2567`。新增 same-process 双发 instrumentation 后，token `ui_dm_20260407_1301_sameproc_a/ui_dm_20260407_1301_sameproc_b` 的首发就前推出新的 `dm_transport_connect_begin` timeout，log 在 `/Users/lbcheng/nim-libp2p/build/adb_sameproc_fail.log`，说明 retained peer relay heat 还没被这条新 harness 验真 | yes |
| `Android 生产 UI + 本地 tsnet 产品节点` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh_repeat.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh_s3.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh_s4.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh_s5.log` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 对 budget 化 multistream 协商做一组完整的 fresh Android cold `no-wait` 稳定性样本，目标是不再看单把成功，而是确认 production UI 1:1 连跑时是否始终复用 `relay_ready_session`、节点是否持续存活、以及时延分布是否已经压到 relay RTT 量级 | 同一 fresh route `/ip4/100.64.148.187/udp/59367/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，5 轮 token `ui_dm_20260407_1113_budgetfresh`、`ui_dm_20260407_1114_budgetfresh_repeat`、`ui_dm_20260407_1122_budgetfresh_s3`、`ui_dm_20260407_1123_budgetfresh_s4`、`ui_dm_20260407_1124_budgetfresh_s5` 全部为 `OK (1 test)`，本地 `wait_dm` 五轮全实收；Android logcat 五轮都坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，`sendThreadMessage success durationMs` 依次为 `1427/2442/3271/1732/1832`，分布 `min/median/avg/max = 1427/1832/2141/3271 ms`。赛后 `tailnet_status.proxyListenersReady=true`、节点持续存活；`adb logcat -b crash` 没有新的 `com.unimaker.native.debug` 崩溃，只剩华为输入法 `com.huawei.ohos.inputmethod` 的 Room migration 噪音 | yes |
| `/Users/lbcheng/nim-libp2p/libp2p/multistream.nim` `/Users/lbcheng/nim-libp2p/libp2p/dialer.nim` `/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 `dm_protocol_select_begin` 这条链收成“每一步都吃剩余 budget”的真协商：`Dialer.negotiateStream()` 现在显式接 `totalTimeout`，`beginProtocolNegotiation`、multistream handshake、`select_first_proto/select_proto` 都按剩余 budget 计算 timeout；DM fresh/live 两条路径都不再用“outer timeout 包住内部固定 15s 黑箱” | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` 通过；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`adb install -r -g` 全通过，主包时间 `2026-04-07 11:11:38` | yes |
| `Android 生产 UI + 本地 tsnet 产品节点` `/Users/lbcheng/nim-libp2p/build/android_protocol_budget_node.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh.log` `/Users/lbcheng/nim-libp2p/build/adb_budgetfresh_repeat.log` | fresh 验证新的 budget 化 multistream 协商在生产 UI 热路上的真实效果；目标是确认既不回退，又把 relay no-wait 再往 relay RTT 下界压，并收掉旧的 repeat 偶发 `dm_protocol_select_begin` 崩口 | fresh 本地 `19125` route `/ip4/100.64.148.187/udp/59367/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，relay RTT `min/avg/max = 1433/1805/2461 ms`。cold app token `ui_dm_20260407_1113_budgetfresh` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，`sendThreadMessage success durationMs=1427`。紧接着同 route 的 cold repeat token `ui_dm_20260407_1114_budgetfresh_repeat` 也是 `OK (1 test)`，本地 `wait_dm` 实收，`sendThreadMessage success durationMs=2442`；两轮都没有新的 `com.unimaker.native.debug` crash，也没有重演旧的 `send_with_ack_timeout` | yes |
| `Android 生产 UI + 本地 tsnet 产品节点` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 对 `init-first + native gated` 做第二轮 cold repeat 验证，确认这条线已经压到下界但还没完全收稳；当前剩余 blocker 不是 Android gate，也不是 send path，而是 relay warm 偶发卡在 `dm_protocol_select_begin`，导致 `send_with_ack` 在 `path_intent_upsert` 阶段耗尽 10 秒预算 | 同一 fresh route `/ip4/100.64.148.187/udp/53158/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，第一轮 cold `no-wait` token `ui_dm_20260407_105553_initfirst_nativegated` 为 `OK (1 test)`，本地 `wait_dm` 实收，`sendThreadMessage success durationMs=1323`；但紧接着第二轮 cold repeat token `ui_dm_20260407_105744_initfirst_nativegated_repeat` 失败，本地 `wait_dm` 超时未收，Android logcat 已坐实 `中继会话 RUNNING dm_protocol_select_begin`，`sendDirectText:timeout_status ... stage=path_intent_upsert ... error=send_with_ack_timeout`，最终 `sendThreadMessage failed ... durationMs=10088 error=send_with_ack_timeout pathSelection=relay_only` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 truth-route `init-first` 收成理论正确版本：route intent 允许先于 runtime/provider 就绪声明，但 native 的 relay warm 在 `provider_wait` 期间只记 `prssPending`，不再同步等 provider 也不再烧掉 `8s` warm budget；provider/tailnet 状态一变化就直接唤醒 relay reconcile。这样 Android preheater 可以重新走“先 `pathIntentUpsert`，再 `ensureRuntimeReady`”，不再回到旧的 budget burn / ack timeout | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 为 `5 tests run (6.37s): 5 OK, 0 FAILED, 0 SKIPPED`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android 重新 `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`adb install -r -g` 后主包时间是 `2026-04-07 10:55:34`。fresh 本地 `19125` route `/ip4/100.64.148.187/udp/53158/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，cold app `no-wait` token `ui_dm_20260407_105553_initfirst_nativegated` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`、`dm_write_done -> dm_ack_done`，最终 `sendThreadMessage success durationMs=1323`。同轮 fresh relay RTT 是 `min/avg/max = 1337/1660/2295 ms`，已经压到当前 relay RTT 下界量级 | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 把 peer relay reconcile 从 `750ms` 轮询扫表改成 route/relay state 驱动的 wake loop：`path intent upsert/clear`、direct/relay snapshot 变化、warm 完成都会直接唤醒 reconcile；同时补一条 native 回归，钉住“清掉最后一个 relay route intent 后，relay status 会立即变成 missing”，不再依赖旧轮询节拍 | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 为 `5 tests run (6.08s): 5 OK, 0 FAILED, 0 SKIPPED` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/build/tsnet_product_node` `Android 生产 UI + 本地 tsnet 产品节点` | 用这版 wake-driven relay reconcile 复跑 fresh Android production UI，确认 lower-bound 主因是否还在 native send path；当前 fresh 样本已经前推成真实 `relayOnly` transport connect 失败，不再是 Android gate/lifecycle 抖动 | Android 新包 `lastUpdateTime=2026-04-07 10:38:59`；fresh 本地 `19125` route `/ip4/100.64.148.187/udp/59141/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，`tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_1040_reconcilewake` 失败，本地 `wait_dm` 为 `wait_dm_timeout`；Android logcat 已前推成 `sendThreadMessage failed ... stage=connect_exact_failed durationMs=6322 error=dm_transport_connect_begin`，页面诊断是 `中继会话 FAILED dm_transport_connect_begin`。赛后本地 `19125` 仍存活，`adb logcat -b crash` 没有新的 `com.unimaker.native.debug` native crash | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 否决两条继续前移 Android gate 的试探：1) `primeRoute()` 提前到 `super.onCreate()` 前，没有带来稳定收益，fresh 样本反而回到 `2343ms`；2) preheater 只等 runtime started、不等本机 `peerId`，fresh 样本直接前推成 `中继会话 READY` 后 `ack timeout`，本地 `wait_dm` 为空。当前源码和 APK 都已回滚到上一版稳定顺序，说明 Android 入口这层已经收完，下一步只能下探 native relay session / ACK 本体，不能再在 Android lifecycle gate 上试切口 | `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 两次均通过；带 `primeRoute@before-super` 的主包 `lastUpdateTime=2026-04-07 10:12:24`，fresh `no-wait` token `ui_dm_20260407_101240_oncreatewarm` 虽然 `OK (1 test)` 且本地 `wait_dm` 实收，但 Android logcat 显示 `sendThreadMessage success durationMs=2343`，没有优于稳定样本 `1630ms`。随后带 `requirePeerId=false` 的主包 `lastUpdateTime=2026-04-07 10:17:19`，fresh `no-wait` token `ui_dm_20260407_101730_startgate` 失败，Android logcat 已坐实 `中继会话 READY` 后 `send_failed 9816ms ack timeout`，本地 `wait_dm` 返回 `wait_dm_timeout`。现已回滚源码并重装稳定包，设备主包时间恢复为 `2026-04-07 10:20:53` | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt` `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/task_plan.md` `/Users/lbcheng/nim-libp2p/progress.md` | 证伪并回退 truth-route `init-first` 试验：app 级 preheater 不能在 runtime/provider 未 ready 时先 `initialize + pathIntentUpsert`，否则会先烧掉 relay warm 预算，再把 send 前推成 `dm_transport_connect_begin/route transport not ready`；稳定顺序必须保持 `scheduleWarm -> ensureRuntimeReady -> pathIntentUpsert`，native relay warm 也必须在 worker 内用剩余 budget 继续 `ensureLocalTsnetReady`，不能先把 task 掉成 `pending` 退出 | 设备主包仍是 `lastUpdateTime=2026-04-07 09:58:40`；fresh 重起本地 `19125` 后新 seed `/ip4/100.64.148.187/udp/62863/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，本地 `tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_100358_revertcheck` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `runPeerRouteSessionWarmAsync ... source=android_truth_route_launch`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，并以 `sendThreadMessage success durationMs=1630` 收尾；赛后 `tailnet_status.proxyListenersReady=true`，`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash，仍然只有华为输入法噪音 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/UniMakerApplication.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 把 truth-route relay preheat 从会话页内的 ViewModel side effect 前移到 App 级 owner：新增 `TruthRouteTransportPreheater`，`MainActivity` 一解析出 route 就直接 `primeRoute()`，先 `scheduleWarm()`，worker 在 `ensureRuntimeReady()` 通过后再 `pathIntentUpsert(source=android_truth_route_launch)`；`NativeAppViewModel` 只保留 route snapshot 观察，不再自己 clear/declare/activate transport intent | `cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；`adb install -r /Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` 后主包 `lastUpdateTime=2026-04-07 09:48:47`；fresh 重起本地 `19125` 后新 seed `/ip4/100.64.148.187/udp/59157/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，本地 `tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_0950_routepreheat` 为 `OK (1 test)`，本地 `wait_dm` 实收，Android logcat 已坐实 `runPeerRouteSessionWarmAsync ... source=android_truth_route_launch`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`、`sendThreadMessage success durationMs=1830`；同 route 的 `waitRelayReady=true` token `ui_dm_20260407_0952_routepreheat_wait` 也为 `OK (1 test)`，本地 `wait_dm` 实收，logcat 已坐实 `relay ready before tap ... 中继会话 READY`、`sendConversationText transport_ready ... selectedConnKey=532922185888`、`sendThreadMessage success durationMs=716`；赛后 `tailnet_status.proxyListenersReady=true`，`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 收掉 `relay_only` 新真失败：peer relay warm 不能再偷吃“已存地址”，必须直接消费 `RouteTransportIntent/RelaySessionTargetSpec.selectedAddrs`；这样 Android truth-route 的 relay-only 预热才会用页面 seed route，不再立刻报 `route_missing` | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim` 为 `4 OK`，新增回归钉住 `relay_only` intent 不再落 `route_missing`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android 重新 `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`adb install -r` 后主包 `lastUpdateTime=2026-04-07 09:19:37`；fresh `19125` 新 seed `/ip4/100.64.148.187/udp/61288/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0920_nowait_relayonly_seedfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，Android logcat 已坐实 `sendConversationText transport_wait ... relayOnly=true policy=relay_only`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`、`sendThreadMessage success durationMs=3379`；`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash，赛后 `tailnet_status.proxyListenersReady=true` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` | 收掉本地产品节点 `tailnet_status` 高频轮询新崩栈：`tailnet_status` RPC 不再走 `cstring -> JsonNode -> rpcResponse(JsonNode) -> $response`，改成直接文本包回，避免 Android diagnostics 热路继续构造 live JSON tree 把 `encodeFrame/alloc` 打穿 | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh 重起本地 `19125` 后，对 `tailnet_status` 连续压 `120` 次，本地节点持续存活，`local_info` 仍可读，`android_truthroute_node.log` 不再出现旧的 `encodeFrame -> alloc -> SIGSEGV`；同一 fresh route `/ip4/100.64.148.187/udp/49572/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，`tailnet_status.proxyListenersReady=true`、`relayListeners=2/2 incoming` | yes |
| `/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 在收掉本地 `tailnet_status` 崩栈后复跑 fresh Android production UI，确认 blocker 已从“本地节点被 diagnostics 轮询打死”前推；同一条 fresh route 上重新验证 `waitRelayReady=true` 和 cold app `no-wait` | fresh 本地 `19125` seed `/ip4/100.64.148.187/udp/49572/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 且 `tailnet_status.proxyListenersReady=true` 后，production UI token `ui_dm_20260407_0859_waitready_listenerfix` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `observeTruthRouteTransportSnapshot ready ... selectedPath=relay connKey=532651157056`、`relay ready before tap diagnostics=... 中继会话 READY`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false`、`sendThreadMessage success durationMs=714`。随后 cold app `no-wait` token `ui_dm_20260407_0901_nowait_listenerfix` 也为 `OK (1 test)`，本地 `wait_dm` 实收，logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false`、`observeTruthRouteTransportSnapshot ready ... connKey=532705743840`、`sendThreadMessage success durationMs=3581`；赛后本地 `19125` 继续存活，`tailnet_status.proxyListenersReady=true`，`adb logcat -b crash` 里没有 `com.unimaker.native.debug` 的新 crash | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 收掉 truth-route fresh 进入页的 stale diagnostics：route revision 切换后立刻清空同会话旧 `conversationSendStatusState/conversationLatencyState`，并把 `observeTruthRouteTransportSnapshot()` 改成 native 查询后再二次校验 `revision/pathKey`，防止旧 route observer 把过期 direct/relay 状态写回新页面 | `cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；主包重装后 `lastUpdateTime=2026-04-07 08:35:59`；fresh 本地 `19125` 新 seed `/ip4/100.64.148.187/udp/53225/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，production UI `waitRelayReady=true` token `ui_dm_20260407_0837_diagreset_wait` 失败点已前推成 `relay session not ready before tap`，对应 fresh `tailnet_status` 仍是 `proxyListenersReady=false`、`relayListeners=IPv4 incoming + IPv6 registering`；同一 fresh route 的 cold app `no-wait` token `ui_dm_20260407_0838_diagreset_nowait` 为 `OK (1 test)`，本地 `wait_dm` 实收，Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false -> sendThreadMessage success durationMs=3791`，`adb exec-out uiautomator dump` 抓到 fresh deep link 页面上的 `conversation_diagnostics` 已只剩 `libp2p RELAY · tailnet DIRECT · 打洞 0% 0/2 · 直连DM -- · 中继DM -- · 中继会话 RUNNING dm_protocol_select_begin`，不再带旧的 `发送失败/已送达/发送中` 假状态；`adb logcat -b crash` 仍然只有华为输入法噪音 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 删掉 Android 侧残留的 `directSessionTarget` 双编排：移除 ambient/background direct target sync、旧 direct target set/clear/status/cache 死代码，只保留 truth-route 的 `path intent + pathSnapshot` 单心脏热路，避免后台 `android_ambient_direct_target` 再抢 seeded `/tsnet` DM 的 relay warm | 旧 `DirectSessionWarmAttemptResult/RelaySessionWarmAttemptResult`、`queryNativeDirectSessionWarmStatus/queryNativeRelaySessionWarmStatus`、`android_ambient_direct_target` 等残留引用已清零；`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；主包重装后 `lastUpdateTime=2026-04-07 08:21:31`；fresh 本地 `19125` 新 seed `/ip4/100.64.148.187/udp/58385/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 在 `tailnet_status.proxyListenersReady=true` 后，production UI token `ui_dm_20260407_0825_waitready_fresh` 与 cold app `ui_dm_20260407_0826_nowait_fresh` 都为 `OK (1 test)`，本地 `wait_dm` 两轮都实收；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false -> sendThreadMessage success durationMs=4014`，`adb logcat -b crash` 没有新的 `com.unimaker.native.debug` native crash | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `tests/testmobile_ffi_conversation_transport_snapshot.nim` | 把 unified `ConversationTransportSnapshot` 补齐 direct 计数与 DM 延迟字段；Android 会话页 `refreshConversationLatency()` 只读 `pathSnapshot`，不再为 direct/relay 统计分开回读旧 status；新增 mobile FFI 回归钉住 policy 更新、同 peer 多 route 隔离、重复 `pathSnapshot` 稳定可读 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_conversation_transport_snapshot.nim` 为 `3 OK`；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；`bash /Users/lbcheng/UniMaker/android/scripts/build_nim_libp2p_android.sh debug` 与 `cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；主包重装后 `lastUpdateTime=2026-04-07 07:58:32`；fresh 本地 `19125` seed `/ip4/100.64.148.187/udp/62154/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0800_pathsnap` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`，Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session -> sendThreadMessage success durationMs=2978` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/cpp/libp2p_bridge.cpp` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 把 seeded `/tsnet` DM 热路收成 owner 单心脏：新增 `path intent + unified route snapshot + route wakeup`，发送入口改成 `upsert intent -> wait route snapshot -> sendOn sessionConnKey`；Android truth-route 只声明 intent、读统一 snapshot，发送不再自己拼 `preferredSessionConnKey/direct vs relay` | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；`bash /Users/lbcheng/UniMaker/android/scripts/build_nim_libp2p_android.sh debug` 通过；`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；`adb install -r app-debug.apk` 后 `lastUpdateTime=2026-04-07 06:25:01`；fresh 本地 `19125` seed `/ip4/100.64.148.187/udp/54029/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0627_singleheart` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true` | yes |
| `libp2p/transports/msquicdriver_experimental.nim` | 把 `MsQuicPendingSend` 从 `stream.pendingSends: seq[ref] + clientContext=cast[pointer](pending)` 收成理论正确的 raw-root callback context：stream owner 只保存 `pendingSendRoots/pendingSendOrder` 的 raw pointer 真相，`MsQuicPendingSend` 生命周期统一改成 `GC_ref/GC_unref` 手动 root/unroot，`seSendComplete` 只按 raw context 精确取回 pending send，不再让 GC 扫描这条 live `ref` 链 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；fresh 安装 `lastUpdateTime=2026-04-07 00:27:08` 后，本地 fresh seed `/ip4/100.64.148.187/udp/60958/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260407_002915_pendroot` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `19125` 继续健康，旧的 Android `UniMakerNativeN -> eqtrace(MsQuicPendingSend) -> createStream -> udpListenerLoop -> SIGSEGV` 未再出现 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/transports/tsnettransport.nim` `examples/mobile_ffi/tsnet_product_node.nim` `tests/testtsnettransport.nim` | 把 `publicIpv6/publicIpv4/directCandidates` 收进 tsnet `bridge-extra` 的唯一值对象导出；Android 和产品节点都走同一条 owner `publishTsnetLocalPeerInfo()` 发布链，产品节点启动时最小喂一次 `hostNetworkStatus` 以触发公网 IP probe；`tailnet_status` 增加 `bridgePublicIpv4/bridgePublicIpv6/bridgeDirectCandidates/bridgeHostNetwork` 便于真机复核 | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 通过；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；fresh 本地 `19125` 的 `tailnet_status` 已出现 `bridgePublicIpv4` 和 `bridgeDirectCandidates`；Android logcat 已出现 `cmdUpdateHostNetworkStatus publish_local_peer_info:start -> public_ip_probe:start/done -> sync_advertised:done` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 把 direct/relay warm 的 `provider_wait` 和 direct 的 `route_wait` 收进 worker 自己的 deadline；relay worker 在真正结束前不得先把 task 状态切成非 `running`，避免 owner 重复 spawn 同 peer task | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；新主包 `lastUpdateTime=2026-04-06 13:41:31` 后，fresh seed `/ip4/100.64.148.187/udp/58756/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_134203` 已证明旧的 `dm_dial_lock_wait` 回归被收掉，但当前新的唯一 blocker 前推成 `connect_exact_failed 6826ms dm_transport_connect_begin`：direct snapshot 稳定停在 `route_wait/no tsnet direct route is registered...`，relay worker 稳定停在 `dm_transport_connect_begin` | in_progress |
| `libp2p/transports/nim-msquic/api/runtime_bridge.nim` | 把 MsQuic callback context 的 owner 模型收成理论正确边界：handle 索引表只存 raw pointer，live/retired 生命周期统一改成 `GC_ref/GC_unref` 手动 root 表；callback 入口再临时 root 一次，`store/drop` 不再扫描或扩容 `ref` 容器 | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh 本地 seed `/ip4/100.64.148.187/udp/59770/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_131932` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息；赛后 `tailnet_status/local_info` 继续正常，`proxyListenersReady=true`，旧的 `storeConnectionContext/find` fresh 节点崩链未再出现 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `tests/testmobile_ffi_social_state_cache.nim` | 把 `social_state` 持久化收成理论正确的 owner write-behind：业务热路只改 live tree 和 dirty version，真正 stringify/落盘只允许 owner `cmdSocialStateFlush` 单飞执行；同时把 content/group/direct message 的 owner 写路径接进同一把 social state 锁，停机前强制 flush | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 为 `2 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh 安装 `lastUpdateTime=2026-04-06 11:27:00` 后，production UI token `ui_dm_20260406_112745` 已证明本地 node 不再在 `persistSocialState*` 热路自杀，`wait_dm` 实收且赛后 `19125` 继续健康 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/protocols/dm/dmservice.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 在 `social_state` 崩链收掉后，继续只收 fresh production UI 下“direct ready 到得太晚”这条主线：当前 message 已到 node、ACK 超时不是 receiver crash，而是 sender 在 `dm_ack_wait` 时几乎没有剩余预算；要把 `android_truth_route_prepare` 的 direct ready 明确前移到点击前 | fresh `19125 + wait_dm + production UI 1:1` 下，`cmdSendDirect connectivity ... path=direct_ready_session` 不能再在超时边缘才出现；下一轮要么重新回到 `dm_ack_done`，要么把新的阻塞点前推成 direct reconcile 的更早阶段，而不是再停在泛化 `ack timeout` | in_progress |
| `libp2p/connmanager.nim` `tests/testconnmngr.nim` | 修每 peer 连接上限的 off-by-one，并在名额已满时优先替换同方向旧 live 连接，避免新入站被历史残留卡死 | `nim c -r tests/testconnmngr.nim` 通过，`25 OK` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | listener 持久 accept 流的 `route_status` 校验改走独立 fresh probe，避免校验复用当前 control client 把正在跑的 bridge 连坐打死 | 编译通过，生产节点实跑不再出现 `Too many connections for peer`，继续收 relay listener 真状态 | in_progress |
| `libp2p/transports/tsnet/quiccontrol.nim` `tests/testtsnetquiccontrol_fin.nim` | 把 control 单请求流收成“写完即 half-close”，并补一条“服务端等 EOF 才解码”回归，先把 builtin QUIC 底层请求结束语义钉死 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquiccontrol.nim` 为 `7 OK`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquiccontrol_fin.nim` 为 `1 OK` | yes |
| `libp2p/transports/tsnet/proxy.nim` `tests/testtsnetproxy.nim` | 修 `proxyRouteSnapshots/sanitizeProxyRoutesUnsafe` 上的半残 key 崩溃，禁止产品节点在 listener route 丢失后因为 proxy registry 脏行直接 SIGSEGV | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetproxy.nim` 为 `9 OK`，本地产品节点重启后 `19125` 不再因这条崩溃掉线 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | gateway 同一路由重复 `listen/accept` 时，不得覆盖正在 `ready_wait/busy` 的 active listener；先收掉 `accept stream lost attached listener route` | 远端 journal 不再出现 `lost attached listener route`，本地产品节点重启后 Android 再发时 gateway 可继续进入真实 `dial/bridge` 或给出新的准确失败 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/msquicstream.nim` `tests/testtsnetquicrelay.nim` | 修 framed reader 对 cached 完整 frame 的漏消费，并清掉 listener restart 后悬挂的 `acceptStream/regStream`，禁止 persistent accept lane 因“缓存里已有完整 incoming 但还继续 read”卡死 | `nim c tests/testtsnetquicrelay.nim` 通过；`cached complete json/binary frame...`、`accept stream can receive incoming...`、`route_status stays published while listener ready is pending` 全绿；本地产品节点重启后 `proxyListenersReady=true`、`listenAddrSource=relay_listeners` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | Android 主动拨 seed `/tsnet` 地址发送 DM 时，发送前 gate 只等本机 `providerReady`，不再把 `proxyListenersReady` 误当成发送前置条件 | 最新生产 UI 已不再出现 `local_tailnet_ready_wait`，失败前移回真实发送链 | yes |
| `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnet/proxy.nim` `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnettransport.nim` `tests/testtsnetproxy.nim` `tests/testtsnetquicrelay.nim` | 修本地产品节点这轮连续暴露的三条真崩点：状态读路径字符串派生、proxy resolved remote shared key 悬空、relay bridge task 注册时跨 safe wrapper 返回 `ref handle` | 三组回归全绿；最新生产 UI token `ui_dm_20260331_194833` 跑完后，本地 `tailnet_status` 仍可秒回，节点不再在 `incoming/ready/bridge_attached` 后退出 | yes |
| `examples/mobile_ffi/tsnet_product_node.nim` | `relayPublishedListenAddrs` 做 nil 安全，`local_info` 不能再把产品节点打崩 | 本地重启后 `local_info` 不再 SIGSEGV | yes |
| `docs/android_native_async_architecture.md` | 固化 Android/native 异步架构约束 | 文档存在且约束清楚 | yes |
| `BridgeFacades.kt` | 发送链改成 hints + native kickoff，不再同步 preconnect；消息页删除 `prepareDirectRoutePeer` 这条错层同步预连接 | Kotlin 编译、安装 APK、生产 UI 发送 | in_progress |
| `ChengLibp2pNative.kt` | 增加 seeded kickoff，把 seed addrs 直接带进 native 异步任务 | Kotlin 编译、安装 APK、生产 UI 发送 | in_progress |
| `libp2p_bridge.cpp` | JNI 暴露 seeded kickoff | Native 编译、安装 APK | in_progress |
| `libnimlibp2p.nim` | send_with_ack kickoff 接收 addresses/source 并传进 async 发送任务 | Android so 编译、安装 APK、生产 UI 发送 | in_progress |
| `NativeAppViewModel.kt` | 发送时不再等待后台 preconnect job | Kotlin 编译、安装 APK、生产 UI 发送 | in_progress |
| `quicrelay.nim` `testtsnetquicrelay.nim` | 去掉 `proxyListenersReady` 的旧缓存假绿，gateway 丢 route 后立即掉回未就绪并触发修复 | 单测通过，本地/远端产品节点反向 DM 恢复 | in_progress |
| `tsnet_product_node.nim` `libnimlibp2p.nim` | 本地和远端 `local_info` 都只发布 `relay_listeners` 里的真实 route，不再按 `tailnetIPs` 合成 `/tsnet` 地址 | 本地与远端 `local_info.listenAddrSource=relay_listeners`，且只返回真实 IPv4 route | yes |
| `tsnet_product_rpc.nim` `docs/tsnet_product_rpc.md` | 远端产品节点操作收成结构化 Nim 工具，禁止再手写 `ssh + JSON` | 本地和远端 `tailnet_status/local_info/connect_exact` 用新工具跑通 | yes |
| `api_impl.nim` `api_loss_recovery_test.nim` | 修复 1-RTT PTO 在 probe 发不出去时的过去时间忙循环；远端被动 close/reset 时立即停 maintenance 并清 recovery 状态 | `api_loss_recovery_test` 通过，gateway CPU 恢复正常，`9444` fresh probe 再次 `Running/Mapped` | yes |
| `tsnet_product_node.nim` | `connect_exact` 同步包装不再把短暂 `missing` 当最终失败 | 产品 RPC 同步 `connect_exact` 不再秒回 `connect_exact_missing_state` | yes |
| `libnimlibp2p.nim` `peerstore.nim` | 把 identify 失败点钉到 `ensurePeerIdentified -> beginProtocolNegotiation/select` | 本地产品节点日志能区分是缺 muxer、协商卡住还是最终 identify 超时 | yes |
| `libnimlibp2p.nim` `tsnet/runtime.nim` `tsnetprovider*.nim` `tsnettransport.nim` | 去掉 `idleWakeLoop` 每秒构整份 `tailnetStatus` 的重路径，改成轻量 listener repair 判定 | 本地产品节点跑 `connect_exact` 时不再在后台状态轮询里崩溃 | yes |
| `quicrelay.nim` | 修 owner 级 relay 状态写回错误，恢复 listener/dial task、listener stage、udp dial state 的真实持久化 | `testtsnetquicrelay.nim` 全绿 | yes |
| `quicrelay.nim` `docs/transports/tsnet_quic_relay_protocol.md` | 给长期控制流写操作加硬超时，超时立即判定控制流失效并重建，禁止再出现“流卡死但 route 还显示 busy/awaiting” | `testtsnetquicrelay.nim` 全绿，长期控制流不再无限挂住 | yes |
| `runtime.nim` | `proxyListenerReadiness` 不再只信本地 ready 集合，而是按 gateway 真实发布态校验 route；发布态一丢就立刻掉回未就绪并触发修复 | `route_not_registered` 不再在远端仍报 `awaiting/ready` 时持续复现 | in_progress |
| `runtime.nim` `libnimlibp2p.nim` `testtsnetquicrelay.nim` | listener 自修判定覆盖“本地仍 await/ready，但 gateway 已经下架 route”的情况，避免 route 被下架后永远不重注册 | 本地回归通过，远端 route 被网关下架后能自动补回 | in_progress |
| `libnimlibp2p.nim` | `tailnetControlRefreshLoop` 只在地址真变化时才触发 `syncAdvertisedPeerInfo`，并给 `spawnTailnetWarmAdvertisedPeerInfo` 加单任务并发门，避免产品节点自己把 listener 发布链刷爆 | 本地单节点 `wait_tailnet_ready/local_info` 秒回且不再 99% CPU；远端同步后不再周期性丢 route | yes |
| `libnimlibp2p.nim` | 修 `send_dm_kickoff` 预序列化直发包缺 `from/mid/timestamp/ackRequested`，保证接收端即使 `conn.peerId` 未就绪也能正确记账入库 | 新一轮 `send_dm_status` 完成后，远端 `dm_rows` 能看到 token | in_progress |
| `libnimlibp2p.nim` | `send_dm_kickoff` 外层若已 `connect_exact` 成功，就直接复用现有连接做 `waitForPeerReady`，不再因为带显式地址而重复走一次 exact dial | 外层 `connect_exact` 成功后，`send_dm_kickoff` 不再报内部 `connect timeout` | in_progress |
| `tsnet_product_node.nim` `libnimlibp2p.nim` `peerstore.nim` | 继续收 Mac→远端产品节点在真实 `relay_listeners` route 上的 `send_dm_timeout`；当前 `connect_exact` 已成功，卡在 identify/DM 发送后半段 | `connect_exact` 成功后，`send_dm` 不再超时，远端 `wait_dm` 能收到 token | yes |
| `libnimlibp2p.nim` | `send_with_ack_kickoff` 内层异步发送命令必须继承 `requestKey`，否则 Android 只能看到永远不前进的 `kickoff` 假状态 | Android `timeout_status.payload.stage` 不再固定卡在 `kickoff` | yes |
| `libnimlibp2p.nim` `dmservice.nim` | 把 `send_with_ack` 内层 DM 发送过程接进 kickoff 状态机，明确暴露 `fresh_dial/write/ack_wait/ack_done` | Android 日志已能看到内层阶段推进，最新成功实跑 `ui_dm_20260331_224458` 已收尾到 `stage=sent` | yes |
| `app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` | instrumentation 改走真 deep link `app://unimaker/...`，并在开跑前先唤醒设备、尝试 dismiss 非安全锁屏；若仍是系统 PIN 锁则立刻真失败，不再把锁屏误报成 `message input not shown` | 锁屏现场会在约 3 秒内明确报 `device locked by system PIN`；解锁后最新生产 UI 实跑 `ui_dm_20260331_224458` 已通过 | yes |
| `NativeAppViewModel.kt` | `configureNativeConnectivity` 收成单飞 + 冷却窗口，发送期不再被同签名高频刷新反复抢 native | Android logcat 出现 `skip=cooldown/in_flight`，生产 UI 失败类型不再回到 `peer_ready_failed` | yes |
| `MainActivity.kt` | deep link 必须从 `intent.data` 解析 truth route，不能只认 `route` extra | 冷启动可直接代码切到目标节点消息页，并带上 `seedAddrsJson/draftText` | yes |
| `BridgeFacades.kt` `libnimlibp2p.nim` | 继续收 Android 生产 UI 当前 `send_with_ack_timeout@send_begin`；现在页面正确、draft 正确、消息真正发出，但 Mac 仍未收到 | 最新生产 UI token `ui_dm_20260331_224458` 已通过，本地 Mac `wait_dm` 已收到 token | yes |
| `core-storage/NativeEntities.kt` `core-storage/NativeRepositories.kt` `core-bridge/BridgeFacades.kt` `core-bridge/ChengLibp2pNative.kt` `examples/mobile_ffi/libnimlibp2p.nim` | 修 Android 生产 UI 发送前后的两类真问题：peers 高频全表写库崩溃，以及 `sendWithAck` 总预算被 12 秒下限、late-connect 漂移、kickoff 前置等待拉长 | 生产 UI 最新实跑 `ui_dm_20260331_153421` 已在 `9873ms` 内返回真实 `peer_ready_failed connect timeout`，不再崩溃、不卡 12 秒以上 | yes |
| `core-bridge/BridgeFacades.kt` `core-bridge/ChengLibp2pNative.kt` `examples/mobile_ffi/libnimlibp2p.nim` `NativeAppViewModel.kt` | 继续把 Android 当前已打通的生产 UI 路径收回到你给的 1 到 3 秒产品基线；当前主线已通，剩下是时延优化 | 生产 UI 再发时，`connect_exact/send_dm/wait_dm` 恢复到 1 到 3 秒量级 | in_progress |
| `app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt` | Android 的 tailnet ready 必须包含 bootstrap relay gate；若 `deferAutoJoinUntilRelayReady=true`，就先 `reserveOnAllRelays` 并等 `startupRelayReady`，不能只靠 `providerReady/proxyListenersReady/tailnetIPs` 放行 | Kotlin 编译通过；最新生产 UI 实跑 `ui_dm_20260331_224458` 已进入真实发送并成功送达 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 把 `cmdConnectedPeersInfo` 收成真正的后台快照读：不再在只读状态查询里实时测 transport RTT，避免 Android 发送前被 `connectedPeersInfo/diagnostics` 队列饿死 | 重编 Android 后，生产 UI 不再出现 `queued 12s send_with_ack_timeout`，而是回到 10 秒内真实 `peer_ready_failed/connect timeout` 或成功 | yes |
| `BridgeFacades.kt` `NativeAppViewModel.kt` | Android 生产 UI 自动发送动作必须跟随当前页面真实输入框和发送按钮位置，不能复用旧坐标 | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已稳定触发一次真实发送并成功 | yes |
| `NativeAppViewModel.kt` `ChengLibp2pNative.kt` | 发送生命周期必须全程保持 interactive native priority，不能 12 秒后让后台 `getConnectedPeers/socialListDiscoveredPeers` 恢复直打 native 并触发 `FD_SET>=1024` 崩溃 | Android 发送期间不再被系统拉回桌面，crash buffer 不再出现 `FD_SET` 越界 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `libnimlibp2p.nim` `examples/mobile_ffi/tsnet_product_node.nim` | 继续收 Android 生产 UI 这轮新的真实 `peer_ready_failed 10s connect timeout`；当前本地 listener 稳态已恢复，但发送还没走到 `incoming/ready`，说明 blocker 已前移回 connect 阶段 | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已不再报 `peer_ready_failed connect timeout`，本地 `wait_dm` 已收到 token | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` | 先用本地 relay gateway 把“builtin QUIC accept 长流 idle 时自己掉 route”排除掉，再继续盯远端真实 `route lost` | 新增 `idle 12s + route_status` 回归通过；若生产现场仍掉 route，则主线转到远端 gateway/部署态而不是客户端基础件 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | 继续收远端现场那条真实 `nim_quic relay route lost`，重点看 accept 控制流为什么会被网关删路由，而不是再回头猜 Android/UI/builtin QUIC 拥塞；已补 `route_status false` 和 `clearListenerRoutesForConnection` 的精确日志 | 生产 UI 下一轮若仍失败，要么拿到远端更窄的 detach/route delete 证据，要么直接打通本地接收 | in_progress |
| `examples/mobile_ffi/tsnet_remote_admin.nim` `docs/tsnet_remote_admin.md` | 把远端 gateway 也收进结构化 Nim 运维：支持 `sync/build/restart/status/deploy/journal-gateway`，不再手写远端命令 | `status-gateway` 可读到远端服务状态，`deploy-gateway` 已成功把 `nim-tsnet-gateway.service` 切到新二进制，`journal-gateway` 能直接抓到 `listener notify/ready failed` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/tsnet_product_node.nim` | 继续收本地产品节点 persistent accept 流为什么没把 gateway 已写出的 `incoming` 读出来；当前远端已坐实是 `incoming_write_done` 后 30 秒等不到 `ready`，不是 `route_status` 自己乱报 | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已成功送达，本地接收链当前不再阻断主线 | yes |
| `core-bridge/BridgeFacades.kt` `core-bridge/ChengLibp2pNative.kt` `examples/mobile_ffi/libnimlibp2p.nim` `examples/mobile_ffi/tsnet_product_node.nim` | 把 Android 这轮 `connect_done` 已成功却仍被 UI 提前判死的错位收掉；核心是让桥层和 native 共用同一个 10 秒 deadline 起点 | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已通过，本地 `wait_dm` 收到 token，用户可见耗时 `7808ms` | yes |
| `libp2p/protocols/dm/dmservice.nim` `examples/mobile_ffi/libnimlibp2p.nim` | 已修“exact connect 后同一请求又 fresh dial 一次”这条真问题；当前要继续验证它是否已经稳定消除旧的 `Stream Underlying Connection Closed!`，并收新前移出的 `peer_ready_wait/send_with_ack_timeout` | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已成功，旧的 `Stream Underlying Connection Closed!` 未再出现 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/tsnet_product_node.nim` | 继续收本地 listener published 稳态：这轮发送前 `relayListeners[0].stage=awaiting`，发送后掉成 `relayListeners=[]`、`listenAddrSource=runtime`，需要确认是不是 listener publish 在发送窗口里松掉，导致 exact connect 之后卡死在 `peer_ready_wait` | 最新生产 UI 实跑 `ui_dm_20260331_224458` 已成功送达，这条不再是当前主线；若后续要继续压时延，再单独收稳态波动 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | exact-connect 多地址预算改成“剩余总预算按剩余尝试数均分”，不再把前几个 `/tsnet` 尝试压成几百毫秒的假超时 | 最新生产 UI 实跑 `ui_dm_20260401_124032` 已从 `peer_ready_failed connect timeout` 前移成 `dm_fresh_dial_begin 9814ms send_with_ack_timeout` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 把 exact-connect 成功拿到的 `verifiedMuxer` 真贯穿到 `sendDirectWithAckWaiter`，并按当前 peer 的真实 live muxer 取值，禁止同一请求在发送层再次 fresh dial | 最新生产 UI 实跑 `ui_dm_20260401_152109` 已不再出现 `dm_fresh_dial_begin`，而是前移成 `connect_exact 9807ms send_with_ack_timeout` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | exact-connect 成功后，DM send 只沿用本次真正命中的单地址，并先小窗等待 live `verifiedMuxer`，避免“刚 exact-connect 成功又拿整组 seed 地址 fresh dial”把 QUIC 数据面搞串 | 重新编译通过；后续若 relay 恢复，不应再回到 `dm_fresh_dial_begin` 多地址并发旧症状 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | UDP listener 不再共用 owner 级 relay client；远端若明确回 `register ack err=not_found`，listener task 直接停掉，不再无限重试把本地节点拖崩 | 最新本地节点在 15 轮状态轮询后保持存活；listener 阶段落到 `stopped`，`tsnet_product_node` 不再被这条路径打成 `SIGSEGV` | yes |
| `app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` | 收 `ui_dm_20260401_125457` 这轮 Android `RenderThread/libhwui.so` `SIGSEGV`，确认它不是当前 DM 主线 | 后续 `ui_dm_20260401_130140` 和 `ui_dm_20260401_152109` 已回到真实网络失败，不再复现这条 crash；当前主线已前移回底层 relay/listener | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/tsnet_product_node.nim` `examples/mobile_ffi/tsnet_remote_admin.nim` | 继续收 exact-connect 成功后本地 listener 为什么没收到新的 `incoming`；当前活实例日志仍持续 `published=true`，但 `tailnet_status.relayListeners=[]`、`local_info.listenAddrSource=runtime`，并且 `wait_dm` 超时 | 最新生产 UI `ui_dm_20260401_172929` 赛后本地节点仍是 `relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners`，这条“published 日志和状态 API 矛盾”已不再复现 | yes |
| `examples/tsnet_quic_relay_probe.nim` `examples/tsnet_quic_relay_accept_probe.nim` `libp2p/transports/tsnet/quicrelay.nim` | 修 `quicRelayEndpoint()` 从 control URL 错保留 `9444` 端口的派生 bug，避免 relay probe 和 listener 实际打到 control handler | 同样输入 `quic://64.176.84.12:9444/nim-tsnet-control-quic/v1` 时，最小 `ping` probe 已 `acknowledged=true`，最小 `accept` probe 已 `ok=true` | yes |
| `libp2p/transports/tsnettransport.nim` | 去掉接收入站连接时对全局 raw->advertised reverse registry 的依赖，改用当前 transport 的 `listenerMappings` 改写本地监听地址，修 `lookupAdvertisedForRaw -> Table.getOrDefault` 崩溃 | `nim c -r --hints:off --warnings:off -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `16 OK`，`tests/testtsnettransport_quic.nim` 为 `2 OK`，最新生产 UI `ui_dm_20260401_172929` 跑完后本地节点持续存活 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 命令队列 ownership 再收紧：旧 tail、已完成命令对象、已完成 drain future 都由 node 持有到生命周期结束，修 `processCommandQueue -> GC prepareDealloc` 这条 receiver 真崩点 | 本地产品节点和 Android 主包重编重装后，最新生产 UI `ui_dm_20260401_175004` 跑完 `wait_dm` 不再报 `socket closed while reading`，节点赛后仍存活 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` | `stopRelayListeners()` 在退出/repair 阶段必须 `move` 摘走 owner task 后再 stop/cancel，不能只 bump epoch 留下长活 listener/dial task | 关键缩小回归 `startUdpRelayListener skips duplicate active route`、`openDialUdpProxy replaces older dial task for the same route` 已通过 | yes |
| `app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt` | `scheduleWarm()` 必须先 `ensureRuntimeReady + ensureTailnetReady`，不能只停在 `stage=deferred` 把 4 秒多启动成本拖进发送窗口 | 生产 UI token `ui_dm_codex_20260401_193432` 已打通，点击后约 68ms 即进入 native kickoff，`remainingBudgetMs=9736`，本地 `wait_dm` 收到同名 token，instrumentation 为 `OK (1 test)` | yes |
| `libp2p/transports/nim-msquic/api/api_impl.nim` `examples/mobile_ffi/tsnet_product_node.nim` | owned datagram transport 关闭时，`ConnectionState/ListenerState` 必须等 `closeWait()` 真完成后再放掉 GC root，修 relay listener 路径里的 `readDatagramLoop -> SIGBUS` | 重编后生产 UI token `ui_dm_codex_20260401_195056` 整轮本地 `19125` 持续存活，`tailnet_status` 赛后仍可读；失败前移成真实 `connect_exact 9819ms send_with_ack_timeout` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/tsnet_product_node.nim` | `relayOwnerTasks` 里的 `TsnetRelayTaskHandle` 不能再用值对象直接 `add(handle)`；listener 自修路径会在 `relayStoreTask` 上炸 `SIGSEGV`，需要保持稳定引用形态 | 最新生产 UI token `ui_dm_codex_20260401_195735` 整轮本地 `19125` 存活，`tailnet_status` 赛后仍是 `proxyListenersReady=true`、`relayListeners=awaiting`，失败稳定为 `connect_exact 9811ms send_with_ack_timeout` | yes |
| `libp2p/transports/nim-msquic/protocol/protocol_core.nim` `libp2p/transports/nim-msquic/tests/protocol_unprotected_header_test.nim` | pure Nim builtin QUIC 的 long header parser 不能再对 `dcid/scid` 做裸切片；零长度 CID 或脏包时必须安全返回，避免本地节点炸在 `parseUnprotectedHeader` | `nim c -r libp2p/transports/nim-msquic/tests/protocol_unprotected_header_test.nim` 通过；重编后生产 UI token `ui_dm_20260401_205820` 整轮本地 `19125` 持续存活 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/switch.nim` | 继续收纯 Nim 主线下 `ui_dm_20260401_205820` 暴露出的真实 `peer_ready_failed 9814ms connect timeout`；当前 parser 崩溃和节点自爆都已排除，下一步只看 exact connect 后 `peer_ready` 为什么没按 10 秒内完成 | 下一轮生产 UI 要么恢复送达，要么把 `peer_ready_failed` 再前推成更窄的 identify/live muxer 证据 | in_progress |
| `libp2p/transports/tsnet/proxy.nim` | 把 proxy 路由注册表彻底收成“写时整体替换、读时只看 snapshot”，不要再让 `reconcileProxyListeners` 读 live `proxyRegistrations` | `tests/testtsnetproxy.nim` 维持 `16 OK`，且生产 UI 不再在 `sanitizeProxyRoutesUnsafe/proxyRouteSnapshots` 上炸 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/tcprelay.nim` `tests/testtsnetquicrelay.nim` | 把当前 tsnet/relay 主线里剩余的 keyed 共享 live `seq` 全部收成 keyed 容器：`relayListenerStates` 改 `OrderedTable[route,state]`，`relayOwnerTasks` 改 `Table[routeId,handle]`，不再靠锁包着 `seq` 原地 `add`/覆盖 | `nim c -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquicrelay.nim` 通过，`build/tsnet_product_node` 重编成功；同 route 重复 stage 更新仍只保留 1 条状态 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/libnimlibp2p.nim` | 继续收这轮新前移出的 `udp listener task start routeId=3 -> startup delay -> 崩溃/修复风暴`，确认为什么已有两个 listener 正常时还会再拉新 route | 最新生产 UI 发送窗口里本地 `19125` 已持续存活，不再复现这条本地 SIGSEGV；主线已前移到 Android 进程 crash | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` | 继续收当前最新 `Process crashed`；先确认是不是 Huawei 输入法 / ICU cleaner / draftText 路径把 Android 进程自己打死，而不是 DM 网络层 | `adb logcat -b crash` 不再出现 `com.unimaker.native.debug` 的 `SIGBUS`，生产 UI 再次回到真实 DM 发送结果 | yes |
| `libp2p/transports/tsnet/proxy.nim` `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnetproviderinapp.nim` `libp2p/transports/tsnetprovider.nim` `libp2p/transports/tsnettransport.nim` | transport 外层 repair 只在 provider 的 proxy route 注册表真空时才允许重激活；`ready < expected` 这种内层 listener 自修中的粗告警不能再触发 transport 级 listener 重建 | `tests/testtsnetproxy.nim` 为 `16 OK`，`tests/testtsnettransport.nim` 编译通过，本地 `tsnet_product_node` 可正常拉起，后续生产 UI 不再出现新的 `udp listener task start routeId=3` | in_progress |
| `/Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` | 让这轮新主包真正覆盖安装到手机；当前 Nim 改动已进 APK，但 `adb install -r` 还停在设备侧确认，不能继续拿旧包跑生产 UI | `dumpsys package com.unimaker.native.debug` 的 `lastUpdateTime` 更新到这轮构建之后，再继续 `wait_dm + instrumentation` | in_progress |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` | 继续收 Android 这轮“UI 先报 queued 超时、native 后面还在跑”的真错位；发送窗口里后台 `bridge_social_sync`、诊断轮询、connectedPeers/discovery 不能再把单线程命令队列堆到几十条 | 最新生产 UI 不再先报 `result not shown` 或 `Process crashed`；要么 10 秒内送达，要么把 `queued ... send_with_ack_timeout` 继续前推成具体是哪类后台命令堵住了 kickoff | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` | relay listener 诊断读路径必须对默认/nil 字符串硬安全，不能再让 `relayListenerStatesPayload` 这种状态读因为空字段直接把本地节点打死 | 本地 `19125` 重启后 `tailnet_status/local_info` 稳定可读，后续生产 UI 再次跑过也不复现 `relayListenerStateSnapshots -> ownedText` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` | 把 `incoming/bridge_attached` 后的 listener published 状态收成真实 ready，不能再因为 ready cache 或 owner 活跃路由状态被重写，就误触发 `listenerNeedsRepair` 和 transport 外层重建 | 最新本地 `tsnet_product_node` 在完整发送窗口内持续存活，生产 UI 已不再复现旧的 `listenerNeedsRepair=true -> rawGet/SIGSEGV` | yes |
| `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt` | 清掉 Android 发送链会跑到的 `Regex/RegexMatcher`，把 app 进程自己的 `ReferenceQueueDaemon -> MatcherNative_free` 前推掉 | 最新生产 UI token `ui_dm_20260402_132721` 已不再报 `Process crashed`，而是回到真实 `connect_exact 5817ms send_with_ack_timeout` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/switch.nim` `app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 继续收当前真实 `connect_exact 5817ms send_with_ack_timeout`；现在本地节点和 Android 进程都不先崩了，下一步只看 exact connect 后半段为什么仍没在 10 秒内完成送达 | 生产 UI 要么直接送达，要么把 `connect_exact` 再前推成更窄的 identify/live muxer/ack 证据 | in_progress |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 显式 `/tsnet` seed 发送前的 tailnet ready 判定不能再硬等本机 `proxyListenersReady`；这属于入站 published listener 健康度，不是 sender 出站前置条件 | 最新生产 UI 不再报 `runtime_prepare ... tailnet_not_ready`，而是重新回到真实 `connect_exact` 发送链 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 继续收当前真实 `connect_exact 9359ms send_with_ack_timeout`；这轮本地 Mac 全程 `relayListeners[0].stage=awaiting` 且 `wait_dm_timeout`，说明 exact connect 仍没真打到本地 listener，下一步直接钉 `connectMultiaddrExactAsync/ensurePeerReadyViaExplicitAddresses` 的 connect 超时和后续清理路径 | 生产 UI 要么直接送达，要么把 `connect_exact` 继续前推成明确的 `connect timeout / identify timeout / verified muxer` 证据 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` | 把 `relayListenerStates` 彻底赶出 readiness/repair 热路径；`listenerNeedsRepair` 只看 `relayReadyRoutes` 和“真正在跑的 task”，不能再因为诊断 stage 表或僵尸 active 标记把本地 `19125` RPC 卡死 | 本地 `tailnet_status/local_info` 秒回，`proxyListenersReady=true`、`relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners` | yes |
| `/Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` `examples/mobile_ffi/libnimlibp2p.nim` | 用带最新 native 的主包重新复跑当前真实 `connect_exact 9219ms send_with_ack_timeout`；当前本地 listener 和 RPC 阻塞链都已经恢复，下一轮只收 exact connect 本体 | 生产 UI 要么直接送达，要么把 `connect_exact` 再前推成更窄的 sender/receiver 证据 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` | keyed 路由状态表不能再用 `OrderedTable`；`route -> epoch`、`route -> routeId` 这种热路径删键必须是 O(1)，否则会在 `relayRegistryLock` 下把 `tailnet_status/local_info` 卡死 | 新本地 `tsnet_product_node` 赛后 `tailnet_status/local_info` 继续秒回，不再复现 `statusPayload -> proxyListenerReadiness` 卡锁 | yes |
| `/Users/lbcheng/UniMaker/android/core-design/src/main/kotlin/com/unimaker/nativeapp/core/design/UiComponents.kt` | 会话输入条不能在 `EditText` 创建或 update 时自动 `requestFocus + showSoftInput`；只允许用户真点击时做一次性聚焦，避免 deep link 预填 draft 把 Huawei 输入法拉起并拖崩主包 | 生产 UI 不再报 `Process crashed`，而是重新回到真实 DM 失败 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `processCommandQueue` 不能直接 `asyncSpawn(n.commandDrainTask)`；drain future 必须先落成局部强引用，再存字段和调度，避免 receiver 真收流时又炸进 `GC prepareDealloc` | 生产 UI token `ui_dm_20260402_161037` 跑完整轮后，本地 `wait_dm` 不再是 `socket closed while reading`，本地节点赛后仍活着 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/switch.nim` | 继续收当前真实 `peer_ready_failed 9139ms connect timeout`；现在本地节点、Android 主进程、RPC 状态读都已稳定，主线只剩 exact connect / peer_ready 本体 | 最新生产 UI token `ui_dm_20260402_182345` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android -> 本地 Mac 产品级发送主线重新打通 | yes |
| `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` | listener repair 不能再把 `proxyRouteSnapshots()` 瞬时缩水当 expected 真相；同时 `relayOwnerEpoch` 不能默认是 `0`，否则缺失 route 会被误判 active | 定向回归 `runtime reconcile keeps active listener when proxy snapshot shrinks` 为 `1 OK`，本地 `19125` 重启后两条 relay listener 都在 `awaiting` | yes |
| `/Users/lbcheng/UniMaker/android/core-design/src/main/kotlin/com/unimaker/nativeapp/core/design/UniMakerTheme.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt` | 继续收这轮新的 Android 冷启动 `Process crashed`；当前最新主因已不是 DM 网络，而是 `com.unimaker.native.debug` 在 deep link 启动时先 `SIGSEGV` 到 `libart/Compose` 路径 | 生产 UI 冷启动已不再先死在 `Process crashed`，后续复跑也已重新进入真实发送并最终成功送达 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/UniMakerApplication.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | Android 冷启动只允许首帧后单路 bootstrap，不再三路同时抢跑 native warm | 生产 UI 冷启动不再先报 `Process crashed` 或卡死在 Compose 启动阶段 | yes |
| `libp2p/transports/tsnet/runtime.nim` `tests/testtsnetquicrelay.nim` | listener repair 真相源收回 runtime 本地：repair 只认 runtime 自己记住的 `advertised -> raw` 映射，不再回查全局 `proxyRouteSnapshots()`；repair 队列里也不再存 `MultiAddress` 对象 | 定向回归继续通过，生产 UI 再跑时本地不再炸在 `proxyRouteSnapshots` 或 `rawSocketFromAddress` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 收成功后赛后的唯一剩余真问题：`processCommandQueue -> decRef` command drain 生命周期崩溃 | 最新成功 token `ui_dm_20260402_182345` 赛后本地 `19125` 仍继续存活，`tailnet_status/local_info` 均可秒回 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 显式 `/tsnet` seed 的 sender 路径不能在 `bridge.sendDirectText()` 前再跑 `ensureTailnetReady()`；这会先把 10 秒预算烧在 UI 层，native 真进 `cmdSendDirect` 时只剩约 1 秒 | 生产 UI 已不再出现 `connect_exact 1128ms send_with_ack_timeout` 这种“预算先在 UI 层烧掉”的假快超时，并已重新成功送达 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | listener repair 后再次发布本地 tsnet peer info 时，不能再遍历 live `peerInfo.listenAddrs`；发布路径必须优先只读 `peerInfoListenAddrTexts` 快照，避免再次炸进 `publishTsnetLocalPeerInfo -> safeMultiaddrText -> MultiAddress.$ -> decRef` | 最新成功 token `ui_dm_20260402_182345` 前后，本地 `19125` 一直存活，`tailnet_status/local_info` 继续秒回，没有再复现这条发布路径崩溃 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt` `libp2p/transports/tsnet/quicrelay.nim` | receiver authoritative ack 必须先回、instrumentation 只认 send status、idle `awaiting` route 在 `published=false` 时不能再 preserve serving route | 连续两轮 token `ui_dm_writefix_ack_9_20260402_203335` 与 `ui_dm_writefix_ack_10_20260402_203430` 都要做到 instrumentation `OK (1 test)`、本地 `wait_dm` 收到同名 DM、赛后 `tailnet_status/local_info` 秒回 | yes |
| `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` | repair 不能在 persistent accept stream 正在收流量时再起第二个 UDP listener；`incoming/ready/bridge_attached` 必须直接压住 repair，repair 入口也不能再把字符串 route 重新还原成 live `MultiAddress` | 本地节点在“成功后一轮立刻再发”的场景里不再炸进 `rawSocketFromAddress` 或 `closeOwnedDatagramTransport`，赛后 `tailnet_status/local_info` 继续秒回 | yes |
| `libp2p/protocols/dm/dmservice.nim` `tests/testdirectdm.nim` | 把 authoritative ACK 收成生产级长活双向 session：同一 peer 维持 1 条持久 DM session，`dm/ack` 都在同一 session 内帧化；同时补齐 `DirectMessageService.stop()`，停服时先取消 reader、关 session，避免 `closeWithEOF` 抢读 | `tests/testdirectdm.nim` 最新 `3 OK`，新增的 `activeSessionCountForPeer == 1` 断言通过，`build/tsnet_product_node` 与 Android `:app:assembleDebug` 都已重编成功 | yes |
| `libp2p/protocols/dm/dmservice.nim` `tests/testdmsessionstate.nim` | 把 DM 传输层正式收成小而硬的显式状态机：生命周期与活动计数分离，`dm/ack` 收发、ACK 等待、reader attach、close/fail 都走统一迁移，并暴露只读 `sessionControlSnapshots()` | 定向回归 `testdmsessionstate.nim` 为 `1 OK`，session 在 ACK 成功后稳定回到 `DmSessionLive + ReaderAttached` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/proxy.nim` | relay listener/dial 长活 task 只拿稳定 `TsnetQuicRelayEndpoint`，不再在循环里重 parse raw `relayUrl`；`proxyRouteCount()` 也改成 owner 计数表，不再在 idle reconcile 扫 live `proxyRegistrationSnapshots` | `tests/testtsnetquicrelay.nim`、`tests/testtsnetproxy.nim`、`build/tsnet_product_node` 全部重编通过，生产 UI token `ui_dm_state_machine_20260403_080113` / `ui_dm_state_machine_20260403_080324` 再次 `OK (1 test)` 且本地 `wait_dm` 收到同名消息 | yes |
| `libp2p/builders.nim` `tests/test_msquic_builder.nim` | QUIC-only 标准 builder 必须收成纯 `MsQuicTransport -> MsQuicMuxer` 语义，不再表面挂 `mplex` 或隐式 secure default，避免架构层把 QUIC 原生多流和 TCP 上层 mux 混在一起 | `tests/test_msquic_builder.nim` 验证 `upgrader.secureManagers.len == 0`、`MuxedUpgrade.muxers.len == 0`，`build/tsnet_product_node` 重编通过 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` `ops/systemd/nim-tsnet-gateway.service` | 把 relay listener 所有权正式收成 generation token，`listen -> accept/await/ready` 全链带 generation；并把远端 gateway 同步到同一协议代际，彻底消掉 `listener generation missing` | 本地新 `tsnet_product_node` 对远端新 gateway 复跑时，不再出现 `registration missing generation`，route 能稳定从 `known` 进入 `awaiting/incoming` | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/transports/tsnet/quicrelay.nim` | 收生产 UI 成功后的赛后尾部崩链：tsnet-only publish/reconcile 全程改成字符串快照优先，不再在 `mergeTsnetListenAddrsIntoPeerInfo/refreshLanAwareAddrs` 对 live `MultiAddress` 做 `$`；同时去掉 `markRelayListenerStage*` 对冗余 `relayListenerStates` 的热路径写表 | 最新生产 UI token `ui_dm_state_machine_20260403_081531` 成功后，`tailnet_status/local_info` 继续秒回，不再 `Connection refused` | yes |
| `/Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` `Android 生产 UI + 本地 tsnet 产品节点` | 用带长活双向 session ACK 的新主包重跑 Android 生产 UI 1:1 路径，确认 `dm_ack_wait` 不再回退，而且没有偷偷再开第二条 ACK 流 | 新主包覆盖安装后，生产 UI 连续两轮 `ui_dm_sessionack_20260403_062425` / `ui_dm_sessionack_20260403_062530` 都为 `OK (1 test)`，Android logcat 已闭环到 `dm_ack_done` / `cmdSendDirect success` / `sendThreadMessage success`，本地 `social_state.json` 已写入两条 `status=received` 同名消息 | yes |
| `libp2p/dialer.nim` `examples/mobile_ffi/libnimlibp2p.nim` | native QUIC 显式 seed 发送不能再先 eager identify，再 `peer_ready_deferred_to_dm_dial` 二次 fresh dial；同一请求必须 exact-connect 后直接复用 `verifiedMuxer` 发送 | 最新生产 UI token `ui_dm_exact_reuse_20260403_094328` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android 日志已闭环到 `path=verified_live_muxer` | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` `tests/testtsnetquicrelay.nim` | 把 listener `repair` 从 `ready < expected` 猜测触发改成 route task 退出后的显式 repair request，彻底禁止 `incoming/readLp` 窗口误 repair | 复跑时本地日志持续为 `listenerNeedsRepair=false reason=serving_stage`，本地节点赛后继续存活，Android 失败不再是 `dm_ack_wait/socket closed/SIGSEGV`，而是前移成真实 `connect_exact` | yes |
| `libp2p/transports/tsnet/proxy.nim` `tests/testtsnetproxy.nim` | 把 `resolved remote` 这支的热路径和快照查找彻底收回固定 `sharedKey`，不再让 `rawText/advertisedText` 进入有效性判定和 lookup；压掉 `registerResolvedRemote -> refreshResolvedRemoteSnapshotsUnsafe -> MultiAddress.init` | `tests/testtsnetproxy.nim` 为 `18 OK`，生产 UI 复跑后本地 `19125` 不再在这条 receiver 路径上崩，`tailnet_status/local_info` 赛后继续可读 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/core-design/src/main/kotlin/com/unimaker/nativeapp/core/design/UiComponents.kt` | 继续收当前新的唯一 blocker：production UI 走到 `verified_live_muxer` 后，Android 主进程在 `ReferenceQueueDaemon -> libicu_jni MatcherNative_free` 崩溃；先把发送窗口里所有会拉起输入法/文本管线的主动焦点和隐藏 TextField 路径钉死 | production UI 不再报 `Process crashed`，而是重新回到真实 `wait_send_ack / dm_ack / send success` 结果 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/protocols/dm/dmservice.nim` | 显式 `/tsnet` seed 发送的 `peer_ready` 必须等价于“本次已建好的 DM session”，不能再拿 `verifiedMuxer` 猜会话；warm 完成后要用 `sessionConnKey` 贯穿到真实 send | 最新生产 UI token `ui_dm_session_connkey_20260403_205629` 已 `OK (1 test)`，Android 日志出现 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done`，本地 `wait_dm` 收到同名消息 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | truth route 预热不能再依赖 Compose 重组，也不能再按 `peerId` 粗暴复用旧 warm attempt；route 入口必须直接准备 truth scene，warm attempt 必须至少带 seed key | 最新 token `ui_dm_warm_key2_20260403_213119` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；后续只剩把这条约 `7.1s` 成功路径继续压回 `1-3s` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 显式 `/tsnet` seed bootstrap 不能再拆成“generic connect + warmSession”两段；权威 primitive 必须直接建立 DM session 并产出 `sessionConnKey`，否则首条业务流还会被重新拉回 live protocol select 路径 | 最新生产 UI token `ui_dm_session_exact_20260403_220040` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，旧的 `connect_exact_failed ... live connection protocol select timeout` 已不再是主线 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `examples/mobile_ffi/tsnet_product_node.nim` | 继续收 success 之后的 listener published 稳态漂移；当前发送成功后本地节点仍存活，但 `tailnet_status.relayListeners=[]`、`proxyListenersReady=false`，`local_info.listenAddrSource=synthesized`，需要把赛后 published listener 状态重新钉死 | 连续成功发送后，赛后 `tailnet_status/local_info` 仍保持 `relayListeners` 非空、`proxyListenersReady=true`、`listenAddrSource=relay_listeners` | in_progress |
| `Android 生产 UI + 本地 tsnet 产品节点` | 继续把当前成功/失败链前移到真实 `connect_exact` 本体，并最终把成功路径压回 1 到 3 秒 | 生产 UI 1:1 路径稳定不再被误 repair 干扰；随后连续多轮 `OK (1 test)`，且 `sendThreadMessage success durationMs` 大多回到 1 到 3 秒 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` | bootstrap exact-connect 任务不能再按 `peerId` 单键复用；同一 peer 的不同 seed/bootstrap 意图必须按 `peerId + exact selectedAddrs` 归属，sender 也只能以 live `sessionConnKey` 作为 bootstrap 已完成的真相 | 最新生产 UI token `ui_dm_bootstrap_key_20260403_230320` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息，旧的 `bootstrap direct session not warmed` 已不再复现 | yes |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnettransport.nim` | 继续收赛后 published listener 稳态投影的最后一层偏移；当前已不再完全掉成空集，但仍会退成单条 `/ip4` `stage=known`，并把 `proxyListenerReady` 算成 `0` | 连续成功发送后，赛后 `tailnet_status` 回到 `proxyListenersReady=true`、双栈 `relayListeners` 非空且不低于 `incoming/awaiting`，`local_info.listenAddrSource=relay_listeners` 且不再只剩单条地址 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` | 把 published listener 投影收成“qrelay 最后确定 stage + runtime owner cache”的确定性状态机，不再允许瞬时读空把赛后 published route 抹掉 | 生产 UI 后即使发送失败，`tailnet_status.proxyListenersReady` 仍为 `true`、双栈 `relayListeners` 非空，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续可读 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | direct session warm 任务不能只靠 “join 现有 bootstrap task” 就算完成；显式 seed 发送必须以 live `sessionConnKey` 或已完成的 warm result 为真相，不能再被未完成旧 task 拦成 `bootstrap direct session not warmed` | 生产 UI token `ui_dm_bootstrap_intent_20260404_052034` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已闭环到 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | receiver social state 不能再在每条入站 DM 后处理里整份 `parseJson(social_state.json)`；正确边界是“内存已解析快照 + clone-on-read + persist 时原子发布新快照” | 生产 UI token `ui_dm_social_cache_20260404_054113` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，节点赛后 `tailnet_status/local_info` 继续秒回 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `hostNetworkStatus` 不能再把 `JsonNode` 当运行真相；idle/public-ip-probe/side-effects 热路径必须只读值对象快照，JSON 只能留在 FFI/status/event 出参边界 | 带 `HostNetworkStatusSnapshot` 的生产 UI token `ui_dm_host_snapshot_20260404_062321` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status/local_info` 继续秒回且节点不再在 `idleWakeLoop -> sanitizeHostNetworkStatus` 崩 | yes |
| `libp2p/services/noderesourceservice.nim` | macOS 本地资源采集不能再走 `osproc` 的 `poUsePath + 继承环境` 热路径；命令执行边界必须收成绝对路径 + 显式空环境，避免 fresh 本地节点再次炸进 `envToCStringArray` | `tests/testnoderesource_service.nim`、`build/tsnet_product_node` 重编通过；fresh `19125` 节点重新拉起后 `tailnet_status/local_info` 正常，后续生产 UI token `ui_dm_direct_truth_20260404_074124` 成功且赛后节点继续存活 | yes |
| `Android NativeAppViewModel/BridgeFacades/ChengLibp2pNative/libp2p_bridge.cpp/examples/mobile_ffi/libnimlibp2p.nim` | 把 relay/direct ready 快照里的 `sessionConnKey` 贯通成 sender 唯一可传递真相：Android 会话页先暴露 `中继会话 READY` 与 `preferredSessionConnKey`，`waitRelayReady` instrumentation 只在 relay session ready 后点击；native kickoff 任务必须继续携带这条 key，且 relay-prepared reuse 在真正发送时必须保持 `path=relay_ready_session relayOnly=true`，不能再掉回 `direct_ready_session` 假状态 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；华为安装器两步确认后主包 `lastUpdateTime=2026-04-07 05:27:05`。fresh 本地 seed `/ip4/100.64.148.187/udp/49223/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0528_truthfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息；logcat 已坐实 `relay ready before tap -> sendConversationText relay_session_ready -> preferredSessionConnKey=532539786816 -> cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=719` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `tailnet_status` FFI 导出不能再在 Android 调用线程里直接 `buildTailnetStatusPayload()`；必须统一走 owner `cmdTailnetStatus`，只把最终文本快照返回 JNI，避免 diagnostics 轮询再次把 app 打进 `libp2p_tailnet_status_json` | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；Android 主包重装后 `lastUpdateTime=2026-04-07 05:43:04`。fresh 本地 seed `/ip4/100.64.148.187/udp/59468/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，token `ui_dm_20260407_054359_tailfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`、节点持续存活；`adb logcat -b crash` 不再出现 `com.unimaker.native.debug` 的 `SIGSEGV`，节点日志已坐实 `submitCommand cmdTailnetStatus -> statusPayload step=... -> runCommand completed kind=cmdTailnetStatus` | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `examples/mobile_ffi/libnimlibp2p.nim` | 继续把 production UI 的 relay 预热从“send-time 等待 ready”前移到“点击前已 ready”；当前 `waitRelayReady=false` 在 fresh 冷启动下虽已能成功，但还会先经历 `中继会话 RUNNING dm_transport_connect_begin -> dm_protocol_select_begin`，最终耗时仍显著高于 relay RTT 下界 | fresh 冷启动 no-wait 样本里，点击后直接命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，且 `sendThreadMessage success durationMs` 稳定回到约 `1.3s~1.6s`，不再像当前 token `ui_dm_20260407_054359_nowait_cold` 这样要等到 `4509ms` | in_progress |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 把 Android direct warm 的网络真相从 `ownerKey` 解耦：ready snapshot 与 active warm 复用只认 `peerId + seedKey`，owner 只属于 UI 生命周期 | 生产 UI token `ui_dm_peer_seed_truth_20260404_082630` 继续 `OK (1 test)`；同时 logcat 能明确区分“没吃到 ready snapshot”与“warm 已完成但 sender 没复用” | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | accept stream 已进入 `incoming/ready/bridge_attached/serving` 后，published 真相只能由 accept stream + keepalive 持有；validation loop 必须退出 `route_status` probe，不能再保留第二条 validation client 真相源 | 赛后节点不再崩在 `probeRoutePublishedOnValidationClient -> relayRouteStatus`；生产 UI 成功后 `tailnet_status/local_info` 继续可读且节点仍存活 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | bootstrap exact-connect task 不能再自己决定更长总预算；前台发送和 bootstrap task 必须共用同一份调用方 deadline，同时跨意图未完成 task 不能 join | 最新生产 UI token `ui_dm_budget_truth_20260404_111708` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息，旧的 `bootstrap direct session not warmed` 已被前推掉 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `examples/mobile_ffi/tsnet_product_node.nim` | `receivedDmLog` 不能继续跨线程共享 live `JsonNode`；接收日志必须收成值对象快照，`wait_dm/dmSnapshot` 和 game ingest 读取时各自物化 JSON，不能再一边删除原日志一边复用同一批 ref 节点 | `wait_dm` 空跑正常返回 `wait_dm_timeout` 而不是把 `19125` 打死；随后生产 UI token `ui_dm_received_snapshot_20260404_113213` 为 `OK (1 test)` 且本地 `wait_dm` 收到同名消息 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `socialStateParsedSnapshot` 不能再在 `loadSocialStateUnsafe/persistSocialStateUnsafe` 热路径里整份 `cloneJson`；`socialStateMemLock` 下必须只有一棵 live tree，文本快照只负责落盘和出参 | 生产 UI token `ui_dm_social_owner_tree_2_20260404_120905` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status/local_info` 继续秒回 | yes |
| `Android 生产 UI + 本地 tsnet 产品节点` | 继续把当前真实 tap-to-sent 成功路径压回 `1-3s`；Android 重复 warm gate、bootstrap 队列优先级、hostNetworkStatus 热路径 JSON 都已收正，当前剩余最大头是 native `warmSeededDirectSessionAsync/bootstrapSeedConnectExactAsync` 本体 | 后续连续多轮 `OK (1 test)`，且 `sendThreadMessage success durationMs` 大多回到 `1-3s` | in_progress |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 把 truth-route direct target 收成“常驻 desired target + reconcile loop”，并禁止 relay fallback 清掉 active direct target；direct 与 relay 必须独立运行，send 只消费当前 ready 路径 | token `ui_dm_direct_state_machine_2_20260404_143100` 为 `OK (1 test)` 且本地 `wait_dm` 收到同名消息；logcat 同时坐实当前成功链是 `relay_fallback targetStage=running -> ready_wait -> dm_ack_done` | yes |
| `Android 生产 UI + native direct path state` | 继续把当前成功链从 `relay_fallback -> ready_wait` 推进到“direct target 在点击前 ready，点击时自动选直连”；发送不能再靠 relay 成功掩盖 direct-path 仍未 ready | 后续生产 UI logcat 要稳定出现 `direct_target_ready` 或 `path=warmed_session_conn`，并把 tap-to-sent 压回 `1-3s` | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `Android 生产 UI + 本地 tsnet 产品节点` | 把显式 truth-route send 收成“direct/relay 并行状态机，send 只消费 ready session”；禁止 send 自己在 explicit `/tsnet` 路径上重新 fresh dial/fallback | 最新 token `ui_dm_dual_path_20260404_192151` 已 `OK (1 test)`，logcat 已坐实 `path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success`，`dm_fresh_dial_begin` 已从这条路径消失 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `Android 生产 UI + 本地 tsnet 产品节点` | `networkDiscoverySnapshot` / 节点列表观测线程必须只读稳定 direct-target 快照，不能再直接碰 live `bootstrapSeedConnectTasks`；观测和发送状态机彻底分层 | 最新 token `ui_dm_discovery_snapshot_fix_20260404_204711` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，`adb logcat -b crash` 不再出现 `libp2p_network_discovery_snapshot` 崩溃 | yes |
| `libp2p/protocols/dm/dmservice.nim` | DM 层拨号锁必须按 `peer + intent + direct_or_relay` 分桶，后台 direct warm 和前台 relay route warm 不能再共用一把锁 | token `ui_dm_dial_lock_split_20260404_210705` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，`direct message dial lock timeout` 不再复现 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | 显式 `/tsnet` send 选路必须同时观察 direct/relay 两张 ready 快照，send 只消费当前 ready session；`direct` 与 `relay` 同时 ready 时优先 `direct`，否则走 `relay` | token `ui_dm_direct_relay_race_20260404_211303` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；sender 稳定走 `path=relay_ready_session`，说明这轮是 direct 未 ready 时的正确 relay 选择，不再是状态机误判 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | direct path 的打洞成功率不能再靠日志或单次发送结果反推；必须收成 owner 侧稳定统计快照，并挂进 peer path projection / connectedPeers / networkDiscovery 输出链 | fresh 复跑里节点列表和路径投影都能读到 `directTargetAttemptCount/directTargetSuccessCount/directTargetFailureCount/...`，且生产 UI token `ui_dm_direct_stats_20260404_213100` 继续 `OK (1 test)` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | direct owner 吸收现成 live `direct/punched_direct/local` 会话时，也必须记一次 ready 成功迁移；不能只把状态切成 `ready` 却不更新 `attempt/success`，否则 UI 会长期显示假 `打洞 0% 0/0` | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 为 `1 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；ready 迁移现已统一走 `markDirectPeerPathStateReady()` | yes |
| `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnetprovidertypes.nim` `tests/testtsnetproxy.nim` | exact `/tsnet` QUIC target 不能再只有 `local -> relay` 两支；正确边界是 `local -> direct -> relay`，并用显式 `TsnetProxyDialMode.DirectRoute` 区分直连 exact target | `nim c -r ... tests/testtsnetproxy.nim` 为 `21 OK`，新回归已覆盖 exact target 优先 direct route | yes |
| `libp2p/transports/tsnet/quicrelay.nim` | relay keepalive/validation 的子 future 不能再只 `cancelSoon()` 就立刻释放 stream/client；超时包装和 `awaitPersistentIncomingWithRouteValidation()` 必须 `cancelAndWait()` 真正 join 子任务，避免本地节点沿 `keepRelayConnectionAlive -> readJsonMessageWithTimeout -> readBinaryFrame -> msquicstream.read` 赛后自杀 | 新主包 `lastUpdateTime=2026-04-05 15:28:47` 的 production UI token `ui_dm_20260405_153022` 下，本地 fresh `19125` 节点赛后仍能继续回 `tailnet_status/local_info`，`relayListeners[*].stage=incoming`，不再复现这条本地 keepalive 崩栈 | yes |
| `Android 常驻 direct target 状态机 + tsnet 直连打洞状态` | 继续把 direct path 从“发送时正确 fallback relay”推进到“点击前 direct 已 ready”；当前 send 状态机已正确，剩余问题已经缩成 direct path 自身没在点击前 ready | production UI fresh 复跑里，sender 出现 `path=direct_ready_session`，并且本地 `tailnet_status.tailnetDirectRouteCount/tailnetPunchedRouteCount` 不再长期为 `0` | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/core-model/src/main/kotlin/com/unimaker/nativeapp/core/model/DomainModels.kt` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` `/Users/lbcheng/UniMaker/android/feature-nodes/src/main/kotlin/com/unimaker/nativeapp/feature/nodes/NodesScreen.kt` | 对同一节点统计并显示打洞成功率、直连和中继 DM 延时；native 必须按真实 `sendPath` 聚合 peer 级路径延时，Android 只读这份快照展示 | `build/tsnet_product_node` 重编通过，`tests/testmobile_ffi_latency_probe_only.nim` 编译通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；`adb install -r app-debug.apk` 后生产 UI token `ui_dm_20260405_112419` 的诊断串已出现 `打洞 0% 0/0 · 直连DM -- · 中继DM --` | yes |
| `libp2p/transports/tsnetprovidertypes.nim` `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnetprovider*.nim` `libp2p/transports/tsnettransport.nim` `libp2p/dial*.nim` `libp2p/protocols/dm/dmservice.nim` `examples/mobile_ffi/libnimlibp2p.nim` | 把 explicit `/tsnet` send 收成“direct/relay 独立并行，send 只消费 ready session”的单一状态机：新增 `TsnetUdpExactDialPlan`、`DirectPeerPathState`，direct reconcile 改成 owner 单飞 loop，send-time 不再 fresh dial 乱猜，direct ready 时稳定优先 direct | `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`nim c -r ... tests/testtsnetproxy.nim` 为 `23 OK`，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过 | yes |
| `Android truth route direct desired -> native owner` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt` | 页面进入会话时必须先把 direct desired 真正落到 native owner，不能再因为 `runtime_not_ready` 或 handle 尚未初始化把第一次声明吞掉；direct setter 只补 `init(handle)`，不 `start()` | 新主包 `lastUpdateTime=2026-04-05 17:12:11` 后，production UI token `ui_dm_20260405_171226` 仍失败，但真实前因已经缩成 `send_with_ack_timeout stage=envelope_ready`；`local_tailnet_ready_wait` 已消失，relay warm 也确实独立启动。下一步只剩继续把 `cmdSendDirectKickoff -> waitForDirectOrRelaySessionReady/warmSessionDetailed(relayOnly=true)` 在预算内收口 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/transports/tsnettransport.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | relay warm 必须在页面进入后尽早启动并真正等待 UDP bridge ready；当前已把 `waitForProviderUdpDialReady()` 收成真等待，也把 truth-route runtime 提前到页面进入，但 relay session 仍然在 send deadline 内差最后 `sessionConnKey` 收口 | 新主包 `lastUpdateTime=2026-04-05 18:58:26` 的 production UI token `ui_dm_20260405_185956` 中，`triggerTruthRouteRuntimeActivation runtime_done` 已在发送前 1.4 秒出现，`runPeerRouteSessionWarmAsync start source=android_truth_route_activation` 已提前到 send 后约 0.7 秒，`tsnet quic dial done` 已在 timeout 前出现；当前真实 blocker 是 `relayStatus=running relayConnKey=0` 直到 send deadline 结束，随后 warm 自己才以 `quic_dial_timeout` 收口 | in_progress |
| `libp2p/stream/connection.nim` `tests/testconnection.nim` | `Connection.readLp(maxSize)` 不能再按 `maxSize+10` 直接申请整块缓冲；改成“前缀探测 + 按剩余长度分块读取”，避免本地节点在入站 DM 首帧读上炸进 `connectionReadLpAsync -> newObj` | `nim c -r tests/testconnection.nim` 为 `5 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetproxy.nim` 通过，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh 本地节点 seed `/ip4/100.64.148.187/udp/55797/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260405_195634` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status/local_info` 继续秒回 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | relay route session warm 的 deadline 不能再靠 owner 外层过期去粗暴 cancel；warm 自己必须按 `warmBudgetMs` 收口并发布真实失败阶段，不能再把真因盖成 `route_session_target_cancelled` | 新主包生效后，同一条 production UI 失败已从 `route_session_target_cancelled` 前推成 `quic_dial_timeout`，`runPeerRouteSessionWarmAsync` 会直接写回 `direct message fresh dial timeout / quic_dial_timeout` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | owner 线程里的 `ensureLocalTsnetReady()` 不能再给自己排 `cmdTailnetWarm` 造成队列自锁；要直接 owner-inline 启动 tailnet warm worker | fresh 本地 `19125` 节点已坐实 `providerReady=true`、`proxyListenersReady=true`，production UI 不再停在 `local tailnet provider not ready`，真实失败前推到 `quic_dial_timeout` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/transports/tsnettransport.nim` `libp2p/protocols/dm/dmservice.nim` `libp2p/dialer.nim` | 继续把 relay exact 路径从 `dm_switch_dial_wait_begin` 前推到真实黑盒：补齐 `switch.dial -> transport.dial -> transport.upgrade -> stream open/select` 的主动日志，不再等最终超时才猜 | 下一轮 production UI 里必须看见 `dialer transport upgrade begin/done`、`dialer stream open begin/done`、`dialer protocol select begin/done` 中的最后一站，或者拿到更靠前的真实失败阶段 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt` | 把 `send_with_ack` 诊断收成 request-local 无锁快照：native 不再把 `queuePending/runningKind` 混进 request payload，Android timeout 只读 `pathSelection/sendPath/directTargetStatus/relayTargetStatus`，并给 terminal state 一个极短 settle 窗口 | production UI 失败串已不再回到假 `send_with_ack_timeout envelope_ready`；最新 token `ui_dm_20260405_230314` 已直接前推成 `connect_exact_failed ... local tailnet provider not ready` | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt` `examples/mobile_ffi/libnimlibp2p.nim` | fresh install 后第一次进入会话时，truth-route activation 必须在 sender 前把 local tailnet provider 真正拉到 `ready`；当前 request-local 诊断已经收正，新的唯一主线就是首发 `provider_wait_failed` 时序 | 下一轮 production UI 首发不能再失败在 `local tailnet provider not ready`；要么继续前推到更深的 direct/relay warm 失败阶段，要么直接送达 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` | direct 快照不能只认 direct worker 自己建出来的 `sessionConnKey`；只要 peer 上已经有 live `direct/punched_direct/local` 会话，owner 就必须立刻吸收成 direct ready，sender 直接优先吃这条直连 | 新主包 `lastUpdateTime=2026-04-06 00:13:48` 下，production UI token `ui_dm_20260406_001425` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已坐实 `runPeerRouteSessionWarmAsync warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `tests/testmobile_ffi_social_state_cache.nim` | `social_state` 只允许“首次读盘，之后只认节点内存快照”；receiver 后处理和 diagnostics 不再每次 `readFile + parseJson`，避免 DM 后处理把本地 `19125` 节点炸进 `loadSocialStateUnsafe -> parseJson -> Table rawInsert` | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 通过；新主包 `lastUpdateTime=2026-04-06 04:59:06` 后，旧的 receiver social-state 崩链不再复现，本地节点不再在 ACK 后处理窗口自杀 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `social_state` 文件路径不能继续在收消息后处理热路径里现拼；`cfg.dataDir.get() / "social_state.json"` 必须在节点初始化时一次算好并固化，避免 `persistSocialStateUnsafe -> socialStatePath -> rawNewString` 再把本地 `19125` 节点炸死 | crash report `/Users/lbcheng/Library/Logs/DiagnosticReports/tsnet_product_node-2026-04-06-083212.ips` 已坐实旧崩链；改成 `node.socialStatePathText` 后，fresh 本地 seed `/ip4/100.64.148.187/udp/64906/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_083742` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，节点日志已闭环到 `persistSocialState begin -> done -> postprocess done`，赛后 `tailnet_status/local_info` 继续正常 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `send_with_ack` 失败态不能再把 `sendPath` 假写成 `direct_ready_session`；同时补 `reconcilePeerRouteSession` 的 spawn/running/retry/live 日志，主动挖出 relay warm 的真实时序 | 新主包 `lastUpdateTime=2026-04-06 05:09:12` 后，production UI token `ui_dm_20260406_050950` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已坐实 `reconcilePeerRouteSession spawn -> waitForDirectOrRelaySessionReady ... relayStatus=running -> runPeerRouteSessionWarmAsync warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session -> dm_ack_done -> sendThreadMessage success durationMs=5818` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` | `direct_session_target_set/clear` 的 owner 边界必须统一：FFI 线程不能再直接改 `directSessionTargets/directPeerPathStates/peerRouteSessionTasks`，而 `cmdDirectSessionTargetSet` 也不能错误要求 `n.started=true`，否则页面进入时的 direct desired 会被整批拒掉，sender 只会看到 `directTargetStatus=missing` | 新主包 `lastUpdateTime=2026-04-06 05:25:16` 后，fresh 本地节点 seed `/ip4/100.64.148.187/udp/56321/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_052534` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已坐实 `reconcilePeerRouteSession spawn source=android_truth_route_activation` 在 sender 前触发，随后 `runPeerRouteSessionWarmAsync warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session -> dm_ack_done -> sendThreadMessage success durationMs=5918` | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | truth-route direct 预热不能再跑在公用 `Dispatchers.IO` 上等启动期其它重活让路；把 prepare 阶段的 runtime/tailnet warm 前移，并把 `triggerTruthRouteRuntimeActivation` 收成专用单线程 worker，确保 route 进入就能立刻发出 `android_truth_route_prepare` | 新主包 `lastUpdateTime=2026-04-06 06:06:36` 后，沿同一 fresh `19125` 节点 seed `/ip4/100.64.148.187/udp/49873/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 跑 production UI token `ui_dm_20260406_060709` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；关键日志已前移成 `runPeerRouteSessionWarmAsync ... source=android_truth_route_prepare -> warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session`，`sendThreadMessage success durationMs=4099` | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt` | truth-route direct desired 只能有一个 Android 写入源；`prepare`、`activation`、`syncDesired...` 不能再同时直接写 native，否则会把 source 覆盖回 `android_truth_route_activation`，还会把同一路 truth route 重复 prepare 一次 | 新主包 `lastUpdateTime=2026-04-06 06:16:39` 后，fresh 本地节点 seed `/ip4/100.64.148.187/udp/49974/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_061746` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；`runPeerRouteSessionWarmAsync` 已稳定回到 `source=android_truth_route_prepare`，`cmdSendDirect connectivity ... path=direct_ready_session`，`sendThreadMessage success durationMs=3477` | yes |
| `libp2p/multistream.nim` `libp2p/protocols/dm/dmservice.nim` `libp2p/transports/msquictransport.nim` `libp2p/transports/nim-msquic/api/api_impl.nim` `tests/testmultistream_stability.nim` | 继续把 direct 热路径压回 `3s` 内：v1 multistream 已收成 handshake 与首个 protocol 同首飞，再补齐 `protocol_select_done -> dm_write_begin`、`session frame write`、`incoming handler -> readLp begin/done`、`MsQuic stream packet send/receive` 主动日志，直接判定新瓶颈归属 | `nim c -r tests/testmultistream_stability.nim` 继续通过；新主包 `lastUpdateTime=2026-04-06 06:54:58` 下，fresh seed `/ip4/100.64.148.187/udp/56611/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_065541` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，`sendThreadMessage success durationMs=2666`；日志已坐实 sender `dm_protocol_select_done -> dm_write_begin` 仅约 `102ms`、`DmFrameMessage` 写入仅 `1ms`，当前剩余大头主要是 `multistream dialer handshake read elapsedMs=730` 与 receiver `readLp begin -> done` 约 `799ms` | yes |
| `libp2p/multistream.nim` `tests/testmultistream_stability.nim` | v1 multistream 的 dialer 和 listener 都要收成同一套 coalesced 边界：dialer 一次写出 `response1 + first protocol`，listener 若在 replay buffer 里已经拿到首个 protocol，就一次写回 `response1 + protocol_ack` 或 `response1 + response2`，不再拆成两轮 write | `nim c -r tests/testmultistream_stability.nim` 为 `3 OK`；dialer 回归已钉死 `conn.writes.len == 1` 且内容是 `lpFrame(\"/multistream/1.0.0\\n\") & lpFrame(\"/test/proto/1.0.0\\n\")`，listener 回归同样只写一次 combined frame。新主包 `lastUpdateTime=2026-04-06 08:13:18` 后，fresh seed `/ip4/100.64.148.187/udp/59045/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_081333` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；app 日志已坐实 `multistream dialer handshake write begin stage=response1_select_first_proto_coalesced`，节点日志也已坐实 `pending post-handshake content -> response1_protocol_ack_coalesced`，说明真实生产流量已经命中这条 combined path，但总耗时仍是 `sendThreadMessage success durationMs=3479`，当前剩余大头不再是这轮 protocol 二次写 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | 会话页 diagnostics 不能只靠 `connectedPeersInfo` 猜 direct 统计；native `directSessionTargetStatus` 必须一并带出 `attempt/success/rate + direct/relay DM latency`，Android `refreshConversationLatency()` 直接并进当前会话的 `ConversationLatencySnapshot` | `nim c -r tests/testmobile_ffi_social_state_cache.nim`、`nim c -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 都通过；但 fresh 真机验证被新的本地 `19125` 节点热崩链挡住：production UI / instrumentation 期间节点在 `loadSocialStateUnsafe -> parseJson -> newObjNoInit` 再次 `SIGSEGV`，当前要先把 `social_state` 的 parse 热路径从 owner 收掉，统计显示才能继续真机闭环 | in_progress |
| `examples/mobile_ffi/libnimlibp2p.nim` | `social_state` 运行态必须在 `cmdStart` 一次预热成 owner 内存态；DM/content/LAN 入站热路径只做 owner 内存增量写，`libp2p_get_diagnostics_json` 必须走 `cmdDiagnostics`，不能再在调用线程旁路 parse `social_state.json` | 新主包 `lastUpdateTime=2026-04-06 09:34:10`；fresh 本地 seed `/ip4/100.64.148.187/udp/49453/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`；production UI token `ui_dm_social_owner_20260406_093421` 为 `OK (1 test)`；本地 `wait_dm` 收到同名消息；节点日志已闭环到 `handleDirectMessageNode socialState owner begin -> appendConversation done -> appendNotification done -> persistSocialState done -> postprocess done`；赛后 `tailnet_status/local_info` 继续正常 | yes |
| `libp2p/protocols/dm/dmservice.nim` | direct warm 如果已经是“点击前准备好 session”，就不能把 reader 继续拖到 sender 首条写入后才挂；正确边界是 warm 完成时异步启动 reader，但不把 reader fully ready 绑成 warm 完成条件 | fresh 主包 `lastUpdateTime=2026-04-06 10:07:37` 后，production UI token `ui_dm_20260406_100805` 为 `OK (1 test)`，logcat 已出现 `runPeerRouteSessionWarmAsync ... stage=dm_reader_async_start -> warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session -> dm_ack_done -> sendThreadMessage success durationMs=3485` | yes |
| `libp2p/transports/nim-msquic/api/api_impl.nim` | `HQUIC` fast path 不能再把裸指针直接当活句柄；注册表必须是唯一权威，已 release 的 handle 必须先在 `gHandleRegistry` 判活，不能继续 raw cast 成 `StreamState/ConnectionState` | fresh 1:1 首轮已前推出节点崩链 `quicrelay.writeJsonMessage -> msquicStreamSend -> pendingChunks.add -> SIGSEGV`；收紧 handle 权威后，同一条 fresh 闭环重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，节点未再在 `quicrelay` ready 回包时自杀 | yes |
| `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | truth-route direct target 的 Android apply 必须走单线程 owner；`prepare` 阶段 immediate sync 到 native 这条 fast path 已证伪，当前只保留 collector 串行化，不再把 declare 抢在 provider ready 之前 | 新主包 `lastUpdateTime=2026-04-06 10:48:12` 后，fresh 本地 seed `/ip4/100.64.148.187/udp/64280/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_104908` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；时序重新稳定成 `source=android_truth_route_prepare -> warm_done ... pathKind=direct -> path=direct_ready_session`，`sendThreadMessage success durationMs=3385` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `libp2p/protocols/dm/dmservice.nim` | `ackRequested=true` 的 sender 预算必须正式拆成 `pre-send` 和 `post-ready ack` 两段；receiver authoritative ACK 发送窗口是固定协议边界，不能继续让 `ready/connect` 把整份 SLA 吃光后再用几百毫秒去等 ACK | 新主包 `lastUpdateTime=2026-04-06 11:42:23`，fresh 本地 seed `/ip4/100.64.148.187/udp/55840/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_114312` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已重新闭环到 `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=4199`，假 `ack timeout` 已被前推掉 | yes |
| `libp2p/protocols/dm/dmservice.nim` `libp2p/multistream.nim` `libp2p/transports/nim-msquic/api/api_impl.nim` | 继续把当前 cold start direct 成功链从稳定 `3.3s` 压向理论上界；现在 sender 写入、protocol coalesced、reader attach、owner 调度都已收正，剩余大头仍是 `dm_transport_connect_begin -> dm_transport_connect_done` 与 handshake/read 首帧到达 | 下一轮 fresh `19125 + production UI 1:1` 里，要么把总时延压进 `3s` 内，要么明确坐实已经触到当前协议与网络条件下的理论上界 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `libp2p/transports/tsnet/runtime.nim` `tests/testtsnetquicrelay.nim` | relay route task 退出只能清自己那一代的活状态；stale route 的 `incoming/ready/serving` 必须在 task 退出时同步摘掉，而 `published route` 热读不能再碰 `relayListenerStates`，只能从 owner set 投影 `dropped/bridge_attached/ready/incoming/awaiting/pending/serving/known` 物化 | 定向回归 `stale listener routeId cannot overwrite replacement task stage` 与 `runtime listenerNeedsRepair repairs stale incoming udp route after route task exit` 均为 `2 OK`；新主包 `lastUpdateTime=2026-04-06 14:15:22`，fresh seed `/ip4/100.64.148.187/udp/64211/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_141728` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status/local_info` 继续秒回，本地 `19125` 节点未再炸回 `relayListenerStates/getOrDefault` | yes |
| `libp2p/transports/tsnet/runtime.nim` `tests/testtsnetquicrelay.nim` | runtime listener 注册真相不能再用 `seq[route target object]`；published/repair 只认稳定的 `tcp/udpListenerRoutes + route->raw` 映射，彻底去掉 `listenerRouteTargetSnapshot` 热读 | 定向回归 `runtime local published udp stage counts as ready`、`runtime listenerNeedsRepair repairs stale incoming udp route after route task exit`、`runtime listenerNeedsRepair requests repair for known udp route without explicit flag`、`runtime reconcile repairs only explicitly requested route and keeps active listener` 为 `4 OK`；新主包 `lastUpdateTime=2026-04-06 16:00:10`，fresh seed `/ip4/100.64.148.187/udp/58458/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 production UI token `ui_dm_20260406_160055` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、双栈 `relayListeners[*].stage=ready`、`local_info.listenAddrSource=relay_listeners`，本地 `19125` 未再复现 `publishedListenerRoutes -> runtimeListenerRoutes -> listenerRouteTargetSnapshot -> SIGSEGV` | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `examples/mobile_ffi/tsnet_product_node.nim` `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnettransport.nim` `tests/testtsnetquicrelay.nim` | bridge-extra 的直连真相只认“本机全局 IPv6 + IPv6 directCandidates”；删掉 `bridgePublicIpv4` 与自动 IPv4 candidate 合成，产品节点启动时从非 VPN 接口探测本机全局 IPv6 并写进 hostNetwork/bridge-extra | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `17 OK`；定向回归 `tests/testtsnetquicrelay 'relay candidates include public IPv6 and explicit hints from bridgeExtraJson'` 为 `1 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；自动 `adb install -r` 后设备 `lastUpdateTime=2026-04-06 17:19:42`；fresh `19125` 的 `tailnet_status` 已不再出现 `bridgePublicIpv4`，并导出 `bridgePublicIpv6=240e:33d:680e:1e00:808:1e06:a8b5:76ca` 与两条 `/ip6/.../quic-v1/p2p/...` `bridgeDirectCandidates`，`bridgeHostNetwork.localIpv6/preferredIpv6` 同步为这条 `en0` 全局 IPv6 | yes |
| `examples/mobile_ffi/libnimlibp2p.nim` `examples/mobile_ffi/tsnet_product_node.nim` `examples/mobile_ffi/tsnet_product_rpc.nim` `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnetprovider*.nim` `libp2p/transports/tsnettransport.nim` `libp2p/transports/nim-msquic/api/api_impl.nim` | 先用 `tsnet ping --ping-target relay` 测真实中继 RTT，再把 relay DM 收成“listener fully ready 后预热 relay session，发送只复用已准备好的 relay sessionConnKey”；benchmark 热路不能读 `statusPayload()`、不能复用 direct desired 表，也不能在 ACK 热路复制 `seq[StreamState]` 快照 | fresh `19125` 上 `tailnet_ping --ping-target relay --sample-count 5` 已稳定测得 `min/avg/max = 1321/1368/1420 ms`，所以当前网络下 tsnet relay DM 的理论下界就是约 `1.3s` 单 RTT 加极小 framing 开销；已新增 `relay_session_prepare/relay_session_status` 与 `send_dm_kickoff --session-conn-key` 基准接口，并把 relay desired 收成独立表。当前 self relay benchmark 还没打到成功样本，但已把真实 blocker 前推到 `tsnet-quicrelay` 自测中的 `udp dial state lookup miss -> listener repair` 与 `msquic applyAckFrame -> flushAllConnectionStreams` 热路，最新修正已去掉 ACK 热路的 `seq[ref]` 快照复制，待下一次 fresh 复测确认 | in_progress |
| `libp2p/transports/tsnet/runtime.nim` `libp2p/transports/tsnetprovider.nim` `libp2p/transports/tsnettransport.nim` `libp2p/transports/msquictransport.nim` `examples/mobile_ffi/libnimlibp2p.nim` | relay lower-bound 自测剩余实现继续只收 owner 热读链：1) `publishLocalPeerInfo`/relay status/export 一律手写固定字段 JSON，不再 `%*`/`$JsonNode`；2) provider repair/status 读侧只允许锁内最小标量快照，不再整包 `providerReadSnapshot()`；3) `awaitMsQuicDial()` 禁止成功分支取消 timeout future；4) `waitForProviderUdpDialReady -> udpDialState` 不能再把 `MultiAddress` 现转字符串，必须改成 owner 侧稳定文本/标量状态 | 当前 fresh 自测已经把旧崩链全部前推掉：`bridge-extra merge`、`relay session status scan`、`peerRouteSessionSnapshotJson`、`providerProxyRouteCountSafe/providerReadSnapshot`、`awaitMsQuicDial cancelSoon` 都已收掉；fresh relay RTT 继续稳定在 `~1.3s`。新的唯一 top crash 是 `waitForProviderUdpDialReady -> providerUdpDialStateSafe -> tsruntime.udpDialState -> MultiAddress.$ -> SIGSEGV`，同时 wrong-route 实测已明确出现 `route_not_registered -> listener_busy -> direct message fresh dial connect timeout`，下一轮只需把 `udpDialState` 收成稳定文本状态并用正确 fresh route 复测 | in_progress |
| `libp2p/transports/tsnet/quicrelay.nim` `tests/testtsnetquicrelay.nim` `examples/mobile_ffi/libnimlibp2p.nim` | relay lower-bound fresh warm 继续只收单飞真相：listener state 只能有单层 `routeKey` 真相，peer route warm 期间同一路由的 UDP dial proxy 只能复用现有 `route -> rawKey`，绝不允许 cancel drain 期间再开第二个 raw proxy；在这条前提下继续逼近 `bridge attach` 真实失败 | 最新 fresh `relay_session_prepare` 已不再 `socket closed while reading`，节点未自杀；第二轮 warm 已不再出现第二个 `udp dial proxy scheduled`，而是命中 `udp dial proxy reuse active route state`，返回的真实失败也从 `dm_transport_connect_begin` 前推到 `dm_session_warm_failed/direct message fresh dial connect timeout`。当前唯一剩余 blocker 已收窄成 `udp listener bridge attach failed ... stream closed before a complete JSON frame was received` | in_progress |
| `libp2p/stream/connection.nim` `libp2p/protocols/connectivity/relay/relay.nim` `libp2p/protocols/secure/secure.nim` `libp2p/pnet.nim` `libp2p/transports/msquictransport.nim` `libp2p/muxers/mplex/mplex.nim` `libp2p/muxers/yamux/yamux.nim` `libp2p/transports/tsnettransport.nim` `examples/mobile_ffi/libnimlibp2p.nim` `tests/testtsnettransport.nim` | relay lower-bound 当前继续只收 path truth 和 prepared-session reuse：1) `RelayBridge` 必须把 `relayPath` 沿 transport/secure/muxer/channel 复制链完整保真，`relay_session_prepare` 只认真正的 relay session；2) tsnet QUIC 默认配置必须和“常驻 `sessionConnKey`”语义一致，不能继续 `idleTimeout=30s + keepAlive=0`；3) 发送时只复用 `sessionConnKey`，不再 fresh dial | fresh `19125` 新路由 `/ip4/100.64.148.187/udp/56068/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，`tailnet_ping --ping-target relay --sample-count 3 --timeoutMs 15000` 为 `1362/1613/1888 ms`；`relay_session_prepare` 返回 `status=ready sessionConnKey=4405177696 prepareLatencyMs=3869`。把同一条 session 空放 `75s` 后，`relay_session_status` 仍是 `ready/sessionConnKey=4405177696`，`send_dm` 成功返回 `stage=sent durationMs=1334`，日志已坐实 `path=relay_ready_session -> dm_session_reuse -> dm_write_done -> dm_ack_done`，且消息帧仍复用同一 `connKey=4405177696`；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过 | yes |
| `libp2p/services/wanbootstrapservice.nim` `/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt` | WAN bootstrap join 不能再被前一笔 join 的轮询等待和后置 `rendezvous/snapshot/kad` 长挂拖成整笔 20s 超时；service 必须复用单一 ongoing join future，并给 post-connect 阶段硬 deadline；Android `transport_addrs` 只认有真实网络信息的 host snapshot，不能再被空 placeholder 顶掉 | `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testwanbootstrap_authoritative_autojoin.nim` 为 `1 OK`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_wan_provider_seed_addrs.nim` 为 `2 OK`，`./gradlew :app:compileDebugKotlin -x lint -x test --no-daemon` 通过；双端 fresh 脚本 `/Users/lbcheng/UniMaker/scripts/run_android_harmony_bootstrap_relay_dm.sh --skip-build --skip-install` 已跨过 `bootstrap_join` 跑到后续 `bootstrap_tick/DM`，不再停在旧的 `bootstrap_join_random_timeout` / `joinViaRandomBootstrapAsync_timeout`；Android `transport_addrs` 现已拿到真实 `hostNetworkStatus.networkType=cellular preferredIpv6=240e:43d:682c:222:a0b5:23ff:fe8e:a9d3`，Harmony `transport_addrs` 也为 `ok=true`。当前唯一真 blocker 已前推成 `providerReason=tsnet_publication_not_ready`，两端 `candidateCount` 仍然只有 `1`，节点页还没发现彼此 | in_progress |
| `Android 安装闭环` `/Users/lbcheng/UniMaker/scripts/run_android_harmony_bootstrap_relay_dm.sh` `Harmony device runtime` | 先把 fresh 双端 WAN 发现闭环恢复到“脚本能同时拿到 Android/Harmony peerId 并真正进入 bootstrap/discovery”；当前 shared native 的 direct seed/published peer info 修正已经上车，但 Android 新包安装被华为系统双层确认卡住、Harmony 设备又因锁屏导致 `aa start` 失败，脚本实际停在 `harmonyPeer unresolved`，还没资格验证双端发现 | Android 新包已通过手动点掉华为安装器两层 `继续安装` 真正装上，`lastUpdateTime` 前推到 `2026-04-08 07:01:55`；fresh 脚本 `/Users/lbcheng/UniMaker/scripts/run_android_harmony_bootstrap_relay_dm.sh --android-serial GBJ0222B24021692 --harmony-target 3KN0224C18003262 --skip-build --skip-install` 至少要同时拿到 `androidPeer` 与 `harmonyPeer`，不再停在 `peerId unresolved after bootstrap/runtime snapshot`。如果 Harmony 仍锁屏，就先解决设备锁屏，再继续验证 `candidateCount`/双端互见 | in_progress |
