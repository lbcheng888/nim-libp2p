# progress

| 时间 | 进展 |
| --- | --- |
| 2026-04-08 | 已把移动端 LSMR 自举的 handoff 边界继续收正到 `Identified`，并把“publish 到 anchor 失败”的误判排掉。当前 repo 代码里 `[lsmrservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim)` 已明确：`Connected` 不再推进 refresh/control，只有 `handleIdentifiedSyncPeer()` 和 `bootstrapConnectedHandoff()` 会推进；其中 bootstrap handoff 先在现有 muxer 上 `ensurePeerIdentifySnapshot(peerId)`，拿不到 identify 就稳定落 `bootstrap_peer_identify_not_ready`，拿到后才 `refreshCoordinateRecord()`，并在本地 cert ready 后统一 `queuePeerControlHandoff(peerId)`。这轮最小复现也把真相坐实了：原来的 `/tmp/repro_lsmr_bootstrap_handoff.nim` 在 handoff 一返回就立刻读 `anchorHasClient`，会得到假阴性；但同一份复现只要在末尾多等 `2s`，`/tmp/repro_lsmr_bootstrap_handoff_wait.nim` 就会稳定跑出 `joinOk=true handoffCompleted=true clientLocalSome=true anchorHasClient=true`，说明 `control publish/sync -> anchor promote` 这条链本身已经通了，之前只是读得过早。正式回归方面，`nim c -r -d:ssl --verbosity:0 tests/testlsmrservice_network_slow_witness_refresh.nim` 仍为 `1 OK`，`nim c -r -d:ssl --verbosity:0 tests/testlsmrservice_network_slow_sync_imports.nim` 仍为 `1 OK`；`nim c -r -d:ssl --verbosity:0 tests/testmobile_ffi_routing_status.nim` 当前仍被工作区现存的 `finalizeReceivedDirectMessageAsync is not GC-safe as it calls appendConversationMessageNoLock` 旧编译问题挡住，不是这轮 LSMR handoff 改动引入。现在只剩拿到 `tests/testwanbootstrap_service.nim` 的最终 summary，再 fresh 部署 root anchor 做 Android/鸿蒙 LSMR 互见。 |
| 2026-04-08 | 已把移动端 LSMR 自举的根锚点前提收正。fresh Android 单边状态已坐实：`lsmrMinWitnessQuorum=1` 生效，但 `lsmrLocalCertReady=false / lsmrActiveCertificates=0 / lsmrWitnessRequests=1 / lsmrWitnessSuccess=0 / lsmrLastRefreshReason=""`，说明问题不是 asset/loader，而是现网根锚点没在提供 LSMR witness/control。本轮已在 `[dual_stack_bootstrap_node.nim](/Users/lbcheng/nim-libp2p/examples/dual_stack_bootstrap_node.nim)` 把公网 bootstrap host 升成真正的 `dualStack + primary=lsmr` root anchor：builder 现在显式 `withLsmr(lsmrConfig)`，并用 `bootstrapDualStackRoleConfig(role=anchor, primaryPlane=lsmr)` 挂同一套 LSMR 配置；同时新增 `BOOTSTRAP_LSMR_OPERATOR_ID/REGION_DIGIT/SERVE_DEPTH/MIN_WITNESS_QUORUM` 环境入口，默认就是移动端当前需要的 `unimaker-root / 5 / 2 / 1`。在 `[lsmrservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim)` 里，`refreshCoordinateRecord()` 现在一开始就把 `lastRefreshReason` 置成 `refresh_in_progress`，`requestWitnessFromPeer()` 的异常路径也会稳定落成 `witness_request_failed`，后续不再允许 fresh 诊断继续空白。并且在 `[testwanbootstrap_service.nim](/Users/lbcheng/nim-libp2p/tests/testwanbootstrap_service.nim)` 新增了 `lsmr bootstrap handoff installs local cert and publishes it to anchor`：它直接钉住“单 root anchor + quorum=1 + bootstrapConnectedHandoff”这条网络闭环，要求 client 连上 authoritative bootstrap 后本地 cert 真安装成功、`lsmrWitnessSuccess>=1`、`routingStatus.lsmrLastRefreshReason=="ok"`，并且 anchor 最终能收到 client 的 active cert。当前主因已经从‘移动端自己没把 LSMR 跑起来’前推到‘现网 bootstrap root host 还没部署这版’。 |
| 2026-04-08 | 已把 LSMR control protocol 的 session 语义收正。当前根因已经坐实：client 侧 `LsmrControlSession` 会在同一条 control stream 上顺手 drain `sync -> publish`，但 server 侧 `[lsmrservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim)` 的 `handleControl` 还是一请求一流、回完就 `close()`；于是 `syncCoordinatesFromPeer()` 成功后，同流上的 `coordinate_publish` 必然 read timeout，再补起第二个 control dial，最终把 `[testlsmrservice_network_slow_sync_imports.nim](/Users/lbcheng/nim-libp2p/tests/testlsmrservice_network_slow_sync_imports.nim)` 打成 `Opened SecureConn: 4 / Closed SecureConn: 3`。这轮已把 control handler 改成在同一条 stream 上循环处理多次 `request/response`，直到 EOF/close 才退出。验证结果已恢复：`nim c -r -d:ssl --verbosity:0 tests/testlsmrservice_network_slow_sync_imports.nim` 为 `1 OK`，`nim c -r -d:ssl --verbosity:0 tests/testlsmrservice_network_slow_witness_refresh.nim` 仍为 `1 OK`，`nim c -d:ssl --verbosity:0 tests/fabric_network_lsmr.nim` 通过。当前 native LSMR 自举主因已经从“control session 语义错位”前推干净，下一步可以直接回到 Android/鸿蒙 fresh 验证 `lsmrLocalCertReady / lsmrActiveCertificates / snapshot 互见`。 |
| 2026-04-08 | 已把 LSMR fresh control-plane 里两层错耦合拆开，但闭环还没跑通。`[dial.nim](/Users/lbcheng/nim-libp2p/libp2p/dial.nim)` 现在新增 `DialOptions.skipAutoIdentify`，`[dialer.nim](/Users/lbcheng/nim-libp2p/libp2p/dialer.nim)` 会在这类连接上跳过 outgoing auto identify；`[lsmrservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/lsmrservice.nim)` 里的 witness/control/overlay connect 已显式走这条 control-plane，同时把 `connected` 热路的“refresh hint”和“refresh 完自动 publish 回 peer”拆开，本地还没 cert 时只触发 refresh，不再让 root anchor 因为被连上一次就先反向开 control stream 抢业务首流。编译验证已过：`testlsmrservice_network_slow_witness_refresh.nim`、`testwanbootstrap_service.nim` 都能 `nim c -d:ssl` 通过；`testmobile_ffi_routing_status.nim` 仍被工作区现存的 `libnimlibp2p finalizeReceivedDirectMessageAsync GC-safe` 旧问题卡住，不是这轮新引入。定向 `lsmr_diag` 实测也已经把主因前推清楚：旧的 anchor 侧反向 `control-queue/publish storm` 已经消失，单锚 `quorum=1` 下本地 cert 仍能装上，但 fresh witness 首流仍会和 incoming `/ipfs/id/1.0.0` identify 竞争，现场停在 `peerStore.identify newStream begin/done -> multistream /ipfs/id/1.0.0` 与 `lsmr witness dial-begin -> proto-dial-stream-begin` 同窗，最后不再是 publish storm，而是更底层的 incoming identify 首流竞争。下一步只该继续收 incoming auto identify 的连接平面，不再回头改 LSMR 状态机或 quorum/asset。 |
| 2026-04-08 | 已把移动端 WAN advertise/export 的 `SignedPeerRecord` 导出热路收正，当前不再直接拿热路缓存的 `peerInfo.signedPeerRecord.envelope` 去做 identify/rendezvous 外发编码。根因已经坐实：Android `lastRendezvousAdvertiseError=Signed Peer Record encode failed: KeyError` 不是 mDNS、不是平台单边问题，而是共享 native 在导出 SPR 时直接编码了失真的缓存对象，具体炸在 `Envelope.encode -> env.publicKey.getBytes() -> CryptoError.KeyError`。这轮最终修法落在 `[peerinfo.nim](/Users/lbcheng/nim-libp2p/libp2p/peerinfo.nim)`：新增 `currentSignedPeerRecord/currentSignedPeerRecordBytes`，导出时优先复用稳定缓存里的 `payload/signature/seqNo`，只用 live `privateKey/publicKey` 修正 envelope 的 `publicKey`，只有缓存空或坏时才 fallback 重建完整 `SignedPeerRecord`；`[identify.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/identify.nim)`、`[rendezvous.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/rendezvous/rendezvous.nim)`、`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 已全部切到这条导出路径。中途试过“每次现算整份 SPR 再编码”，已被 `testidentify/testrendezvous` 坐实会打坏稳定 `seqNo` 语义，现已完全撤回。新增回归 `[testpeerinfo.nim](/Users/lbcheng/nim-libp2p/tests/testpeerinfo.nim)` 已钉住 stale cache 场景，当前验证结果为：`testpeerinfo` `4 OK`、`testidentify` `7 OK`、`testrendezvous` `21 OK`、`build/tsnet_product_node` 通过。当前唯一 live blocker 已前推成“把这版重新打进 Android 和鸿蒙后 fresh 复跑双端 WAN bootstrap/discovery”，下一步只该验证 `lastRendezvousAdvertiseError` 是否消失、`rendezvousAdvertised` 是否起量、双端节点页能否开始互相发现。 |
| 2026-04-07 | 已把鸿蒙 `tailnet_status` 从“查询链自己超时”前推到真实 `tailnet_not_ready`。这轮先在 `[libp2p_harmony_napi.cpp](/Users/lbcheng/UniMaker/hongmeng/entry/src/main/cpp/libp2p_harmony_napi.cpp)`、`[Libp2pRuntimeService.ets](/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/services/Libp2pRuntimeService.ets)`、`[EntryAbility.ets](/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/entryability/EntryAbility.ets)` 补了 `tailnetStartKickoff/getTailnetStartStatus*`，让 automation 先认 `tailnet start` 真状态，不再把 queued/starting/started/inFlight 误判成失败；同时把 `ensureStarted()` 和 bootstrap poll 里的 host network refresh 抢占拿掉，避免 `tailnet_status` 查询线程和 host network refresh 互相卡死。随后在 `[tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim)` 和 `[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 加了 native 非 ready 快照路径：provider 或 proxy listener 还没 ready 时，`tailnet_status` 直接返回轻量值快照，不再走 `submitCommand(cmdTailnetStatus)` 的重路径。中途试过把 ETS async 状态查询硬改同步，已被系统日志坐实会把 `com.example.unimaker` 打成 `signal:11`，现已全部回退。fresh 鸿蒙 automation token `harmony-tailnet-check-210533` 现已在 `21:05:38 -> 21:05:58` 直接返回 `cmd_result {"ok":false,"error":"tailnet_not_ready",...,"snapshotPayload":"{\"ok\":false}"}`；最新 hilog 里不再出现旧的 `invokeAsync:tailnetStatusJsonAsync:timeout=2500`、`invokeAsync:getTailnetStartStatusAsync:timeout=1500` 和 `refreshHostNetworkStatus:bootstrap_status:*` 连环超时，当前 live blocker 已正式前推成“tailnet 还没 ready”，可以直接继续收 WAN bootstrap/discovery。 |
| 2026-04-07 | 已把移动端 WAN bootstrap 的字段边界收正，不再让 `relayMultiaddrs` 同时扮演 relay reservation target 和 relay-backed bootstrap seed。`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 现在新增 `relayBootstrapMultiaddrs/relay_bootstrap_multiaddrs` ingestion：authoritative bootstrap、`configuredWanBootstrapAddrs()`、`configuredAnchorSeedAddrs()`、`configuredRelayBootstrapGate()` 都改成认 bootstrap seed 字段；旧的 `relayMultiaddrs` 继续只给 `configuredRelayReservationAddrs()`。同时 Android `[BridgeFacades.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt)` 和鸿蒙 `[Libp2pRuntimeService.ets](/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/services/Libp2pRuntimeService.ets)` 已同步支持新字段，Android 的 bootstrap override 也改成喂 `static_bootstrap + relayBootstrapMultiaddrs`，不再误伤 reservation 配置。回归 `[testmobile_ffi_wan_provider_seed_addrs.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_wan_provider_seed_addrs.nim)` 新增 `relay bootstrap multiaddrs enter authoritative bootstrap without reusing relay reservation field`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_wan_provider_seed_addrs.nim` 已为 `2 OK`，新用例已坐实：只给 `relayBootstrapMultiaddrs` 也会出现在 `bootstrap status.selectedCandidates[*].addrs`、`provider.anchorSeedAddrs` 和 `provider.authoritativeBootstrapPeerIds` 里，并且 auto authoritative join 会实际拨到 `/p2p-circuit/p2p/<bootstrapPeer>`；`testwanbootstrap_authoritative_autojoin.nim` 仍是 `1 OK`，`build/tsnet_product_node` 也已重编通过。双端 loader 改动也已经重编重装：Android `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过并已 `adb install -r -g`，设备 `lastUpdateTime=2026-04-07 19:17:57`；鸿蒙 `release_hongmeng.sh --target 3KN0224C18003262 --skip-nfc-smoke` 成功重编、打包、安装、拉起，设备 `updateTime=1775560657635`。Android 与鸿蒙当前 bundled `mobile-bootstrap.json` 都已经带上真实 `relayBootstrapMultiaddrs`，代码边界和资产边界都已收正；当前 live blocker 已经前推到 Harmony tailnet/bootstrap readiness，本身不再是 bootstrap seed 字段缺失。 |
| 2026-04-07 | 已把鸿蒙节点页按安卓 Nodes 页面收成同一套真相，代码都落在 `[NodePageNative.ets](/Users/lbcheng/UniMaker/hongmeng/entry/src/main/ets/pages/NodePageNative.ets)`。这轮不再让鸿蒙自己拼一套 `NodeEntry + source contains` 假状态，而是把节点列表改成 `connectedPeersInfo + connectedPeers + discoveredPeers + discoverySnapshot` 按 `peerId` 合并，`hostPeers` 只留给资源概览；同时把 source 过滤收成安卓同款 tag 推断，列表行改成“标题 + 域名/PeerId + transport/discovery/wake/path chips”，空态文案也改成安卓同款 BLE/QUIC 移动发现链提示。鸿蒙 `release_hongmeng.sh --target 3KN0224C18003262 --skip-nfc-smoke` 已成功重编并通过 `hdc install` 重装，设备 `bm dump -n com.example.unimaker` 已更新到 `updateTime=1775546524822`。中途唯一真报错是 ArkTS 不允许 `Object.prototype.hasOwnProperty.call`，已改成直接字段判空后再次打包通过；最终 ability 因设备锁屏没自动拉起，但包已是新版本。 |
| 2026-04-07 | 已把 Android same-process retained heat 的验证从“马上二连发”扩成“带显式空窗”。`[ConversationRouteSendTest.kt](/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt)` 现在支持 `repeatDelayMs`，二发前的 home 空窗不再写死 `500ms`。我先在 fresh 本地 route `/ip4/100.64.148.187/udp/56556/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上复跑一次默认短空窗 same-process 双发：token `ui_dm_20260407_1421_retain2_a/ui_dm_20260407_1421_retain2_b` 为 `OK (1 test)`，本地 `dm_rows` 两条全实收，log `[adb_retained_heat2_extract.log](/Users/lbcheng/nim-libp2p/build/adb_retained_heat2_extract.log)` 已坐实同一 app PID `18293` 下第二发前 snapshot 已是 `status=ready selectedPath=relay selectedConnKey=533165791696`，并命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，第二发 `sendThreadMessage success durationMs=715`。随后重编并重装 androidTest 到 `lastUpdateTime=2026-04-07 14:25:36`，再跑 `repeatDelayMs=10000` 的 same-process 双发：token `ui_dm_20260407_1426_retain10s_a/ui_dm_20260407_1426_retain10s_b` 也为 `OK (1 test)`，本地两条全实收；log `[adb_retained_heat_10s_extract.log](/Users/lbcheng/nim-libp2p/build/adb_retained_heat_10s_extract.log)` 已坐实同一 app PID `20302` 下隔 `10s` 仍然是 `status=ready selectedPath=relay selectedConnKey=533164702160`，第二发继续命中 `relay_ready_session`，最终 `sendThreadMessage success durationMs=818`。这说明 retained relay heat 现在不是只撑马上二连发，而是已经能覆盖真实的同进程短空窗。 |
| 2026-04-07 | 已把 peer relay session 从 route 生命周期里再解耦一层。`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 里的 `RelaySessionTargetSpec` 现在带 retained lease；`clearRouteTransportIntent()` 不再因为 route 清掉就立刻删掉非 sticky relay target，而是保留 peer 级 retained heat；`reconcilePeerRouteSession()` 也只有在 lease 真过期时才 `release_retained`，并且 live relay session 存活时会持续刷新 lease。这样同进程里的短空窗或 route 清理，不会再把 peer relay session 直接打回 fresh warm。回归 `[testmobile_ffi_conversation_transport_snapshot.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim)` 已新增 `clearing a failed relay route still retains peer relay heat`，专门用“已失败但已脱离 running 的 relay task”钉住 clear 之后仍不能被 owner 立刻删空；`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 为 `7 tests run (8.51s): 7 OK, 0 FAILED, 0 SKIPPED`，`tests/testtsnettransport.nim` 仍为 `18 OK`，`build/tsnet_product_node` 也已重编通过。 |
| 2026-04-07 | 已给“删掉固定 `3s` ACK 预留”补上一条稳定 native 回归，但不再依赖 live direct/relay 热身。`[testmobile_ffi_conversation_transport_snapshot.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim)` 新增 `seeded kickoff never reintroduces fake ack reserve`：直接走 `libp2p_send_with_ack_seeded_kickoff()` + 假 `/tsnet` seed，等待 kickoff terminal payload 后断言 `reservedAckBudgetMs=0`，从状态机边界直接钉住“低预算失败也不能再偷偷预留 ACK 预算”。`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin /Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim > /tmp/testmobile_ffi_conversation_transport_snapshot.log 2>&1` 已为 `6 tests run (7.36s): 6 OK, 0 FAILED, 0 SKIPPED`；同时 `tests/testtsnettransport.nim` 仍是 `18 OK`。 |
| 2026-04-07 | 已把 seeded `/tsnet` DM send 主链里那段错误的固定 `3s` ACK 预留删掉。`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 现在不再把 `waitForRouteTransportReady()`、`ensurePeerReadyViaExplicitAddresses()` 和后续 send/ack 人工切成两段预算，而是让 route wait、session ready、dm write、ack 共用同一个总 deadline；这和前一轮已经落下去的“长活 relay warm worker + 内部重试”配起来后，send 不会再比 relay worker 更早因为 `route transport not ready` 提前失败。native `tests/testtsnettransport.nim` 仍是 `18 OK`，`tests/testmobile_ffi_conversation_transport_snapshot.nim` 编译通过，`build/tsnet_product_node` 通过；Android `build_nim_libp2p_android.sh debug`、`assembleDebug`、`adb install -r -g` 全通过，主包时间是 `2026-04-07 13:30:18`。fresh 本地 route 已更新成 `/ip4/100.64.148.187/udp/60897/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，赛前赛后 `tailnet_status.proxyListenersReady=true`。same-process 双发 token `ui_dm_20260407_1332_sameproc_fix_a/ui_dm_20260407_1332_sameproc_fix_b` 为 `OK (1 test)`，本地两次 `wait_dm` 全实收；Android logcat `[adb_sameproc_fix_extract.log](/Users/lbcheng/nim-libp2p/build/adb_sameproc_fix_extract.log)` 已坐实 repeat 发前 route snapshot 已是 `status=ready selectedPath=relay selectedConnKey=532830587344`，发送链命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，第二发 `sendThreadMessage success durationMs=818`。这说明上一轮 same-process 首发里的 `route transport not ready · connect_exact_failed · 6792ms` 已经被这刀真正收掉。 |
| 2026-04-07 | 已把 single-heart route wait 漏掉的 `provider_wait -> ensureLocalTsnetReady()` 补回 `[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 主链。`waitForRouteTransportReady()` 现在在 route snapshot 还停在 `provider_wait` 时，不再只是傻等 wakeup，而是会直接拉起本地 tailnet provider，provider ready 后立刻 `reconcileDesiredRouteSessionsNow()` 并重发 route wake。native `tests/testtsnettransport.nim` 仍是 `18 OK`，`build/tsnet_product_node` 通过；Android `build_nim_libp2p_android.sh debug`、`assembleDebug`、`assembleDebugAndroidTest`、`adb install -r -g` 全通过，主包时间是 `2026-04-07 12:55:56`。fresh 本地 route 现在是 `/ip4/100.64.148.187/udp/49611/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，赛时 `tailnet_status.proxyListenersReady=true`。cold app token `ui_dm_20260407_1256_providerfix_s1` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat `[adb_providerfix_s1.log](/Users/lbcheng/nim-libp2p/build/adb_providerfix_s1.log)` 已坐实 `waitForRouteTransportReady ... relayStage=running -> ready`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，最终 `sendThreadMessage success durationMs=1629`，旧的 `local tailnet provider pending` 已消失。紧接着第二条独立 instrumentation token `ui_dm_20260407_1257_providerfix_s2` 也是 `OK (1 test)`、本地 `wait_dm` 实收，但 app PID 已从 `25754` 变成 `26274`，这不是同进程 retained-heat 样本；它仍重新跑了 `dm_protocol_select_begin -> warm_done`，最终 `durationMs=2567`。为了继续验 peer 级 retained relay heat，我给 `[ConversationRouteSendTest.kt](/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt)` 加了可选 `repeatToken/repeatRoute` 同进程双发入口；但第一轮 same-process token `ui_dm_20260407_1301_sameproc_a/ui_dm_20260407_1301_sameproc_b` 的首发就前推出新的 `dm_transport_connect_begin` timeout，log 在 `[adb_sameproc_fail.log](/Users/lbcheng/nim-libp2p/build/adb_sameproc_fail.log)`，说明 retained peer relay heat 这条还没被新 harness 验真，当前真 blocker 已经变成同进程首发里偶发 `dm_transport_connect_begin` 抖动。 |
| 2026-04-07 | 已把 budget 化 multistream 协商按 fresh Android production UI 做成完整 5 轮 cold `no-wait` 样本，不再只看单把成功。当前 fresh route 还是 `/ip4/100.64.148.187/udp/59367/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，5 轮 token `ui_dm_20260407_1113_budgetfresh`、`ui_dm_20260407_1114_budgetfresh_repeat`、`ui_dm_20260407_1122_budgetfresh_s3`、`ui_dm_20260407_1123_budgetfresh_s4`、`ui_dm_20260407_1124_budgetfresh_s5` 全部为 `OK (1 test)`，本地 `wait_dm` 五轮全实收；Android logcat 五轮都坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，`sendThreadMessage success durationMs` 依次为 `1427/2442/3271/1732/1832`，分布 `min/median/avg/max = 1427/1832/2141/3271 ms`。这说明当前主链已经稳定压到 relay RTT 量级，发送本体不再是 blocker；唯一残留只是偶发点击时 relay warm 还停在 `dm_protocol_select_begin`，会把单轮抬成约 `3.2s`。赛后本地 `19125` 仍是 `proxyListenersReady=true`、节点持续存活，`adb logcat -b crash` 里也没有新的 `com.unimaker.native.debug` 崩溃，只有华为输入法 `com.huawei.ohos.inputmethod` 的 Room migration 噪音。 |
| 2026-04-07 | `dm_protocol_select_begin` 这条剩余热路已经按理论正确方式收成 budget 化 multistream 协商。`[multistream.nim](/Users/lbcheng/nim-libp2p/libp2p/multistream.nim)` 现在会把 handshake write/read、`select_first_proto`、`select_proto` 都按调用方剩余 budget 计算 timeout；`[dialer.nim](/Users/lbcheng/nim-libp2p/libp2p/dialer.nim)` 的 `negotiateStream()` 也显式接 `totalTimeout`，连 `beginProtocolNegotiation` 都不再脱离 deadline 裸跑；`[dmservice.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim)` 的 fresh/live 协商都已经切到这条新链，不再靠“outer timeout 包内部固定 15 秒 multistream 黑箱”。 |
| 2026-04-07 | 这版已经按 fresh Android production UI 闭环坐实，而且结果继续前推。`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_conversation_transport_snapshot.nim` 通过，`nim c -r ... tests/testtsnettransport.nim` 为 `18 OK`，`build/tsnet_product_node`、`build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon`、`adb install -r -g` 全通过；设备主包已更新到 `lastUpdateTime=2026-04-07 11:11:38`。fresh 本地 `19125` route 是 `/ip4/100.64.148.187/udp/59367/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，relay RTT `min/avg/max = 1433/1805/2461 ms`。cold app token `ui_dm_20260407_1113_budgetfresh` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，并以 `sendThreadMessage success durationMs=1427` 收尾。紧接着同 route 的 cold repeat token `ui_dm_20260407_1114_budgetfresh_repeat` 也是 `OK (1 test)`，本地 `wait_dm` 实收，`sendThreadMessage success durationMs=2442`；两轮都没再出现旧的 `send_with_ack_timeout` 或新的 native crash。 |
| 2026-04-07 | `init-first` 这条路已经被收成正确模型，不再是旧的错版本。`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 现在会把 relay warm 在 `provider_wait` 期间停在 `prssPending`，只记录真实 `provider_wait` 状态，不再同步 `ensureLocalTsnetReady()` 把 `8s` warm budget 烧掉；`updateTailnetStartStatus()` 也已经直接接上 `firePeerRouteSessionReconcileWake()`，provider/tailnet 状态一变化就会立刻唤醒 relay reconcile。配合 `[TruthRouteTransportPreheater.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt)` 重新前移 `pathIntentUpsert`，Android 现在可以先声明 route intent，再等 runtime ready，而不会重演旧的 `dm_transport_connect_begin/ack timeout`。 |
| 2026-04-07 | 这版已经按 fresh Android production UI 闭环坐实。native 回归 `/tmp/testmobile_ffi_conversation_transport_snapshot.log` 为 `5 tests run (6.37s): 5 OK, 0 FAILED, 0 SKIPPED`；`build/tsnet_product_node`、`build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 全通过。设备主包已更新到 `lastUpdateTime=2026-04-07 10:55:34`；fresh 本地 `19125` route 是 `/ip4/100.64.148.187/udp/53158/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，`tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_105553_initfirst_nativegated` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，随后 `dm_write_done -> dm_ack_done`，最终 `sendThreadMessage success durationMs=1323`。同轮 fresh relay RTT 是 `min/avg/max = 1337/1660/2295 ms`，说明当前 Android UI cold `no-wait` 已经压到 relay RTT 下界量级。 |
| 2026-04-07 | 已把 `[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 里的 peer relay reconcile 从旧的 `750ms` 轮询改成 wake-driven：`path intent upsert/clear`、direct/relay snapshot 变化、relay warm 完成都会直接 `firePeerRouteSessionReconcileWake()`；同时补了 `[testmobile_ffi_conversation_transport_snapshot.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim)` 新回归，钉住“清掉最后一个 relay route intent 后，relay status 会立即回到 missing”，不再依赖旧轮询节拍。native 编译通过，定向回归日志 `/tmp/testmobile_ffi_conversation_transport_snapshot.log` 已收成 `5 tests run (6.08s): 5 OK, 0 FAILED, 0 SKIPPED`。 |
| 2026-04-07 | 用这版 wake-driven relay reconcile 复跑 fresh Android production UI 后，主因已经重新前推到 native send path。Android 新包时间是 `2026-04-07 10:38:59`；fresh 本地 `19125` route 是 `/ip4/100.64.148.187/udp/59141/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，赛前 `tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_1040_reconcilewake` 失败，本地 `wait_dm` 为 `wait_dm_timeout`；Android logcat 已坐实 `sendThreadMessage failed ... stage=connect_exact_failed durationMs=6322 error=dm_transport_connect_begin`，页面诊断也已经是 `中继会话 FAILED dm_transport_connect_begin`。同一份 log 里还能看到失败后 native 又起了一次 `runPeerRouteSessionWarmAsync ... source=android_send_with_ack`，说明 Android gate 已不是主因，send-time 仍可能落回 relay-only fresh dial；赛后本地 `19125` 继续健康，`adb logcat -b crash` 没有新的 `com.unimaker.native.debug` native crash。 |
| 2026-04-07 | 又试了两条继续前移 Android gate 的切口，结论都是否。第一条把 `[MainActivity.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt)` 的 `primeRoute()` 提前到 `super.onCreate()` 前，fresh `no-wait` token `ui_dm_20260407_101240_oncreatewarm` 虽然成功，但 Android logcat 只收到了 `sendThreadMessage success durationMs=2343`，没有优于稳定样本 `1630ms`。第二条把 `[P2PRuntimeSupervisor.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/P2PRuntimeSupervisor.kt)` / `[TruthRouteTransportPreheater.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt)` 改成 preheater 只等 runtime started、不等本机 `peerId`，fresh `no-wait` token `ui_dm_20260407_101730_startgate` 则被前推成 `中继会话 READY` 后 `send_failed 9816ms ack timeout`，本地 `wait_dm` 为 `wait_dm_timeout`。这说明 Android lifecycle / gate 这层已经收完，再往这层动只会引入新抖动或新回归。 |
| 2026-04-07 | 上面两条试探已经全部回滚，源码恢复到上一版稳定顺序，并重新 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` + `adb install -r`；设备主包时间现在是 `2026-04-07 10:20:53`。也就是说，当前稳定边界仍然是 app 级 `TruthRouteTransportPreheater + scheduleWarm -> ensureRuntimeReady -> pathIntentUpsert` 这版；下一步如果继续压 lower bound，只能下探 native relay session / ACK 本体，不该再回头折腾 Android 入口顺序。 |
| 2026-04-07 | `init-first` 试验已经被证伪并回退。`[TruthRouteTransportPreheater.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt)` 不能在 runtime/provider 未 ready 时先 `initialize + pathIntentUpsert`，`[libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 的 relay warm 也不能在 `provider_wait_pending` 时先把 task 掉成 `pending` 返回；这会把 warm 预算烧光，最后把 fresh 发送前推成 `dm_transport_connect_begin` 或 `route transport not ready`。当前源码已经回到稳定顺序：app 级 preheater 先 `scheduleWarm()`，worker 里 `ensureRuntimeReady()` 成功后才 `pathIntentUpsert(source=android_truth_route_launch)`，native relay warm 继续在同一 worker 内用剩余 budget `ensureLocalTsnetReady()`。 |
| 2026-04-07 | 回退后的 stable 路径已重新按 fresh Android production UI 闭环坐实。设备主包仍是 `lastUpdateTime=2026-04-07 09:58:40`；fresh 重起本地 `19125` 后，新 seed 是 `/ip4/100.64.148.187/udp/62863/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，`tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_100358_revertcheck` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已再次坐实 `runPeerRouteSessionWarmAsync ... source=android_truth_route_launch`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，并以 `sendThreadMessage success durationMs=1630` 收尾。赛后本地 `19125` 继续健康，`tailnet_status.proxyListenersReady=true`；`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash，仍然只有华为输入法噪音。 |
| 2026-04-07 | 已把 truth-route relay preheat 从会话页内 side effect 前移到 App 级 owner。新增 `[TruthRouteTransportPreheater.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/TruthRouteTransportPreheater.kt)`，由 `[UniMakerApplication.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/UniMakerApplication.kt)` 持有；`[MainActivity.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt)` 一解析出 deep link route 就直接 `primeRoute()`，先 `scheduleWarm()`，worker 里的 `ensureRuntimeReady()` 成功后再 `pathIntentUpsert(source=android_truth_route_launch)`。`[NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)` 这边已经删掉旧的 clear/declare/runtime-activation side effect，只剩统一 `pathSnapshot` 观察。 |
| 2026-04-07 | 这刀已经按 fresh Android production UI 闭环坐实。`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，`adb install -r /Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` 后主包时间是 `2026-04-07 09:48:47`。fresh 重起本地 `19125` 后，新 seed 是 `/ip4/100.64.148.187/udp/59157/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，本地 `tailnet_status.proxyListenersReady=true`。cold app `no-wait` token `ui_dm_20260407_0950_routepreheat` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `runPeerRouteSessionWarmAsync ... source=android_truth_route_launch`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，并以 `sendThreadMessage success durationMs=1830` 收尾。相对上一轮 `3379ms`，tap 后 warm 成本已经被压掉了大半。 |
| 2026-04-07 | 同一条 fresh route 上继续补跑 `waitRelayReady=true` 守门，token `ui_dm_20260407_0952_routepreheat_wait` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `relay ready before tap diagnostics=... 中继会话 READY`、`sendConversationText transport_ready ... selectedConnKey=532922185888`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，最终 `sendThreadMessage success durationMs=716`。赛后本地 `19125` 继续健康，`tailnet_status.proxyListenersReady=true`；`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash，仍然只有华为输入法噪音。 |
| 2026-04-07 | 已把 `relay_only` 的新真失败收掉。`[examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 现在会把 peer relay warm 的首选地址直接从 `RouteTransportIntent/RelaySessionTargetSpec.selectedAddrs` 投给 `dmService.warmSessionDetailed(preferredAddrs=...)`，不再在 relay-only 热路里偷用“已存地址”；对应新增回归 `[tests/testmobile_ffi_conversation_transport_snapshot.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim)` 已钉住 `relay_only` intent 不再报 `route_missing`。 |
| 2026-04-07 | 这刀已按 fresh Android production UI 复测验证。native 回归为 `4 OK`，`build/tsnet_product_node` 重编通过；Android 重新 `build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 并 `adb install -r` 后，主包时间是 `2026-04-07 09:19:37`。fresh 本地 `19125` seed 是 `/ip4/100.64.148.187/udp/61288/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，同一条 route 上，production UI token `ui_dm_20260407_0920_nowait_relayonly_seedfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息；Android logcat 已坐实 `sendConversationText transport_wait ... relayOnly=true policy=relay_only`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`，并以 `sendThreadMessage success durationMs=3379` 收尾。 |
| 2026-04-07 | 当前唯一剩下的真问题已经重新收窄成 lower-bound 本体：`route_missing` 没了，relay-only 也真走上了 `relay_ready_session`，但点击时 relay session 仍在 `running/provider_wait -> dm_protocol_select_begin`，所以 cold `no-wait` 仍要 `3379ms`。赛后本地 `19125` 继续存活，`tailnet_status.proxyListenersReady=true`；`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` crash，仍然只有华为输入法噪音。 |
| 2026-04-07 | 已收掉本地产品节点 fresh 新崩栈：`[/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim)` 的 `tailnet_status` RPC 不再走 `cstring -> JsonNode -> rpcResponse(JsonNode) -> $response` 这条 live JSON tree 热路，而是直接把 owner 产出的文本快照包成响应字符串发回。fresh 重编 `build/tsnet_product_node` 后，连续压 `120` 次 `tailnet_status`，本地 `19125` 节点持续存活，`local_info` 仍可读，`android_truthroute_node.log` 不再出现旧的 `encodeFrame -> alloc -> SIGSEGV`。 |
| 2026-04-07 | 在收掉这条本地 diagnostics 崩栈后，同一 fresh route `/ip4/100.64.148.187/udp/49572/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 上，本地 `19125` 已能稳定跑到 `tailnet_status.proxyListenersReady=true`、`relayListeners=2/2 incoming`。重新跑 production UI 后，`waitRelayReady=true` 的 `ui_dm_20260407_0859_waitready_listenerfix` 为 `OK (1 test)`，本地 `wait_dm` 实收；Android logcat 已坐实 `observeTruthRouteTransportSnapshot ready ... selectedPath=relay connKey=532651157056`、`relay ready before tap diagnostics=... 中继会话 READY`、`cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false`，并以 `sendThreadMessage success durationMs=714` 收尾。 |
| 2026-04-07 | 同一 fresh route 下继续复跑 cold app `no-wait`，`ui_dm_20260407_0901_nowait_listenerfix` 也为 `OK (1 test)`，本地 `wait_dm` 实收。Android logcat 已坐实点击时 diagnostics 先是 `中继会话 RUNNING dm_transport_connect_begin · 发送中 wait_send_ack`，随后进入 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false`，`observeTruthRouteTransportSnapshot ready ... connKey=532705743840`，最后 `sendThreadMessage success durationMs=3581`。赛后本地 `19125` 继续存活，`tailnet_status.proxyListenersReady=true`；`adb logcat -b crash` 里没有 `com.unimaker.native.debug` 的新 crash，仍然只有华为输入法噪音。 |
| 2026-04-07 | 已把 Android truth-route fresh 进入页的 stale diagnostics 真相收掉。`[/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)` 现在在 route revision 切换后会立刻清空同会话旧的 `conversationSendStatusState/conversationLatencyState`，并且 `observeTruthRouteTransportSnapshot()` 改成 native 查询返回后再二次校验 `truthRouteDirectWarmObserveRevision + active pathKey`，旧 route observer 不会再把过期 direct/relay 状态写回新页面。 |
| 2026-04-07 | 这刀已按 fresh 主包和 fresh 页面实测验证。`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，主包重装后 `lastUpdateTime=2026-04-07 08:35:59`。fresh 本地 `19125` 新 seed 是 `/ip4/100.64.148.187/udp/53225/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。同一 fresh route 上，`waitRelayReady=true` 的 `ui_dm_20260407_0837_diagreset_wait` 没过，但失败点已经明确前推成 `relay session not ready before tap`，对应赛时 `tailnet_status` 仍是 `proxyListenersReady=false`、`relayListeners=IPv4 incoming + IPv6 registering`，说明这轮不再是旧 route 假状态；`no-wait` 的 `ui_dm_20260407_0838_diagreset_nowait` 为 `OK (1 test)`，本地 `wait_dm` 实收，Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false -> sendThreadMessage success durationMs=3791`。随后用 deep link 最小隔离重新进入同 route，不发送只抓 UI hierarchy，`conversation_diagnostics` 已只剩 `libp2p RELAY · tailnet DIRECT · 打洞 0% 0/2 · 直连DM -- · 中继DM -- · 中继会话 RUNNING dm_protocol_select_begin`，不再夹带旧的 `发送失败/已送达/发送中` 文案。 |
| 2026-04-07 | 已把 Android 侧最后一条双编排尾巴切掉：`[/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)` 现在不再维护 `ambientDirectWarmTargetsDesiredState/appliedDirectSessionTargets/directSessionTargetSync`，旧的 `directSessionTarget` set/clear/status/cache/warm 观察死代码也一起删除。Android truth-route 只剩 `updateActiveTruthRouteDirectWarmTarget() -> pathIntentUpsert/pathSnapshot -> sendDirectText(sendWithAckKickoff)` 这一条主链，后台 `android_ambient_direct_target` 不会再抢 seeded `/tsnet` DM 的 relay warm。 |
| 2026-04-07 | 这刀已经按 fresh 生产闭环验证过。`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，主包重装后 `lastUpdateTime=2026-04-07 08:21:31`。按标准 fresh 重起本地 `19125` 后，新 seed 是 `/ip4/100.64.148.187/udp/58385/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，待 `tailnet_status.proxyListenersReady=true` 后，production UI token `ui_dm_20260407_0825_waitready_fresh` 与 cold app `ui_dm_20260407_0826_nowait_fresh` 都为 `OK (1 test)`，本地 `wait_dm` 两轮都实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`。Android logcat 已坐实 no-wait 发送是 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=false -> sendThreadMessage success durationMs=4014`；`adb logcat -b crash` 里没有新的 `com.unimaker.native.debug` native crash，仍然只有华为输入法噪音。 |
| 2026-04-07 | 已继续把 single-heart controller 的读侧收完整。`[examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 里的 `ConversationTransportSnapshot` 现在会一并导出 `directAttemptCount/directSuccessCount/directSuccessRatePct/directDmLatencyMs/relayDmLatencyMs`，不再逼 Android 为 direct 统计回头读旧 `direct_session_target_status`。`[/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)` 的 `refreshConversationLatency()` 也已改成只读 `queryNativeConversationTransportStatus()`，会话页 direct/relay 统计和 ready/path 真相现在都来自同一份 `pathSnapshot`。 |
| 2026-04-07 | 新增 [tests/testmobile_ffi_conversation_transport_snapshot.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_conversation_transport_snapshot.nim) 回归，钉住三件事：同一路由重复 upsert 时 policy 真更新、同 peer 两个 routeKey 清一个不误杀另一个、重复 `pathSnapshot` 稳定可读并带上新 metrics 字段。验证已通过：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_conversation_transport_snapshot.nim` 为 `3 OK`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；Android 侧 `bash /Users/lbcheng/UniMaker/android/scripts/build_nim_libp2p_android.sh debug`、`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，主包重装后 `lastUpdateTime=2026-04-07 07:58:32`。fresh 本地 `19125` 新 seed `/ip4/100.64.148.187/udp/62154/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0800_pathsnap` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`；Android logcat 已坐实 `cmdSendDirect connectivity ... path=relay_ready_session`，并以 `sendThreadMessage success durationMs=2978` 收尾。 |
| 2026-04-07 | 已把 seeded `/tsnet` DM 热路收成 owner 单心脏 controller：`[examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 新增 `RouteTransportIntent + ConversationTransportSnapshot + route AsyncEvent wakeup`，owner 侧统一维护 `routeTransportIntents/routeTransportSnapshots/peerTransportRoutes`，并新增 `cmdPathIntentUpsert/cmdPathIntentClear/cmdPathSnapshot`。旧的 `direct_session_target_set/clear` 现在只做薄包装转调，不再自己读散落的 direct/relay task 表。`publishDirectSessionTargetSnapshot/runPeerRouteSessionWarmAsync/reconcilePeerRouteSession` 每次状态变化都会立即重算并发布 unified snapshot，seeded `/tsnet` send 也已改成 `upsert intent -> waitForRouteTransportReady -> sendOn sessionConnKey`，不再走旧的 `waitForDirectOrRelaySessionReady` 轮询裁决。 |
| 2026-04-07 | Android truth-route 也已同步切到“只声明 intent、只读 unified snapshot”。`[/Users/lbcheng/UniMaker/android/core-bridge/src/main/cpp/libp2p_bridge.cpp](/Users/lbcheng/UniMaker/android/core-bridge/src/main/cpp/libp2p_bridge.cpp)`、`[/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt)` 已新增 `pathIntentUpsert/pathIntentClear/pathSnapshot` JNI/bridge；`[/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)` 的 truth-route 进入页只声明 transport intent，观察口改成统一 `ConversationTransportSnapshot`，发送时不再自己拼 `preferredSessionConnKey`。验证已通过：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过，`bash /Users/lbcheng/UniMaker/android/scripts/build_nim_libp2p_android.sh debug` 与 `cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，主包重装后 `lastUpdateTime=2026-04-07 06:25:01`。fresh 本地 `19125` seed `/ip4/100.64.148.187/udp/54029/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0627_singleheart` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`，`adb logcat -b crash` 没有新的 `com.unimaker.native.debug` native crash。 |
| 2026-04-07 | 已把 [libp2p/transports/msquicdriver_experimental.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/msquicdriver_experimental.nim) 里的 `MsQuicPendingSend` 收成 raw-root callback context。根因已经坐实不是 transport root 表残留，而是 `MsQuicPendingSend` 还在走 `stream.pendingSends: seq[ref] + clientContext=cast[pointer](pending)` 这条 live `ref` 图，Android fresh tombstone 顶栈稳定是 `UniMakerNativeN -> eqtrace(MsQuicPendingSend) -> collectCycles -> createStream -> quicrelay.udpListenerLoop -> SIGSEGV`。现在 stream owner 只保留 `pendingSendRoots/pendingSendOrder` 的 raw pointer 真相，`MsQuicPendingSend` 生命周期统一改成 `GC_ref/GC_unref`，`seSendComplete` 也改成按 raw context 精确取回 pending send，不再让 GC 扫这条链。 |
| 2026-04-07 | 这刀已经被 fresh Android production UI 1:1 闭环验证收掉：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过。华为安装链这次必须绕过 `InstallStaging` 风险确认，最终用 `pm install-create/install-write/install-commit` 才让主包真正更新到 `lastUpdateTime=2026-04-07 00:27:08`。随后 fresh 本地 `19125` 新 seed 是 `/ip4/100.64.148.187/udp/60958/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，production UI token `ui_dm_20260407_002915_pendroot` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `local_info/tailnet_status` 继续秒回且 `proxyListenersReady=true`；`adb logcat -b crash` 没有再出现 `com.unimaker.native.debug` 的 native `SIGSEGV`，只剩华为输入法噪音。 |
| 2026-04-06 | 已把 tsnet `bridge-extra` 收成公网直连候选的唯一导出值对象。`[libp2p/transports/tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim)` 的 `publishLocalPeerInfo()` 现在会合并 owner 侧动态 `bridgeMetadata`；`[examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)` 新增 `buildTsnetBridgeExtraPayload()`，把 `hostNetwork/publicIpv4/publicIpv6/directCandidates` 统一写入 bridge-extra，并在 `cmdUpdateHostNetworkStatus` 侧效应和 `publicIpProbe` 后沿同一条 `publishTsnetLocalPeerInfo()` 发布；`[examples/mobile_ffi/tsnet_product_node.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim)` 启动时会最小喂一次 `hostNetworkStatus(isConnected=true)`，让本地产品节点也进入同一套公网 IP probe / bridge-extra 导出链。 |
| 2026-04-06 | 最小编译与本地 fresh 节点验证已通过：`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 通过；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh `19125` 节点的 `tailnet_status` 已出现 `bridgePublicIpv4=38.213.31.206` 与 `bridgeDirectCandidates=[/ip4/38.213.31.206/udp/59507/quic-v1/p2p/12D3KooWLgM..., /ip4/38.213.31.206/udp/58939/quic-v1/p2p/12D3KooWLgM...]`，同时 `bridgeHostNetwork.publicIpv6=""`、`publicIpv6ProbeError=create_request_failed:Could not resolve address of remote server`，说明当前这台 Mac 这次跑出来的是公网 IPv4 候选已交换，公网 IPv6 候选当前仍为空。 |
| 2026-04-06 | Android 主包也已带上同版 native so：`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，`adb install -r` 成功，`lastUpdateTime=2026-04-06 16:58:21`。logcat 已坐实 Android 节点现在沿同一条 owner 发布链工作：`cmdUpdateHostNetworkStatus applied connected=true networkType=cellular transport=cellular -> publish_local_peer_info:start/done -> public_ip_probe:start/done -> sync_advertised:done`。也就是说“桥接导出链”已经接通；是否真拿到公网 IPv6 候选，下一轮只需要继续从 Android 侧 tailnet/diagnostics 真值观察，不再回头猜 `/tsnet` 发布。 |
| 2026-04-06 | 已把 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 direct/relay warm worker 收成“`provider_wait/route_wait` 在 worker 自己 deadline 内连续等待”。direct worker 现在会在同一轮 reconcile 里先等 `providerReady`、再等 exact direct plan ready；relay worker 也会在同一 task 内等 `providerReady`，不再立刻把这段空耗甩回 750ms reconcile loop。 |
| 2026-04-06 | 中途 fresh 复跑 token `ui_dm_20260406_133750` 时，新的回归也已经被前推出并修掉：因为 relay worker 还在 task 内等待 `providerReady` 时，错误地先把 `task.state` 切成了 `pending`，owner 误以为“没在跑”又重复 spawn 同 peer warm task，现场直接落成 `dm_dial_lock_wait` 与伪 `dm_transport_connect_begin` 拥塞。现在这条状态/future 错位已修回，relay worker 只在真正返回时才落 `pending`。 |
| 2026-04-06 | 新包 `lastUpdateTime=2026-04-06 13:41:31` 已生效，fresh 本地 seed 是 `/ip4/100.64.148.187/udp/58756/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。沿同一条 production UI token `ui_dm_20260406_134203` 复跑后，旧的 `dm_dial_lock_wait` 回归已不再出现，但当前新的唯一 blocker 也更明确了：`waitForDirectOrRelaySessionReady` 结束时 direct 始终是 `route_wait`，错误为 `no tsnet direct route is registered for /ip4/100.64.148.187/udp/58756/quic-v1/tsnet`；relay worker 则稳定停在 `dm_transport_connect_begin`，最终 sender 失败为 `connect_exact_failed 6826ms dm_transport_connect_begin`。本地 `wait_dm` 未收到消息，`adb logcat -b crash` 没有新的 app/node crash。 |
| 2026-04-06 | 已把 [libp2p/transports/nim-msquic/api/runtime_bridge.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/nim-msquic/api/runtime_bridge.nim) 的 callback context ownership 收成理论正确边界。根因已经前推到 `storeConnectionContext -> find(activeConnectionContexts, ctx)`，说明 `RuntimeBridge` 不能再把 live/retired context 放进任何 `ref` 容器里扫描或扩容；现在 handle 索引表只存 raw pointer，live/retired 生命周期改成 `GC_ref/GC_unref` 手动 root/unroot，`connection/stream/listener` 三条 callback 入口也会先临时 `GC_ref(ctx)`，直到回调返回才 `GC_unref(ctx)`。这样 `store/drop/callback` 都不再碰 `seq[ref].find` 或 `Table[handle, ref]` 的 RC 路径。 |
| 2026-04-06 | 最小编译与 fresh 节点验证已经通过：`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh 本地 `19125` 节点稳定拉起后，`tailnet_status` 和 `local_info` 都继续秒回，当前 seed 是 `/ip4/100.64.148.187/udp/59770/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，`proxyListenersReady=true`，`listenAddrSource=relay_listeners`，旧的 `drop/store/find` 三条 MsQuic context fresh 崩链都没有再出现。 |
| 2026-04-06 | 随后已按 production UI 1:1 闭环复跑。主包时间仍是 `2026-04-06 13:09:10`；production UI token `ui_dm_20260406_131932` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息。Android 日志已坐实这轮继续走直连：`runPeerRouteSessionWarmAsync warm_done ... pathKind=direct` -> `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false` -> `sendThreadMessage success durationMs=5550`。`adb logcat -b crash` 里没有 `com.unimaker.native.debug` 或本地 `19125` 的新 crash，仍然只有华为输入法噪音。 |
| 2026-04-06 | 已把 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `social_state` 持久化边界收成“live tree + dirty version + owner 单飞 flush command”。`persistSocialStateOnOwnerThread/persistSocialStateUnsafe` 不再在业务入口里整棵 `$JsonNode`，现在只更新 live state、标脏并排队 `cmdSocialStateFlush`；真正 stringify/落盘只在 owner 命令里做，`cmdStop` 也会在停机前强制 flush。与此同时，content/group/direct message 三条 owner 热路都接进同一把 `socialStateMemLock`，不再和跨线程 `loadSocialStateUnsafe/persistSocialStateUnsafe` 读写同一棵树时互相乱碰。 |
| 2026-04-06 | 新增 [tests/testmobile_ffi_social_state_cache.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_social_state_cache.nim) 的第二条回归，验证 `social_notifications_mark()` 返回后内存摘要立即变成已读，磁盘 `social_state.json` 会在 owner async flush 后跟上；当前 `nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 已是 `2 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 和 `cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 也都通过。 |
| 2026-04-06 | fresh 真机闭环已经把旧的 node 自杀链证伪掉了。新包 `lastUpdateTime=2026-04-06 11:27:00` 已生效，fresh seed 是 `/ip4/100.64.148.187/udp/60346/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`；production UI token `ui_dm_20260406_112745` 下，本地 `wait_dm` 已实收同名 DM，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`adb logcat -b crash` 里也没有 `com.unimaker.native.debug` 或本地 node 的新 crash。当前新的真实 blocker 已前推成 sender 预算问题：app 侧 `cmdSendDirect connectivity ... path=direct_ready_session` 之后只剩极短 `dm_ack_wait`，最终报 `ack timeout`；更关键的是，`reconcilePeerRouteSession direct_ready_skip source=android_truth_route_prepare connKey=...` 是在失败后才出现，说明这轮 direct ready 真相到得太晚，不再是 `social_state` 热崩。 |
| 2026-04-06 | 已把 direct 打洞统计的真相边界收正。根因不是“没直连”，而是 direct owner 在吸收现成 live `direct/punched_direct/local` 会话时，只把状态切成 `ready`，没把这次 ready 迁移记进 `attempt/success`，所以 UI 会稳定显示 `打洞 0% 0/0`。现已在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增统一 `markDirectPeerPathStateReady()`，让 direct warm 成功和 live direct 吸收都走同一套 ready 迁移与计数，并把吸收分支的 `routeStage` 一并收正到 `local_ready/direct_route_ready`。 |
| 2026-04-06 | 最小验证已通过：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 为 `1 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过。当前最新成功链仍然是直连，不是中继；`progress` 里最近成功 token `ui_dm_20260406_083742` 已坐实 `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=4096`。 |
| 2026-04-06 | 新的本地节点自杀链已经前推并收掉。上一轮失败 token `ui_dm_20260406_083115` 里，app 侧其实已经走到 `runPeerRouteSessionWarmAsync warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session`，随后 sender 才在 `9810ms` 处报 `ack timeout`；本机 crash report `/Users/lbcheng/Library/Logs/DiagnosticReports/tsnet_product_node-2026-04-06-083212.ips` 已坐实本地 `19125` 节点死在 `forAllChildren -> collectCycles -> rawNewString -> socialStatePath -> persistSocialStateUnsafe -> finalizeReceivedDirectMessageAsync`。 |
| 2026-04-06 | 已把 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `social_state` 文件路径改成节点初始化时一次计算并固化到 `node.socialStatePathText`，收消息后处理不再在热路径现拼 `cfg.dataDir.get() / "social_state.json"`。`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 继续 `1 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过。 |
| 2026-04-06 | 用新本地节点 fresh 重拉后，当前 seed 已更新为 `/ip4/100.64.148.187/udp/64906/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。同一条 production UI token `ui_dm_20260406_083742` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；app 日志闭环到 `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=4096`，节点日志也已坐实 `handleDirectMessageNode persistSocialState begin -> done -> postprocess done`，没有再出现 node 自杀。赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 继续健康；`adb logcat -b crash` 里仍然只有华为输入法噪音。 |
| 2026-04-06 | 已继续把“不要等超时再猜”落成主动日志：在 [libp2p/protocols/dm/dmservice.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim) 给 `sendFrameOnSession()` 补了 `frame write begin/done/failed`，并在 `handleIncoming()/runSession()` 直接打印 `incoming handler begin/stored session/run start`；在 [libp2p/transports/nim-msquic/api/api_impl.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/nim-msquic/api/api_impl.nim) 给 `sendOneRttPacketAsync()` 和 `emitOrderedStreamReceive()` 补了 `MsQuic UDP stream packet send begin/done`、`MsQuic ordered stream receive`。这样这条链已经能直接回答“首帧到底是没早发，还是没早到”。 |
| 2026-04-06 | 带上这版日志后，`nim c -r tests/testmultistream_stability.nim` 继续 `3 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；自动 `adb install -r` 后，设备 `lastUpdateTime=2026-04-06 06:54:58`，安装器两层确认已自动点完。 |
| 2026-04-06 | 随后沿同一 fresh `19125` 节点 seed `/ip4/100.64.148.187/udp/56611/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 复跑 production UI token `ui_dm_20260406_065541`，instrumentation 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 继续健康。更关键的是这次时序已经收清了：app 侧 `dm_protocol_select_done` 在 `06:56:02.776`，`dm_write_begin` 在 `06:56:02.878`，中间只有约 `102ms`；`direct message session frame write done elapsedMs=1`，`MsQuic UDP stream packet send done elapsedMs=1`，说明“protocol 选完后 sender 迟迟不发第一帧”已经被排除。 |
| 2026-04-06 | 同一轮新的唯一大头也已经前推成两段可量化时延：app 侧 `multistream dialer handshake read done elapsedMs=730`，`select_first_proto read done elapsedMs=54`；节点侧 `direct message incoming handler stored session -> run start -> readLp begin` 已经是立刻发生，而 `readLp begin -> readLp done` 约 `799ms`。最新 `sendThreadMessage success durationMs=2666` 已经压进 `3s` 内，所以当前真正剩下的只是一件事：继续收 `handshake response` 和 receiver 首帧 payload 到达这两段，而不是 sender 写入或 protocol select。 |
| 2026-04-06 | 已把 [libp2p/multistream.nim](/Users/lbcheng/nim-libp2p/libp2p/multistream.nim) 的 v1 直拨首飞收成 handshake 与首个 protocol 同包序列：`performDialerHandshake()` 新增 `pipelinedV1Proto`，dialer 在写完 `"/multistream/1.0.0\\n"` 后立刻把首个 protocol 也写出去，不再先等 handshake 回包再发；随后 `select_first_proto` 阶段若已 pipeline 就只读响应，不再二次 write。新增 [testmultistream_stability.nim](/Users/lbcheng/nim-libp2p/tests/testmultistream_stability.nim) 的 `PipelinedDialerStream` 回归，断言“第二次 write 发生在第一次 read 之前”，`nim c -r tests/testmultistream_stability.nim` 已为 `3 OK`。 |
| 2026-04-06 | 沿同一条 production UI 1:1 链重新 fresh 安装与验证后，新主包 `lastUpdateTime=2026-04-06 06:44:13`、fresh 本地 seed `/ip4/100.64.148.187/udp/60912/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，token `ui_dm_20260406_064457` 继续 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners[*].stage=incoming`。但这轮时延没有下降，反而把新的唯一瓶颈前推出来了：dialer 侧 `multistream dialer handshake read done elapsedMs=2329`，而 `multistream dialer select_first_proto read done elapsedMs=0`；节点侧 `multistream listener first_read/handshake write/post_handshake read/protocol_ack write` 都已是 `elapsedMs=0`。这说明旧的第二轮 RTT 已被成功消掉，新的大头已经缩成“第一轮 handshake 回包为什么晚到”。 |
| 2026-04-06 | 同一轮节点 PTY 还坐实了第二个新的慢点：listener 在 `protocol_ack write done` 后并没有立刻进入慢读，真正拖长的是 sender 首帧 DM payload 到达时间。节点侧 `direct message readLp done` 比 protocol 已协商完成又晚了约 `1541ms`，而 sender 最终 `sendThreadMessage success durationMs=6695`。当前主线已不再是 `select_first_proto`，而是继续往 `protocol_select_done -> dm_write_begin/write_done` 与 receiver `handleIncoming -> readLp begin/done` 之间收唯一瓶颈。 |
| 2026-04-06 | 已继续把 Android truth-route 预热从“公用 `Dispatchers.IO` 上排队”收成专用单线程：在 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 的 `updateActiveTruthRouteDirectWarmTarget()` 里，route prepare 现在先 `runtimeSupervisor.scheduleWarm("android_truth_route_prepare:<pathHash>")`，再 declare direct target；同时把 `ensureTruthRouteRuntimeActivationWorker()` 从 `viewModelScope.launch(Dispatchers.IO)` 改到独立 `truthRouteRuntimeActivationScope + Dispatchers.IO.limitedParallelism(1)`，不再和启动期其它 IO 重活抢调度。 |
| 2026-04-06 | 带上这条修正后，新主包 `lastUpdateTime=2026-04-06 06:06:36` 在同一 fresh `19125` 节点 seed `/ip4/100.64.148.187/udp/49873/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_060709` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已坐实预热来源从旧的 `android_truth_route_activation` 前移成 `android_truth_route_prepare`，并在发送前完成 `runPeerRouteSessionWarmAsync ... warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session`。同轮 `sendThreadMessage success durationMs=4099`，相对前一轮 `5918/6008ms` 已明显下降。赛后 `tailnet_status.providerReady=true`、`proxyListenersReady=true`，`local_info.listenAddrSource=relay_listeners`，`adb logcat -b crash` 里仍然只有华为输入法自己的 Room 噪音，没有 `com.unimaker.native.debug` 的新 crash。 |
| 2026-04-06 | 已把 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `social_state` 热路径收成“首次读盘，之后只认 `node.socialStateParsedSnapshot + socialStateSnapshot`”；`loadSocialStateUnsafe()` 不再每次 `readFile + parseJson`，`persistSocialStateUnsafe()` 也先回写内存快照再落盘。新增 [testmobile_ffi_social_state_cache.nim](/Users/lbcheng/nim-libp2p/tests/testmobile_ffi_social_state_cache.nim) 回归，验证第一次加载后即使磁盘文件被改坏，第二次 diagnostics 仍稳定读取内存快照。`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 已通过。 |
| 2026-04-06 | 带上这条修复后，新主包 `lastUpdateTime=2026-04-06 04:59:06` 的 production UI token `ui_dm_20260406_045946` 不再出现本地 `19125` 节点自杀；`adb logcat -b crash` 没有 `com.unimaker.native.debug` 或本地 node 的新崩溃，`wait_dm` 是 `wait_dm_timeout`。新的真实失败前推成 `connect_exact_failed ... no tsnet direct route is registered for /ip4/100.64.148.187/udp/63611/quic-v1/tsnet`，同时日志坐实 `waitForDirectOrRelaySessionReady` 看到的是 `directStatus=route_wait + relayStatus=running`，但失败 payload 还假写成 `sendPath=direct_ready_session`。 |
| 2026-04-06 | 已继续修 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)：新增 `selectedReadySendPath()`，失败态 `sendPath` 改成显式 `not_ready`；同时给 `reconcilePeerRouteSession()` 补了 `live_ready / retry_pending / already_running / spawn` 主动日志，不再等超时后猜 relay reconcile 是否真的开跑。 |
| 2026-04-06 | 重新打包安装后，设备安装时间更新到 `2026-04-06 05:09:12`；期间已经把华为安装器两层确认页自动点完，不再停在 `InstallStaging`。fresh 本地 `19125` 节点 seed 更新为 `/ip4/100.64.148.187/udp/52250/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。 |
| 2026-04-06 | 新主包 `lastUpdateTime=2026-04-06 05:09:12` 下，production UI token `ui_dm_20260406_050950` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已闭环：`reconcilePeerRouteSession spawn -> waitForDirectOrRelaySessionReady directStatus=route_wait relayStatus=running -> runPeerRouteSessionWarmAsync warm_done ... pathKind=direct stage=fresh_dial_ready -> cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=5818`。赛后本地 `19125` 继续健康，`tailnet_status.proxyListenersReady=true`，`local_info.listenAddrSource=relay_listeners`。 |
| 2026-03-30 | 已把 Android/native 异步架构约束写成文档 |
| 2026-03-30 | 已把 `NativeNodeDispatcher` 从 2 线程改成 1 线程 |
| 2026-03-30 | 已把发送窗口的只读状态改成优先走缓存 |
| 2026-03-30 | 已重新打包并安装 Android Debug 包 |
| 2026-03-30 | 已把 Android 发送主链改成 `register_hints + send_with_ack_kickoff`，移除 Kotlin 同步 `prepare_route/connect_exact/waitSecure` |
| 2026-03-30 | 已把 `seedAddrs/source` 接进 `send_with_ack` native kickoff，发送任务改为自己处理 connectivity |
| 2026-03-30 | 已去掉消息页发送前等待后台 preconnect job 的 2.5 秒阻塞 |
| 2026-03-30 | 已把 `refreshNodeResources`、`connectedPeers`、`connectedPeersInfo` 收成发送优先的低优先级缓存快照 |
| 2026-03-30 | 已把 UI 帧轮询和系统状态刷新在发送关键窗口内改成直接让路或使用快照 |
| 2026-03-30 | 已给 `getLastDirectError` 增加缓存快照，发送失败后不再因为读错误详情再次卡全局锁 |
| 2026-03-30 | 已确认远端到本地失败的单一主因是本地 `proxyListenersReady` 吃旧缓存，gateway 已无该 route 仍显示 ready |
| 2026-03-30 | 已把远端产品节点操作收成结构化 Nim 工具 `tsnet_product_rpc`，支持 `--op/--peer-id/--listen-addr` 和 `--ssh-host`，不再手写远端 JSON |
| 2026-03-30 | 已同步并在远端用 `/root/Nim/bin/nim` 编译新的 `tsnet_product_rpc`，本地和远端 `tailnet_status/local_info` 已跑通 |
| 2026-03-30 | 已确认 `connect_exact` 真正失败点已收敛到 identify：`peerConnected=true`、`secureOk=true`、`identifiedOk=false` |
| 2026-03-30 | 已修掉 `tsnet_product_node` 把 kickoff 初期短暂 `missing` 误报成 `connect_exact_missing_state` 的竞态 |
| 2026-03-30 | 已给 `ensurePeerIdentified` 和 `peerStore.identify` 补窄日志，能区分 `beginProtocolNegotiation`、`multistream select` 和最终等待超时 |
| 2026-03-30 | `test_msquic_concurrent_streams` 继续通过，说明当前主线不是 QUIC 多流基础件整体失效，而是产品 identify 路径 |
| 2026-03-30 | 已验证新版 `tsnet_product_node` 下，同步 `connect_exact` 不再秒回 `connect_exact_missing_state` |
| 2026-03-30 | 已把 `idleWakeLoop` 的 listener 自修从“构整份 tailnetStatus JSON”改成轻量布尔检查，节点在 `connect_exact` 期间不再因后台状态轮询崩溃 |
| 2026-03-30 | 已修掉 `quicrelay` 里 owner 级状态写成局部副本的问题：`relayOwnerTasks`、`relayListenerStates`、`relayUdpDialStates`、`relayOwnerClients` 现在都是真正写回全局表 |
| 2026-03-30 | `testtsnetquicrelay.nim` 已恢复为 `16 OK`，说明 relay listener/dial 状态机这一层已经收住 |
| 2026-03-30 | 本地 fresh 产品节点重新验证后，新的真实 blocker 已经前移到 `9444` 控制握手：`wait_tailnet_ready` 失败，错误是 `nim_quic connection event and handshake did not complete` |
| 2026-03-30 | 已确认本地产品节点 `local_info` 只返回 `relay_listeners` 的真实 IPv4 route，`listenAddrSource=relay_listeners`，不再按 `tailnetIPs` 伪造双栈 `/tsnet` 地址 |
| 2026-03-30 | 已把远端产品节点更新到同版源码并重编恢复服务，远端 `local_info` 现在也只返回 `relay_listeners` 的真实 IPv4 route，`listenAddrSource=relay_listeners` |
| 2026-03-30 | 已用真实 `relay_listeners` route 实跑 Mac→远端产品节点：`connect_exact` 成功，`identifiedOk=true`，说明地址问题已排除 |
| 2026-03-30 | 同一轮实跑里 `send_dm` 仍然返回 `send_dm_timeout`，远端 `wait_dm` 未收到 token；当前主线已收窄到 `connect_exact` 之后的 identify/DM 发送后半段 |
| 2026-03-30 | 已给 `quicrelay` 长期控制流所有关键写操作加超时，避免 `incoming/ready/await/ack` 卡死后还长期占住 route |
| 2026-03-30 | 已把这条长期控制流约束写进 `docs/transports/tsnet_quic_relay_protocol.md`，以后控制流写超时必须立刻失效重建 |
| 2026-03-30 | 已定位 `send_dm_kickoff` 的预序列化直发包缺少 `from` 等字段；发送端即使拿到 LP ack，接收端仍可能因为 sender 身份为空而不入 `dm_rows` |
| 2026-03-30 | 已修 `libnimlibp2p.nim`：预序列化直发包现在统一补齐 `op/mid/messageId/from/timestamp_ms/timestampMs/ackRequested/reply_to/target` 后再发送 |
| 2026-03-30 | 已强制远端用更新后的 `libnimlibp2p.nim + dmservice.nim` 全量重编 `tsnet_product_node` 并重启 `nim-tsnet-product.service` |
| 2026-03-30 | 已把 `runtime.proxyListenerReadiness` 改成按 gateway 真实发布态校验 route，不再只信本地 `relayReadyRoutes` 缓存 |
| 2026-03-30 | 修完后第一轮 live 已证明先前的 `route_not_registered` 分叉被压下去了：`connect_exact` 一度重新恢复成功 |
| 2026-03-30 | 已定位 `send_dm_kickoff` 的另一处错层：外层 `connect_exact` 成功后，内部 `executeSendDirectCommand` 还会因为带显式地址再次走 `ensurePeerReadyViaExplicitAddresses` 重拨 |
| 2026-03-30 | 已修 `executeSendDirectCommand`：若 peer 已连上，则直接 `waitForPeerReady`，不再对同一 peer 做二次 exact dial |
| 2026-03-30 | 已确认远端 route 不是“从未注册”，而是 18:03:21 因 `incoming` 写 10 秒超时被 gateway 主动下架，之后所有拨号都变成 `missing listener` |
| 2026-03-30 | 已定位节点侧自动补 listener 失效的直接原因：`runtime.listenerNeedsRepair` 和 `tailnetListenerNeedsRepair` 都漏掉了“本地仍 awaiting/ready，但 gateway 已经无 route”的情况 |
| 2026-03-30 | 已把 `tailnetControlRefreshLoop` 改成只有地址真变化才触发 `syncAdvertisedPeerInfo`，并给 `spawnTailnetWarmAdvertisedPeerInfo` 加了单任务并发门，避免产品节点自己每秒叠加发布同步 |
| 2026-03-30 | 本地新二进制验证已通过：`wait_tailnet_ready` 恢复秒回，`local_info` 再次只发布 `relay_listeners` 的真实 route，本地不再出现 99% CPU 的自刷状态 |
| 2026-03-30 | 新一轮本地→远端 `connect_exact` 失败重新收敛为 `route_not_registered`，同时远端 journal 明确显示它仍在每秒刷 `syncAdvertisedPeerInfo reason=tailnet_warm`，说明远端当前还在跑旧产品二进制，尚未吃到这轮本地修复 |
| 2026-03-30 | 已把 `tsnet_product_node.localInfo` 改成惰性优先 `relay_listeners -> runtime -> published -> synthesized`，不再在热路径里 eager 跑 discovery/network snapshot；本地和远端 `local_info` 现在都秒回 |
| 2026-03-30 | 已用 `tsnet_remote_admin --op deploy-product` 把远端产品节点切到新二进制；远端 `nim-tsnet-product.service` 已重启到最新版本 |
| 2026-03-30 | 已再次实跑本地→远端产品路径：`connect_exact` 成功，`send_dm` 成功，远端 `wait_dm` 已收到并入库 token `dm_token_20260330_193650` |
| 2026-03-30 | 已实跑远端→本地反向产品路径：`connect_exact` 成功，`send_dm` 成功，本地 `wait_dm` 已收到并入库 token `dm_token_20260330_193745` |
| 2026-03-30 | 已修 `runSendDirectKickoffAsync` 没把 `requestKey` 传进内层 `cmdSendDirect` 的真 bug；之前 Android `send_with_ack` 虽然真的在跑，但状态表永远只停在 `kickoff` |
| 2026-03-30 | 已给 `send_with_ack` 和 `multistream` 加窄日志，并重新打包安装 Android Debug 包；设备 `com.unimaker.native.debug` 已更新到 20:35:57 这版 |
| 2026-03-30 | 已把 `dmservice.send` 的内层阶段 `reuse_live/fresh_dial/write/ack_wait/ack_done` 接进 `send_with_ack` kickoff 状态机，Android 下一轮不再只看到 `send_begin` 这个黑盒阶段 |
| 2026-03-30 | 已用正确的 `-d:libp2p_msquic_experimental` 编译门验证新的 `libnimlibp2p.nim + dmservice.nim` 通过，本地产品节点构建未被这轮日志改动打坏 |
| 2026-03-30 | 新日志已证实 Android 发送失败里那条 `kickoff` 不是网络阶段，而是状态传播 bug；本地 Mac 仍能看到 identify 流和后续第二条新流 |
| 2026-03-30 | 重装后的生产 UI 自动点击使用了旧坐标，没有真正触发新一轮发送；当前剩余的是 UI 自动动作对齐问题，不再是 `requestKey` 状态 bug |
| 2026-03-30 | 已修 Android tsnet 地址发布污染；`libnimlibp2p.nim` 现在在 tsnet 模式下只发布 `/tsnet` 地址，不再混入宿主机 `10.x/192.168.x` host-network 地址 |
| 2026-03-30 | 已把 `NativeAppViewModel.configureNativeConnectivity` 收成单飞 + 2.5 秒冷却窗口；高频重复调用现在会直接 `skip=cooldown/in_flight` |
| 2026-03-30 | 已修 `MainActivity` deep link 入口：truth route 现在从 `intent.data` 解析，不再只认 `route` extra |
| 2026-03-30 | 已实测验证新 deep link：冷启动后可直接落到目标节点消息页，`seedAddrsJson` 和 `draftText` 都能进页面 |
| 2026-03-30 | 已用生产 UI 真正发出 token `ui2329cfg2`；本地 Mac `wait_dm` 超时，说明这次失败不是假点击 |
| 2026-03-30 | Android 这轮失败已从旧的 `peer_ready_failed` 收敛成 `send_with_ack_timeout`，状态卡在 `send_begin`；页面顶部显示 `libp2p RELAY · tailnet RELAY · 发送失败 send_begin 10062ms send_with_ack_timeout` |
| 2026-03-30 | 这轮 logcat 已证明后台连通性刷新不再是主因：发送后只有 `configureNativeConnectivity skip=cooldown`，没有再次把主链打回 `peer_ready_failed` |
| 2026-03-31 | 新一轮 Android 生产 UI 发送里，顶部状态已从粗粒度 `send_begin` 前推到 `wait_send_ack`，说明 `dmservice.send` 的内层阶段已经开始回写到 kickoff 状态机 |
| 2026-03-31 | 已抓到 Android 真崩点：进程不是正常超时退出，而是在后台线程调用 `libp2p_get_connected_peers_json/social_list_discovered_peers` 时触发 `FORTIFY: FD_SET: file descriptor 1024 >= FD_SETSIZE 1024` 并被系统杀回桌面 |
| 2026-03-31 | 已把 Android interactive native priority 窗口上限从 30 秒提到 120 秒，并把消息发送入口的窗口时长从 12 秒提到 90 秒，避免 tsnet 发送尚未结束时后台轮询恢复直打 native |
| 2026-03-31 | 已删除 `BridgeFacades.sendDirectText` 里的同步 `prepareDirectRoutePeer`；消息页现在只做 `register_hints + send_with_ack_kickoff(seedAddrs)`，不再先走一条错层的 `connectPeer/socialConnectPeer/connectMultiaddr` 预连接 |
| 2026-03-31 | 已把 tsnet 发送 budget 调整为由 `send_with_ack` 自己覆盖完整 `connect + ack`，并重新安装 Android Debug 包；设备 `com.unimaker.native.debug` 更新时间为 `2026-03-31 00:32:43` |
| 2026-03-31 | 已修 `connmanager`：`maxConnsPerPeer=1` 不再因 off-by-one 先放进第 2 条 live 连接；同一 peer 的新同方向连接到来时会先踢掉旧残留，再收新连接 |
| 2026-03-31 | `tests/testconnmngr.nim` 已回归通过，当前是 `25 OK`，并新增覆盖“peer slot 已满时替换同方向旧 live muxer”的测试 |
| 2026-03-31 | 新一轮 Android 生产 UI 实跑里，本地节点不再出现 `Too many connections for peer`；真实失败已前移成 `dm_fresh_dial_begin 33082ms send_with_ack_timeout` |
| 2026-03-31 | 本地节点日志已坐实这轮真死点不是 `connmanager`，而是 listener 持久 accept 流在同一 relay client 上跑 `route_status` 校验时 10 秒超时，把正在跑的 bridge 一起关掉 |
| 2026-03-31 | 已把 listener 持久 accept 流的 `route_status` revalidate 改成独立 fresh probe，不再复用当前 live control client 连接自杀式校验 |
| 2026-03-31 | 已修 `tsnet_product_node.relayPublishedListenAddrs` 的 nil 崩溃；此前 `local_info` 会因空 `tailnetPayload/relayListeners` 直接 SIGSEGV 杀掉本地产品节点 |
| 2026-03-31 | 当前最新 blocker 是本地产品节点 listener 启动期反复报 `udp listener register ack after 10s`；节点最终能回到 `listenAddrSource=relay_listeners`，但 `proxyListenersReady` 仍可能停在 `false/connecting`，导致这轮尚未完成稳定的端到端 DM 复验 |
| 2026-03-31 | 已给 `quicrelay.sharedRelayClient` 增加失效路径：UDP listener 创建注册流失败、`register ack` 超时或被拒时，会主动丢掉当前坏 relay client，下轮 fresh connect，不再死复用同一条卡死连接 |
| 2026-03-31 | 最新一轮带 stale-client 修复的本地节点重启后，tailnet warm 又前移卡在 `warm_provider_begin`；`local_info` 40 秒后仍是 `listenAddrSource=synthesized`、`tailnetStartStatus.stage=starting`，因此本轮还不能继续做可信的 Android 端到端 DM 结论 |
| 2026-03-31 | 已修 `quiccontrol.executeSingleQuicRequest`：builtin QUIC control 单请求流在写完 framed request 后会立刻 `closeWrite()`；本地 `testtsnetquiccontrol.nim` 现在是 `7 OK` |
| 2026-03-31 | 已新增 `tests/testtsnetquiccontrol_fin.nim`，覆盖“服务端必须等 EOF/FIN 才开始解码 request”的场景；当前 builtin QUIC 下回归结果是 `1 OK` |
| 2026-03-31 | 已用同一份 builtin QUIC wire probe 做本地/远端对照：本地 `tsnet_quic_gateway_service` 在 `framed+SNI`、`raw+SNI`、`raw+no SNI` 三组请求下都能秒回；远端 `64.176.84.12:9444` 在同三组请求下都表现为“握手成功、写成功、零响应” |
| 2026-03-31 | 当前 blocker 已进一步坐实到远端 `nim-tsnet-gateway` control 服务可用性/部署状态，不再是本地 builtin QUIC 客户端的 FIN、framing、SNI 或 DM 上层语义问题 |
| 2026-03-31 | 已修 `tsnet/proxy.nim` 的 registry nil 崩溃；`proxyRouteSnapshots -> sanitizeProxyRoutesUnsafe -> sharedKeyToString` 现在不会再因半残 key 把本地产品节点打成 `SIGSEGV`，`tests/testtsnetproxy.nim` 当前是 `9 OK` |
| 2026-03-31 | 已把 `quicrelay` gateway 的重复 `listen/accept` 收紧成“不覆盖正在 `ready_wait/busy` 的 active listener”，远端 journal 不再出现 `nim_quic relay accept stream lost attached listener route` 这条真 bug |
| 2026-03-31 | 修补后重启本地产品节点，`local_info` 再次稳定回到 `listenAddrSource=relay_listeners`、`tailnet_status.providerReady=true`；本地节点不再在发送窗口里自崩 |
| 2026-03-31 | 最新一轮 Android instrumentation 没进入发送链路，失败点前移成 `send button not shown`；远端 gateway 同窗口里只有周期性的 `route_status`，没有新的 `dial/bridge attach`，说明这轮是 UI 自动化入口问题，不是 DM 底层新回退 |
| 2026-03-31 | 已修 `quicrelay.readJsonMessage/readBinaryFrame` 的 framed reader 真 bug：当 `takeCachedBytes()` 已经拿到完整 frame 时，现在会先直接消费，不再错误继续 `stream.read()` 把 persistent accept lane 卡死；同时补了 listener restart 时关闭后未清空 `acceptStream/regStream` 的悬挂引用 |
| 2026-03-31 | 新增 `cached complete json/binary frame is consumed before waiting for more bytes` 两条回归并通过，`accept stream can receive incoming over the same client initiated stream`、`route_status stays published while listener ready is pending` 也继续通过 |
| 2026-03-31 | 用新二进制重启本地产品节点后，`tailnet_status.proxyListenersReady=true`、`relayListeners[0].stage=awaiting`、`local_info.listenAddrSource=relay_listeners` 已恢复，说明本地 relay listener 稳态正常 |
| 2026-03-31 | 最新一轮 Android 生产 UI 真发结果仍是 `发送失败 peer_ready_failed 10110ms connect timeout`，本地 `wait_dm` 超时；这次不再是“gateway 已写 incoming 但本地不读”的旧主因，而是更前面的真实 connect timeout 还没打通 |
| 2026-03-31 | 新一轮复跑发现当前 Android 并未进入发送链路，失败点前移成 `message input not shown`；手工 `uiautomator dump` 现场已坐实系统当前顶层是 `com.android.systemui` 的 PIN 锁页，不是会话页 |
| 2026-03-31 | 已修 `ConversationRouteSendTest.kt`：测试现在改走真 deep link `app://unimaker/...`，并在开跑前先 `wakeUp + dismiss-keyguard + stayon`；若仍被系统 PIN 锁住，会直接报 `device locked by system PIN`，不再白等 60 秒误报输入框缺失 |
| 2026-03-31 | 已重新编译并安装 Android 主包与 `androidTest` 包；锁屏现场回跑验证通过，新 instrumentation 会在约 `2.75s` 内真实失败到 `device locked by system PIN; unlock the phone once and rerun instrumentation` |
| 2026-03-31 | 当前唯一外部 blocker 是设备处于系统 PIN 锁屏态；在人工解锁一次之前，无法继续做可信的生产 UI 1:1 DM 发送验证 |
| 2026-03-31 | 已修 Android peers 刷新写库的真崩点：`RoomSocialRepository.replacePeers` 不再高频 `clear + 全量 upsert`，改成规范化后“没变化不写、有变化增量替换”；最新生产 UI 发送已不再因 `PeerDao_Impl.upsertAll` 触发 SQLite `SIGSEGV` |
| 2026-03-31 | 已把 Android DM 发送总预算收成硬 10 秒路径：`BridgeFacades.sendDirectText`、`ChengLibp2pNative.sendWithAckKickoff`、`libnimlibp2p.executeSendDirectCommand/ensurePeerReadyViaExplicitAddresses` 都已去掉额外的 12 秒下限、+3 秒尾巴和 late-connect 漂移 |
| 2026-03-31 | 最新生产 UI 1:1 实跑 token `ui_dm_20260331_153421` 已验证用户可见真实失败收敛到 `peer_ready_failed 9873ms connect timeout`；同窗 `sendWithAckKickoff task command_done durationMs=9802`，本地 `wait_dm` 仍超时 |
| 2026-03-31 | 当前主线已从“假失败/崩溃/超 20 秒”收敛为“10 秒内真实 connect timeout”；下一步应直接对照你给的 1 到 3 秒基线，继续收 Android 为什么没复现到同样的快速 route 命中 |
| 2026-03-31 | 已修 `cmdConnectedPeersInfo` 的后台只读真 bug：它不再在状态快照里实时做 transport RTT probe，Android 发送前不再被 `connectedPeersInfo/diagnostics` 队列卡成 `queued 12253ms send_with_ack_timeout` |
| 2026-03-31 | 重编并重装带新 `libnimlibp2p.so` 的 Android Debug 包后，最新生产 UI 1:1 实跑 token `ui_dm_20260331_160713` 已不再出现 `queued`；真实结果前移为 `peer_ready_failed 9882ms connect timeout`，本地 `wait_dm` 仍为 `wait_dm_timeout` |
| 2026-03-31 | 当前最新结论：`builtin QUIC` 拥塞控制不是这轮主因。队列饿死已排除后，Android 还在 `ensurePeerReadyViaExplicitAddresses -> connectMultiaddrExactAsync` 阶段真实超时；下一步应继续收 Android exact connect/route publish，而不是回头怀疑发送窗口或旧假失败 |
| 2026-03-31 | 已新增 `probeIdlePersistentListenerRouteStatusStability` 和 `route_status stays published while persistent listener idles for 12 seconds` 回归，用本地 relay gateway 连续探测 idle accept 长流的发布态 |
| 2026-03-31 | 新增的本地 idle 回归已通过，说明当前没有证据支持“builtin QUIC accept 长流 idle 10 秒以上会自己掉 route”这条客户端基础件缺陷；生产现场剩余主线继续收远端真实 `nim_quic relay route lost` |
| 2026-03-31 | 已给 `quicrelay` 补精确日志：当 gateway 返回 `route_status published=false` 时，会直接打印 `listener nil/persistent/streamNil/published/awaiting/busy/readyPending`；连接关闭清理 listener 路由时也会打印被清掉的 routes，下一轮远端若再掉 route 可以直接看是“流真没了”还是“路由被清了” |
| 2026-03-31 | 已把 `tsnet_remote_admin` 扩成 gateway 也能结构化运维：新增 `sync/build/restart/status/deploy/journal-gateway`，并补了文档；以后远端 gateway 不再手写远端命令 |
| 2026-03-31 | 已用新 `deploy-gateway` 成功把远端 `nim-tsnet-gateway.service` 切到新二进制；新 `status-gateway` 显示 `ExecMainStartTimestamp=Tue 2026-03-31 17:00:16 CST`，二进制时间戳为 `2026-03-31 17:00:06 +0800` |
| 2026-03-31 | 远端 gateway 新日志已坐实这轮真因：`17:09:58 incoming_write_done` 后一直卡在 `gateway listener ready_wait_begin`，到 `17:10:28` 才报 `listener notify/ready failed ... timed out waiting for gateway listener ready after 30s`，随后 `listener stream detach + unpublished`；`route_status false listener=nil` 只是后果 |
| 2026-03-31 | 同一轮本地产品节点日志里仍然只有 `incoming_payload_wait_begin` 和持续 `published=true` 的 revalidate，没有任何 `udp listener incoming routeId` / `udp listener ready routeId`；说明本地 listener 根本没把 gateway 已经写出的 `incoming` 读出来 |
| 2026-03-31 | 已把 `awaitPersistentIncomingWithRouteValidation` 里的 fresh `route_status` probe 收成 async，不再在 accept 流等待路径里直接 `waitFor`；本地 `testtsnetquicrelay.nim` 编译通过，本地产品节点已重编重启 |
| 2026-03-31 | 但修完 async probe 后，最新生产 UI token `ui_dm_20260331_170945` 仍是 `peer_ready_failed 9905ms connect timeout`，本地 `wait_dm` 继续超时；当前 blocker 继续前移为“真实远端 `incoming` 已写出，但本地产品节点 persistent accept 流没有读到它” |
| 2026-03-31 | 已把 Android `P2PRuntimeSupervisor.ensureTailnetReady` 的 ready 条件收紧到包含 bootstrap relay gate：若 `deferAutoJoinUntilRelayReady=true`，现在会显式 `reserveOnAllRelays` 并等待 `startupRelayReady`，否则直接报 `relay_not_ready`，不再把“provider/listener 已起、relay 还没 ready”误判成 tailnet 就绪 |
| 2026-03-31 | 同时补抓远端 gateway 最近窗口后，仍然只看到本地 Mac route `100.64.160.30` 的周期性 `route_status`，没有 Android peerId `12D3KooWFAQPMC3LWnu9E3BoAuA1P5BKFWAh74AYdAowaeKcyob2`，这和“Android 之前可能过早放行、实际还没过 relay gate”是对得上的 |
| 2026-03-31 | 新的 Android 改动已通过 `./gradlew :app:compileDebugKotlin -x lint -x test --no-daemon` 和 `./gradlew :app:compileDebugAndroidTestKotlin -x lint -x test --no-daemon`；中途出现的是 Kotlin 增量缓存/daemon 脏状态噪音，fallback 非增量编译后构建成功，不是代码语法错误 |
| 2026-03-31 | 已修 `libnimlibp2p.ensureLocalTsnetReady` 的发送前 gate 过严真 bug：主动拨 seed `/tsnet` 地址发送 DM 时，现在只要求本机 `providerReady`，不再同步硬等 `proxyListenersReady`；最新生产 UI token `ui_dm_20260331_193838` 起，`local_tailnet_ready_wait` 已彻底消失，失败前移回真实发送链 |
| 2026-03-31 | 已连续修掉本地产品节点三条真崩点：`runtime.statusPayload` 里 `resolvedRelayDialUrl/liveQuicRelayMode` 的状态读崩溃、`proxy.sanitizeResolvedRemotesUnsafe/registerResolvedRemote` 的 shared key 悬空崩溃、`quicrelay.relayRegisterTaskSafe` 跨 safe wrapper 返回 `ref handle` 的 `asgnRef/decRef` 崩溃；对应回归 `testtsnettransport.nim` / `testtsnetproxy.nim` / `testtsnetquicrelay.nim` 全部通过 |
| 2026-03-31 | 最新生产 UI 1:1 实跑 token `ui_dm_20260331_194833` 已验证：本地产品节点在整轮发送期间持续存活，`tailnet_status` 赛后仍可秒回，`wait_dm` 最终为 `wait_dm_timeout`；Android 用户可见失败稳定收敛成 `peer_ready_failed 9818ms connect timeout`，说明主线已不再被本地崩溃和假阶段误导 |
| 2026-03-31 | 已修 `dmservice.send` 对显式地址的过度 fresh dial：新增“本请求已验证 live connection”开关，`cmdSendDirect` 只有在 `peer ready` 成功后才允许显式地址先复用 live connection，默认显式地址语义仍保持原样 |
| 2026-03-31 | 带新修补的生产 UI token `ui_dm_20260331_215231` 已把旧的 `Stream Underlying Connection Closed!` 前推掉：Android 日志明确显示 `ensurePeerReadyViaExplicitAddresses attempt:connect_done ok=true peerConnected=true`，随后进入 `peer_ready_wait_start waitMs=4509` |
| 2026-03-31 | 同一轮用户可见失败变成 `connect_exact 9813ms send_with_ack_timeout`，本地 `wait_dm` 仍是 `wait_dm_timeout`；说明当前 10 秒硬限制内的真实主线已前移成“exact connect 成功后，peer ready / send 后半段没有按时完成” |
| 2026-03-31 | 本地产品节点这轮发送前是 `relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners`；发送后 `tailnet_status.relayListeners=[]`、`local_info.listenAddrSource=runtime`，说明 listener published 稳态在发送窗口里有波动，这条已成为下一轮优先怀疑点 |
| 2026-03-31 | 已修 `send_with_ack_kickoff` 的 deadline 起点错位：native 命令现在在 FFI 入队时就写死 `deadlineAtMs`，后台 `runSendDirectKickoffAsync` 只消费剩余预算，不再让 Java 先超时、Nim 还在继续跑 |
| 2026-03-31 | 已按新代码重编 `tsnet_product_node` 和 Android Debug 包，并用 `adb install -r` 覆盖安装；本地 `19125` 重启后再次恢复到 `relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners` |
| 2026-03-31 | 最新生产 UI 1:1 实跑 token `ui_dm_20260331_224458` 已打通：instrumentation 为 `OK (1 test)`，本地 `wait_dm` 已收到同名 token |
| 2026-03-31 | Android/native 日志已闭环：`cmdSendDirect success`，`sendWithAckKickoff task command_done durationMs=7701`，`sendThreadMessage success stage=sent durationMs=7808`；当前 Android -> 本地 Mac 产品级 DM 主线已打通 |
| 2026-04-01 | 已把 `libnimlibp2p.nim` 的 exact-connect 多地址预算改成“剩余总预算按剩余尝试数均分”，不再给未来地址预扣整份名义预算；最新生产 UI token `ui_dm_20260401_124032` 已从旧的 `peer_ready_failed connect timeout` 前移成 `dm_fresh_dial_begin 9814ms send_with_ack_timeout` |
| 2026-04-01 | 已把 exact-connect 成功后的 `verifiedMuxer` 从 `ensurePeerReadyViaExplicitAddresses` 贯穿到 `sendDirectWithAckWaiter`，禁止“本次请求明明已经 exact-connect 成功，却在 DM 发送层又 fresh dial 一次” |
| 2026-04-01 | 带上 `verifiedMuxer` 贯穿后的最新生产 UI token `ui_dm_20260401_125457` 不再是旧的网络超时语义，而是 Android 主进程直接 `Process crashed`；`adb logcat -b crash` 已坐实为 `com.unimaker.native.debug` 的 `RenderThread` 在 `libhwui.so` 里 `SIGSEGV`，本地 `wait_dm` 同窗为 `wait_dm_timeout` |
| 2026-04-01 | 已把 `selectVerifiedMuxer` 从“只按 `Direction.Out` 取 muxer”改成“按当前 peer 的真实 live muxer 取值”；QUIC exact-connect 这条路径会先断旧 live 连接，不能再靠方向猜本次验证成功的 muxer |
| 2026-04-01 | 最新生产 UI token `ui_dm_20260401_152109` 已把 `dm_fresh_dial_begin` 真前推掉：用户可见失败变成 `connect_exact 9807ms send_with_ack_timeout`，本地 `wait_dm` 为 `wait_dm_timeout`，这轮已不再是“exact-connect 成功后发送层又 fresh dial 一次” |
| 2026-04-01 | 同一轮本地活实例日志仍持续打印 `relay route ready check ... publishedOk=true published=true`，但没有新的 `incoming_payload_wait_done` / `udp listener incoming` / `udp listener ready`；赛后 `tailnet_status.relayListeners=[]`、`local_info.listenAddrSource=runtime`，说明当前真实主线已前移成“listener published 日志和状态 API 出现矛盾” |
| 2026-04-01 | 已把 `ensurePeerReadyViaExplicitAddresses` 后的发送收紧成“优先等待 live `verifiedMuxer`，且只把本次 exact-connect 真命中的单个地址继续传给 DM send”，不再在成功 exact-connect 后拿整组 seed 地址重新并发 fresh dial |
| 2026-04-01 | 已修 `quicrelay.udpListenerLoop` 一条真稳定性问题：长活 UDP listener 不再和同 owner 其他 route 共用同一个 relay client；单 route 注册失败时不会再把兄弟 route 的连接一起 invalid 掉 |
| 2026-04-01 | 但这轮现场又前移出新的远端 blocker：本地产品节点 listener 注册收到的是真实 `register ack err=not_found`，而不是本地状态假象；最小 `tsnet_quic_relay_accept_probe` 直连 `quic://64.176.84.12:9444/nim-tsnet-relay-quic/v1` 也直接返回 `error=not_found` |
| 2026-04-01 | 同一 endpoint 的最小 `tsnet_quic_relay_probe` 结果是 `connected=true acknowledged=false error=\"nim_quic relay ping rejected\"`，说明当前远端 `nim-tsnet-relay-quic` endpoint 本身就没有按预期提供 relay 协议，不是 Android/UI 或本地产品节点独有逻辑 |
| 2026-04-01 | 已把 UDP listener 对确定性的 `register ack err=not_found` 收成 fatal stop；最新本地节点在 15 轮状态轮询后仍存活，`providerReady=true`、`libp2pPath=RELAY`，listener 阶段变成 `stopped` 而不是再把整个 `tsnet_product_node` 打成 `SIGSEGV` |
| 2026-04-01 | 已坐实上面那条“远端 relay 服务坏了”是误判：真 bug 是 `quicRelayEndpoint()` 在收到带显式端口的 control URL 时，把 `9444` 原样当成 relay 口保留下来；修后同样输入 `quic://64.176.84.12:9444/nim-tsnet-control-quic/v1` 会正确派生到 `quic://64.176.84.12:9446/nim-tsnet-relay-quic/v1`，最小 `ping/accept` probe 都已恢复成功 |
| 2026-04-01 | 带新 `quicRelayEndpoint` 修补重启并覆盖安装后，生产 UI token `ui_dm_20260401_172157` 暴露了新的本地 receiver 真崩点：`wait_dm` 不再是超时，而是 `socket closed while reading`；本地 `tsnet_product_node` 栈已坐实落在 `tsnettransport.lookupAdvertisedForRaw -> Table.getOrDefault` |
| 2026-04-01 | 已把 `rewriteAcceptedConnection` 的 raw->advertised 反查从全局 reverse registry 收回到 transport 自身 `listenerMappings`，并删掉多余的 endpoint 反查注册；`testtsnettransport.nim` 为 `16 OK`，`testtsnettransport_quic.nim` 为 `2 OK` |
| 2026-04-01 | 最新生产 UI token `ui_dm_20260401_172929` 已验证这条崩点被前推掉：本地产品节点整轮持续存活，赛后 `relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners` 仍稳定；用户可见失败回到真实 `connect_exact 9808ms send_with_ack_timeout`，本地 `wait_dm` 为 `wait_dm_timeout` |
| 2026-04-01 | 继续复跑后又抓到新的 receiver 真崩点：本地节点在 `udp listener incoming -> ready` 之后，不再死在 `lookupAdvertisedForRaw`，而是在 `libnimlibp2p.processCommandQueue` 新建 drain future 时炸进 `GC prepareDealloc`；对应现场 token 是 `ui_dm_20260401_173342`，`wait_dm` 再次变成 `socket closed while reading` |
| 2026-04-01 | 已把命令队列 ownership 再收紧一层：旧 queue tail、已完成命令对象、已完成 drain future 现在都由 node 持有到生命周期结束，不再在 receiver 回调把命令队列重新唤醒时中途释放 |
| 2026-04-01 | 带新命令队列修补重编本地产品节点和 Android 主包后，最新生产 UI token `ui_dm_20260401_175004` 已验证：本地 `wait_dm` 不再是 `socket closed`，节点赛后仍存活；这说明 `processCommandQueue -> GC prepareDealloc` 这条真崩点已经被压住 |
| 2026-04-01 | 但 receiver 活下来后，真实 blocker 又前移回连接复用/identify：用户可见失败重新变成 `dm_fresh_dial_begin 9811ms send_with_ack_timeout`；本地节点日志同时出现 `incoming upgrader identify failed ... MultistreamSelect handshake response1 read timeout` |
| 2026-04-01 | 同一轮赛后本地 `tailnet_status.relayListeners=[]`、`local_info.listenAddrSource=runtime` 再次复现，而节点本身仍存活；说明当前不是节点进程崩溃，而是 listener published 稳态和 inbound identify 之间还有一条未收口的真问题 |
| 2026-04-01 | 已把 `quicrelay.stopRelayListeners()` 收紧成 `move` 摘走 owner task 后再 stop/cancel；关键缩小回归已过，不再把长活 listener/dial task 残留在 stop/repair 阶段 |
| 2026-04-01 | 已把 Android `P2PRuntimeSupervisor.scheduleWarm()` 从只排 `getOrStartWarmAttempt()` 改成直接 `ensureTailnetReady()`；修前 token `ui_dm_codex_20260401_192859` 点击后空耗约 4.3 秒才进入 `cmdSendDirect`，native 剩余预算只剩 `5506ms`，修后 token `ui_dm_codex_20260401_193432` 已成功，`wait_dm` 收到同名 token，instrumentation 为 `OK (1 test)` |
| 2026-04-01 | 同一成功轮赛后虽然仍有 `tailnet_status.relayListeners=[]`、`local_info.listenAddrSource=runtime`，但它已不再阻断 Android -> 本地 Mac DM 主线；当前剩余只是不稳定状态读和继续压回 1 到 3 秒时延 |
| 2026-04-01 | 已修 `nim-msquic api_impl` 的 owned datagram transport 生命周期：`ConnectionState/ListenerState` 现在会一直 `GC_ref` 到 `closeWait()` 真完成，避免 relay listener 路径里 transport 回调还在跑、owner 已被提前释放而炸进 `eventLoop.poll -> readDatagramLoop -> SIGBUS` |
| 2026-04-01 | 带新修补重编 `tsnet_product_node` 后，生产 UI token `ui_dm_codex_20260401_195056` 已验证：本地 `19125` 整轮持续存活，赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners=awaiting`；本地 `wait_dm` 为 `wait_dm_timeout`，用户可见失败重新前移成真实 `connect_exact 9819ms send_with_ack_timeout` |
| 2026-04-01 | 已把 `TsnetRelayTaskHandle` 改回稳定引用形态；`idleWakeLoop -> startUdpRelayListener -> relayStoreTask` 这条在 `relayOwnerTasks.mgetOrPut(...).add(handle)` 上的 `SIGSEGV` 已前推掉 |
| 2026-04-01 | 带新 `TsnetRelayTaskHandle` 修补后，生产 UI token `ui_dm_codex_20260401_195735` 已验证：本地 `19125` 整轮持续存活，赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners=awaiting`，`wait_dm` 为 `wait_dm_timeout`；用户可见失败稳定成 `connect_exact 9811ms send_with_ack_timeout`，当前主线重新收敛到 exact connect，而不是本地节点崩溃 |
| 2026-04-01 | 已把本地产品节点重新锁回纯 Nim `nim_quic` 路径，不再走 Go bridge；当前长会话保活下 `19125` 稳态恢复为 `providerKind=nim-inapp`、`providerReady=true`、`listenAddrSource=relay_listeners`，最新生产 UI token `ui_dm_20260401_204812` 仍是 `connect_exact 9814ms send_with_ack_timeout`，本地 `wait_dm` 为 `wait_dm_timeout`，赛后又回到 `relayListeners=[]`、`listenAddrSource=runtime` 波动 |
| 2026-04-01 | 已修纯 Nim builtin QUIC 新前移出的 parser 真崩点：`protocol_core.parseUnprotectedHeader/decodePacket` 对 long header 的 `dcid/scid` 改成显式安全拷贝，并补了 `protocol_unprotected_header_test.nim` 覆盖零长度 CID；最小协议回归通过 |
| 2026-04-01 | 带新 parser 修补重编 `tsnet_product_node` 并复跑纯 Nim 本地主线后，旧的 `parseUnprotectedHeader -> SIGSEGV` 未再复现；本地 `19125` 整轮持续存活，赛后仍是 `relayListeners=awaiting`、`listenAddrSource=relay_listeners` |
| 2026-04-01 | 同一轮生产 UI token `ui_dm_20260401_205820` 的真实结果重新前移成 `peer_ready_failed 9814ms connect timeout`，本地 `wait_dm` 为 `wait_dm_timeout`；说明当前主线已不再是 builtin QUIC parser 崩溃，而是回到 exact connect 后的 `peer_ready` 真超时 |
| 2026-04-02 | 已把 [proxy.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/proxy.nim) 的 `proxyRouteSnapshots/lookupRawTarget/resolveAdvertisedRemote` 收成“只读 route snapshot”，并把 `registerProxyRoute` 改成 whole-seq replace；`tests/testtsnetproxy.nim` 为 `16 OK` |
| 2026-04-02 | `sanitizeProxyRoutesUnsafe/proxyRouteSnapshots` 这条本地真崩点已前推；最新生产 UI 1:1 实跑里，本地已能走到 `incoming identify done` 和 `/unimaker/dm/1.0.0 readLp begin`，随后才在 `udp listener task start routeId=3` 自修链附近再次崩 |
| 2026-04-02 | 当前最新用户可见失败仍是 Android `send timeout after 10s budget: 发送中 runtime_prepare`，而 logcat 同窗显示 `cmdSendDirectKickoff` 曾排队到 `queued=29`；下一步主线是继续收 listener repair 为什么又起 `routeId=3`，以及 Android kickoff 队列为什么还会把真发送误报成 `runtime_prepare` |
| 2026-04-02 | 已把 `tsnettransport.reconcileProviderListeners` 收成“只在 provider 的 proxy route 注册表真空时，才允许 transport 用自身 `listenerMappings` 重激活 listener”；不再把 `listenerNeedsRepair=true` 的 readiness 粗告警直接扩大成外层全量重建 |
| 2026-04-02 | 对应接口已沿 `proxy.nim -> runtime.nim -> tsnetproviderinapp.nim -> tsnetprovider.nim -> tsnettransport.nim` 打通 `proxyRouteCount`；`tests/testtsnetproxy.nim` 仍是 `16 OK`，`tests/testtsnettransport.nim` 编译通过，`build/tsnet_product_node` 重编成功 |
| 2026-04-02 | 新 `tsnet_product_node` 已能正常拉起，`19125` 控制口可用；当前 seed addr 是 `/ip4/100.64.241.217/udp/64224/quic-v1/tsnet/p2p/12D3KooWFyr4cuZjfaaaZEzLVG6a4whtXaqrbDmPUnd9p8Kr56Fs`，`tailnet_status.proxyListenerExpected=2`、`proxyListenerReady=2` |
| 2026-04-02 | 但 Android 主包这轮覆盖安装尚未真正生效：`adb install -r` 还卡在设备侧确认，`dumpsys package com.unimaker.native.debug` 的 `lastUpdateTime` 仍是 `2026-04-01 22:26:39`；所以这轮还不能诚实给出新的生产 UI 结果 |
| 2026-04-02 | 已把当前 tsnet/relay 主线里两处“按 key 管对象却还用共享 live seq”的注册表改掉：`quicrelay.relayListenerStates` 改成 `OrderedTable[route, state]`，`tcprelay.relayOwnerTasks` 改成 `Table[routeId, handle]`；同时补了“同 route 重复更新 stage 仍只保留一条状态”的回归断言 |
| 2026-04-02 | 这轮结构重构后的静态验证已过：`nim c -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquicrelay.nim` 通过，`build/tsnet_product_node` 重编成功；整套 `testtsnetquicrelay` 长跑这轮未等到结束，因此这次先不宣称运行态全绿 |
| 2026-04-02 | 已把 `relayListenerStates` 再收窄成普通 `Table[route,state]`，并把 `relayListenerStatesPayload()` 改成锁内直接组 JSON；`relayListeners` 状态读这条 `SIGSEGV` 已前推掉，后续日志不再死在 `statusPayload step=relay_listeners_begin` |
| 2026-04-02 | 已把 `tailnetControlRefreshLoop` 从“每轮构整份 `tailnetStatusPayload` 再看 `providerReady`”改成原生 `providerReadySafe()` cheap read；这条后台 refresh 自己把本地节点打死的崩溃链已经前推掉 |
| 2026-04-02 | 已把 `quicrelay.TsnetRelayTaskHandle` 改回稳定引用形态；最新生产 UI 实跑里，本地 `19125` 在完整发送窗口内持续存活，不再复现 `udp listener task start routeId=3 -> SIGSEGV` 的本地节点自爆 |
| 2026-04-02 | 当前主线已从本地 Nim 节点崩溃前移到 Android 进程自身 crash：生产 UI token `ui_dm_20260402_051656` 里，本地 `wait_dm` 为 `wait_dm_timeout`，而 `adb logcat -b crash` 已坐实 `com.unimaker.native.debug` 报 `SIGBUS`，栈在 `ReferenceQueueDaemon -> libicu_jni.so MatcherNative_free`；同窗还能看到 Huawei 输入法自己的 Room migration 异常 |
| 2026-04-02 | 已把 Android `bridge_social_sync` 收紧成发送窗口内不再落 `content_items`，并在发送退出后补一次同步；主包重装后，`com.unimaker.native.debug` 这轮不再复现先前 `ContentDao_Impl$5.call -> SQLite/libsqlite.so` 的主进程 `SIGSEGV` |
| 2026-04-02 | 已把 `ConversationRouteSendTest.kt` 的诊断捕获改成优先看顶部诊断 `contentDescription`，不再只靠 `By.res(targetPackage, "conversation_diagnostics")`；最新 token `ui_dm_20260402_080949` 已拿到这次点击对应的真实失败文案，而不是“result not shown” |
| 2026-04-02 | 已把 `quicrelay.ownedText()` 和 `relayListenerStage()` 收成 nil-safe；本地 `19125` 不再崩在 `relayListenerStateSnapshots -> ownedText`，重启后 `tailnet_status/local_info` 再次恢复 `providerReady=true`、`listenAddrSource=relay_listeners` |
| 2026-04-02 | 最新生产 UI token `ui_dm_20260402_080949` 的真实失败已经前移成 `queued 7679ms send_with_ack_timeout`；Android logcat 同窗坐实 native 节点队列在发送窗口里已堆到 `queued=46`，而失败后后台还继续塞入 `cmdNetworkDiscoverySnapshot/cmdConnectedPeers/cmdConnectedPeersInfo/cmdDiagnostics`，当前主线回到 Android/native 命令队列让路不足 |
| 2026-04-02 | 已把 `quicrelay` 里 `relayOwnerActiveRoutes/relayListenerStates` 继续收成更稳的结构；最新本地 `tsnet_product_node` 在 `incoming -> ready -> bridge_attached` 之后不再复现旧的 `listenerNeedsRepair=true -> stopRelayListenersAsync/rawGet/SIGSEGV`，赛后 `tailnet_status` 仍是 `providerReady=true`、`proxyListenersReady=true`、`relayListeners[0].stage=awaiting` |
| 2026-04-02 | 已把 Android `core-bridge` 发送链里会跑到的 `Regex` 清掉：`ChengLibp2pNative.kt` errno 提取、`BridgeFacades.kt` multiaddr/LAN 判定、hostname token、transcript 规范化全部改成手写解析；主包重装后，生产 UI token `ui_dm_20260402_132721` 不再是 `Process crashed`，`adb logcat -b crash` 里也不再出现 `com.unimaker.native.debug` 的 `RegexMatcher/MatcherNative_free` |
| 2026-04-02 | 当前主线已重新回到真实 DM 失败：生产 UI token `ui_dm_20260402_132721` 为 `send failed: libp2p RELAY · tailnet RELAY · 发送失败 connect_exact 5817ms send_with_ack_timeout`，本地 `wait_dm` 为 `wait_dm_timeout`，说明现在该继续收 exact connect 后半段，而不是再回头收本地节点崩溃或 Android 进程 crash |
| 2026-04-02 | 已把 Android `P2PRuntimeSupervisor.ensureTailnetReady()` 收成可区分“是否必须等本机 published listener”；显式 `/tsnet` seed 发送时只要求 `providerReady + tailnetIPs`，不再把 `proxyListenersReady` 误绑成发送前置 gate |
| 2026-04-02 | 带新 gate 修补的生产 UI token `ui_dm_20260402_142026` 已验证：旧的 `runtime_prepare 9883ms tailnet_not_ready` 被前推掉；再复跑 token `ui_dm_20260402_143146` 后，真实失败重新收敛成 `connect_exact 9359ms send_with_ack_timeout`，本地 `wait_dm` 仍是 `wait_dm_timeout`，且赛后本地 `19125` 继续保持 `relayListeners[0].stage=awaiting` |
| 2026-04-02 | 本地 `19125` 这轮又抓到一条更底层的真问题：`tailnet_status/local_info` 不是连不上，而是主线程在 `statusPayload -> proxyListenerReadiness -> relayRouteLocallyReady` 卡住，`sample` 已坐实另一条事件循环线程同时在 `listenerNeedsRepair -> relayListenerStage` 占着 `relayRegistryLock`；这说明诊断 `relayListenerStates` 仍然进了热路径 |
| 2026-04-02 | 已把 `quicrelay.relayRouteLocallyReady()` 收回成只认 `relayReadyRoutes`，并把 `runtime.listenerNeedsRepair()` 收成只认“真正在跑的 route task”；诊断 stage 不再参与 readiness/repair 判定 |
| 2026-04-02 | 已把 `startRelayListener/startUdpRelayListener` 收紧成：只有真正在跑的 task 才允许 `reason=active_task` 跳过重建；旧的僵尸 active 标记现在会先清掉再重建 listener |
| 2026-04-02 | 带这轮新修补重编 `tsnet_product_node` 后，本地 `19125` 已重新恢复：`tailnet_status` 秒回，`proxyListenersReady=true`、`relayListeners[0].stage=awaiting`，`local_info.listenAddrSource=relay_listeners`；这条“RPC 接得住但 2 秒内回不出来”的本地阻塞链已前推掉 |
| 2026-04-02 | 重新复跑上一轮旧 Android 主包时，真实失败仍是 `connect_exact 9219ms send_with_ack_timeout`；说明本地 listener repair/RPC 阻塞虽然已收掉，但 DM 主线还没通，后面要继续用带新 native 的主包复跑 |
| 2026-04-02 | Android 会话输入条这轮又坐实了一条真问题：`ConversationInputBar` 在 `AndroidView(EditText)` 创建和每次 update 时都直接 `requestFocus + showSoftInput`，即使只是 deep link 预填 draft 也会把 Huawei 输入法拉起来；已改成只有用户真点击占位输入框时才做一次性聚焦，最新主包 `lastUpdateTime=2026-04-02 16:01:07` 安装后，生产 UI 不再报 `Process crashed`，重新回到真实发送失败 |
| 2026-04-02 | 本地 `19125` 发送窗口后又抓到另一条结构性热路径问题：`sample` 已坐实主线程卡在 `statusPayload -> proxyListenerReadiness -> relayRouteReady` 等 `relayRegistryLock`，事件循环线程同时卡在 `startUdpRelayListener -> clearRelayRouteTaskActive -> del`；根因是 `relayOwnerActiveRoutes/relayOwnerRouteTaskIds` 这两张 keyed 表还在用 `OrderedTable`，删键会在全局锁里做重活 |
| 2026-04-02 | 已把 `relayOwnerActiveRoutes/relayOwnerRouteTaskIds` 从 `OrderedTable` 收成普通 `Table`，并重编 `tsnet_product_node`；新节点赛后 `tailnet_status/local_info` 再次恢复秒回，`proxyListenersReady=true`、`relayListeners[0].stage=awaiting`、`listenAddrSource=relay_listeners`，这条删键卡锁链已前推掉 |
| 2026-04-02 | 本地 receiver 再往前收后，又坐实一条 `libnimlibp2p.processCommandQueue -> asyncSpawn -> GC prepareDealloc` 真崩点；日志是在 `udp listener incoming -> bridge_attached` 之后、`incoming identify begin` 同窗触发，`wait_dm` 变成 `socket closed while reading` |
| 2026-04-02 | 已把 `processCommandQueue()` 的 drain future 创建改成局部强引用 `let drainTask = drainCommandQueue(n); n.commandDrainTask = drainTask; asyncSpawn(drainTask)`，不再直接 `asyncSpawn(n.commandDrainTask)`；重编本地节点后，最新生产 UI token `ui_dm_20260402_161037` 已验证：本地 `wait_dm` 为 `wait_dm_timeout`、赛后 `tailnet_status` 仍秒回，真实失败重新收敛成 `peer_ready_failed 9139ms connect timeout` |
| 2026-04-02 | 已把 `runtime.reconcileProxyListeners()` 改成“只按 runtime 自己的 listenerRoutes 增量补建缺失 route”，不再拿 `proxyRouteSnapshots()` 当 expected 全集，也不再整批 `stopRelayListenersAsync()`；同时把 `quicrelay.relayOwnerEpoch` 初始值收成正数，修掉 `ownerEpoch=0` 让缺失 route 假装 active 的误判 |
| 2026-04-02 | 新增定向回归 `runtime reconcile keeps active listener when proxy snapshot shrinks` 已通过，`tests/testtsnetquicrelay` 为 `1 OK`，`build/tsnet_product_node` 重编成功；本地新节点重启后当前真实 seed 是 `/ip4/100.64.241.217/udp/59949/quic-v1/tsnet/p2p/12D3KooWFyr4cuZjfaaaZEzLVG6a4whtXaqrbDmPUnd9p8Kr56Fs`，`tailnet_status.proxyListenersReady=true`、两条 relay listener 都在 `awaiting` |
| 2026-04-02 | 带新 native 的 Android 主包已覆盖安装成功，`lastUpdateTime=2026-04-02 16:56:37`；但这轮生产 UI 冷启动 deep link 还没走到 DM 发送，`wait_dm` 为 `wait_dm_timeout`，instrumentation 先报 `Process crashed` |
| 2026-04-02 | `adb logcat -b crash` 已坐实这轮新的 blocker 是 `com.unimaker.native.debug` 主进程 `SIGSEGV`，崩栈在 `libart` / `androidx.compose.runtime` 启动路径，同窗能看到 `UniMakerThemeKt`；Huawei 输入法自己的 Room downgrade 异常仍然共存，但这轮主因已经不是输入法单独 crash，而是主包冷启动先死 |
| 2026-04-02 | 已把 Android 冷启动启动链收成“首帧后单路 bootstrap”：`UniMakerApplication` 和 `MainActivity` 不再抢跑 `scheduleWarm()`，`NativeAppViewModel.init` 也不再直接拉 native；新的 warm 只在首帧后触发一次。带这版主包复跑后，冷启动 `Process crashed` 已被前推掉，生产 UI 先回到了真实 `dm_fresh_dial_begin 9184ms send_with_ack_timeout` |
| 2026-04-02 | 本地 listener repair 又坐实一条真崩点：`runtime.reconcileProxyListeners()` 仍会回查全局 `proxyRouteSnapshots()`，真流量下再次炸进 `proxyRouteSnapshots -> ownerId`；已改成只用 runtime 自己持有的 `tcp/udpListenerRouteTargets` 做 `advertised -> raw` repair，不再读全局 proxy 快照，定向回归 `runtime reconcile keeps active listener when proxy snapshot shrinks` 继续 `1 OK` |
| 2026-04-02 | 继续收 repair 时又前推掉一条 `MultiAddress` 生命周期问题：repair 临时队列不再存 `TsnetProxyRouteSnapshot`，改成只存 route 字符串，真正调用 `startUdpRelayListener/startRelayListener` 前再解析成 `MultiAddress`；这样本地不再炸在 `rawSocketFromAddress -> familyFromAddress -> splitAddressParts` |
| 2026-04-02 | 最新生产 UI token `ui_dm_20260402_173526` 已重新打通：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名 DM，内容为 `ui_dm_20260402_173526`，说明 Android -> 本地 Mac 这条产品级发送主线已恢复 |
| 2026-04-02 | 但成功后本地节点赛后还会继续崩在 `libnimlibp2p.processCommandQueue -> decRef`；也就是说，DM 主线已恢复，但本地 command drain 生命周期还没完全稳住，这条已成为下一轮唯一剩余真问题 |
| 2026-04-02 | 继续复跑带长活 drain task 的本地主线后，生产 UI token `ui_dm_20260402_175530` 没再先暴露赛后 `decRef`，而是更早前移成 `connect_exact 1128ms send_with_ack_timeout`；Android logcat 已坐实这不是 native connect 只慢 1 秒，而是 UI 层在显式 `/tsnet` seed 路径上先空耗了约 9 秒，导致真正进 `cmdSendDirect` 时 `remainingBudgetMs=1107` |
| 2026-04-02 | 已把 Android [NativeAppViewModel.sendConversationText](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 收紧成：显式 `/tsnet` seed 不再先跑 `ensureTailnetReady()`，而是直接把剩余预算交给 `bridge.sendDirectText()`；新主包已重编并成功 `adb install -r` |
| 2026-04-02 | 同一轮继续收本地节点时，又抓到 repair 发布路径的新真崩点：listener repair 重入后，`publishTsnetLocalPeerInfo -> safeMultiaddrText -> MultiAddress.$ -> decRef` 再次炸在 live `peerInfo.listenAddrs`；已把发布路径改成优先只读 `peerInfoListenAddrTexts` 快照，`tsnet_product_node` 已重新编过 |
| 2026-04-02 | 已把 `relayOwnerHasRunningRouteTask()` 从“锁里读 live `Future.finished()/state`”改成只认 owner 私有的 `active route + routeId + task record`，并在 listener task 正常退出时同步摘掉 `routeId/active/task handle`；定向回归 `relayOwnerHasRunningRouteTask stays readable after listener stage rewrites` 和 `listener stop clears stored route task record` 都已通过，`build/tsnet_product_node` 与 Android 主包也已重编成功 |
| 2026-04-02 | 带这轮 `relayFinishRouteTask` 修补后的最新生产 UI token `ui_dm_20260402_182345` 已重新打通：instrumentation 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 `tailnet_status` 仍是 `providerReady=true`、`proxyListenersReady=true`、`relayListeners[0].stage=awaiting`，`local_info.listenAddrSource=relay_listeners`，说明 `relayOwnerHasRunningRouteTask -> Future.finished()/state -> SIGSEGV` 这条本地真崩点已经被前推掉 |
| 2026-04-02 | 已把 receiver 侧 authoritative DM ack 从 `handleDirectMessageNode()` 的重尾处理里摘出来：`recordReceivedDm` 之后先让 `dmservice` 发送 LP ack，再异步做 social state、conversation append、notification 和 event 记录；`tests/testdirectdm.nim` 与 `build/tsnet_product_node` 已重编通过 |
| 2026-04-02 | Android instrumentation 已收紧成“只认 diagnostics 里的 send status 子串变化”，不再因为 `libp2p/tailnet/latency/updated` 变化把旧的 `发送失败 connect_exact ...` 误判成这次点击的新结果；新的 `ConversationRouteSendTest.kt` 已重编并安装成功 |
| 2026-04-02 | 已把 `quicrelay` 的 unpublished preserve 语义收紧：`published=false` 时，只有 `incoming/ready/bridge_attached` 才允许保留当前 serving route，idle `awaiting` route 必须掉回 repair/re-register，不再无限“假活着” |
| 2026-04-02 | 带上上面三条修补后，本地固定 seed `/ip4/100.64.160.30/udp/40125/quic-v1/tsnet/p2p/12D3KooWHGbPE7DjnvbGHELDyYyPofexgyeGRsnCuo7UDdiq1d8x` 连续两轮生产 UI 1:1 复跑都成功：token `ui_dm_writefix_ack_9_20260402_203335` 与 `ui_dm_writefix_ack_10_20260402_203430` 均为 instrumentation `OK (1 test)`、本地 `wait_dm` 收到同名消息，Android logcat 已闭环到 `stage=dm_ack_done` / `cmdSendDirect success` / `sendThreadMessage success` |
| 2026-04-02 | 两轮成功后的赛后状态仍稳定：`tailnet_status.proxyListenersReady=true`、`relayListeners[0].stage=awaiting`，`local_info.listenAddrSource=relay_listeners`；当前 Android -> 本地 Mac 产品级 DM 主线已连续打通，剩余问题只剩把当前约 5s 成功路径压回 1 到 3 秒 |
| 2026-04-02 | 继续做“成功后一轮立刻再发”稳定性时，又抓到 repair 新崩点：`runtime.reconcileProxyListeners()` 虽然不再炸在 `rawSocketFromAddress`，但正在收流量的 persistent accept stream 仍会被误判为 `ready=0 expected=1`，从而强行再起第二个 UDP listener；旧崩栈已前移成 `closeQuicRelayClient -> closeOwnedDatagramTransport -> decRef` |
| 2026-04-02 | 已把 UDP listener repair 入口彻底改成纯字符串版，并新增 `relayRouteServingStage(owner, route)`；`listenerNeedsRepair()` 与 `reconcileProxyListeners()` 现在都会对 `incoming/ready/bridge_attached` 直接停手，`udp listener incoming_payload_wait_begin` 也会先标成 `incoming`，避免在真实收消息窗口里误 repair |
| 2026-04-02 | 继续收 close 生命周期后，又给 `safeCloseConnection()` 加了 `connState` 显式根引用，避免 `closeConnection()` 期间把 relay client 的 live connection state 提前回收 |
| 2026-04-02 | 带上上面三条新修后，最新两连发 token `ui_dm_closefix_21_20260402_210345` / `ui_dm_closefix_22_20260402_210357` 已证明本地节点不再在 repair/close 路径上掉线：第 21 轮 instrumentation `OK` 且 `wait_dm` 收到，第 22 轮虽然 instrumentation 报 `dm_ack_wait 9786ms send_with_ack_timeout`，但 `wait_dm` 同样收到消息，赛后 `tailnet_status` 仍秒回，`relayListeners[0].stage=incoming`、`local_info.listenAddrSource=relay_listeners` |
| 2026-04-02 | 已撤掉 `dmservice` 里“ACK 写完后 sleep 150ms 再 close”的时间窗补丁；authoritative ACK 现在改成独立 `op=ack` direct message：sender 在 `DirectMessageService` 内注册 waiter，receiver 在消息 handler 成功/失败后另开一条 DM stream 发 ACK，sender 不再读原消息 stream 的同流 LP ack |
| 2026-04-02 | 新 ACK 方案下，`build/tsnet_product_node` 已重编成功；`tests/testdirectdm.nim` 的第一条 `pubsub delivers direct dm with ack` 已跑通，并且日志已出现独立 `op=ack` 入站；整套 `testdirectdm` 仍被旧的 `lpstream.closeWithEOF -> bufferstream not s.reading` 老问题挡住，这条失败已不再是 ACK 方案本身 |
| 2026-04-02 | authoritative ACK 这轮已进一步收成生产级长活双向 session：`dmservice` 现在对同 peer 维护持久 `DirectMessageSession`，`dm/ack` 都在同一 session 内 LP 帧化；同时补了 `DirectMessageService.stop()`，先取消 reader/关 session 再让外层停服，前推掉了 `lpstream.closeWithEOF -> bufferstream not s.reading`。`tests/testdirectdm.nim` 最新为 `3 OK`，新增的 `activeSessionCountForPeer == 1` 断言也通过，`build/tsnet_product_node` 与 Android `:app:assembleDebug` 都已重编成功 |
| 2026-04-03 | 带长活双向 session ACK 的新主包重新覆盖安装后，生产 UI 1:1 路径连续两轮 `ui_dm_sessionack_20260403_062425` / `ui_dm_sessionack_20260403_062530` 都为 instrumentation `OK (1 test)`；Android logcat 两轮都闭环到 `dm_ack_done` / `cmdSendDirect success` / `sendThreadMessage success`，本地 `social_state.json` 也已落入两条 `status=received` 的同名消息 |
| 2026-04-03 | 同上两轮赛后，本地 `19125` 节点持续存活，`tailnet_status` 继续 `proxyListenersReady=true`、`relayListeners[*].stage=incoming`，`local_info.listenAddrSource=relay_listeners` 继续秒回；说明当前主线已不再是 ACK 或赛后节点自杀，而是只剩继续做稳定性长跑和把当前约 `4.3s` 成功路径压回 `1-3s` |
| 2026-04-03 | 已把 `quicrelay` listener 所有权收成 generation token：同一路 route 每次 `listen` 都拿新 generation，后续 `accept/await/ready` 必须带上这代 token，旧控制流只能被判成 stale，不能再靠 `listener_busy` 猜测替换 |
| 2026-04-03 | 上面这条 generation 已坐实为 wire-level relay 协议变更，不是本地私有修补：本地新 `tsnet_product_node` 直接对旧远端 gateway 复跑时，日志稳定是 `udp listener registration missing generation`；已用 `/Users/lbcheng/nim-libp2p/build/tsnet_remote_admin --ssh-host 64.176.84.12 --ssh-user root --op deploy-gateway` 把远端 `nim-tsnet-gateway.service` 同步、重编并重启到新版本 |
| 2026-04-03 | 已把 `dmservice` 正式收成显式 DM session 状态机：新增 `DirectMessageSessionLifecycle`、活动计数 `ReaderAttached/HandlingPayload/WritingFrame/AwaitingAck`、以及 `sessionControlSnapshots()`；`dm/ack` 收发、ACK 等待、reader attach、流关闭现在都走明确迁移，不再靠隐式布尔和日志猜状态 |
| 2026-04-03 | 新增定向回归 [testdmsessionstate.nim](/Users/lbcheng/nim-libp2p/tests/testdmsessionstate.nim) 并已通过：`acked exchange settles to live reader-only session` 为 `1 OK`；同时 Android 主包已重编并 `adb install -r` 成功 |
| 2026-04-03 | 已把 `quicrelay` 的长活 listener/dial task 收成“任务边界一次解析稳定 `TsnetQuicRelayEndpoint`，循环内不再重 parse raw `relayUrl`”；同时把 `proxyRouteCount()` 收成 owner 计数表读取，不再在 idle reconcile 里扫 live `proxyRegistrationSnapshots`。`tests/testtsnetquicrelay.nim`、`tests/testtsnetproxy.nim` 与 `build/tsnet_product_node` 均已重编通过 |
| 2026-04-03 | 带上上面两条修补后，生产 UI token `ui_dm_state_machine_20260403_080113` 和 `ui_dm_state_machine_20260403_080324` 都再次闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名 DM，Android logcat 已闭环到 `dm_ack_done` / `cmdSendDirect success` / `sendThreadMessage success` |
| 2026-04-03 | 赛后发布路径又前移出两条真崩点并都已收掉：先把 `mergeTsnetListenAddrsIntoPeerInfo/refreshLanAwareAddrs` 改成 tsnet-only 下全程“字符串快照优先，`MultiAddress` 只从文本反解析”，前推掉 `safeMultiaddrText -> MultiAddress.$`；再把 `markRelayListenerStage*` 对冗余 `relayListenerStates` 写表彻底拿掉，前推掉 `rawInsert/decRef` |
| 2026-04-03 | 带上上面两条新修后，生产 UI token `ui_dm_state_machine_20260403_081531` 再次闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名 DM，Android logcat 继续闭环到 `dm_ack_done` / `cmdSendDirect success` / `sendThreadMessage success`，而且赛后 `tailnet_status/local_info` 继续秒回，`proxyListenersReady=true`、`listenAddrSource=relay_listeners`；这条“消息能到但本地赛后自杀”的尾部崩链已前推掉 |
| 2026-04-03 | 已先关掉 native QUIC 上两条错误的 eager identify：`dialer.internalConnect` 不再对 `MsQuicConnection` 异步跑 identify，`ensurePeerIdentified` 也改成“secure live muxer 即视为 peer 已验证”，不再在发送热路径里再抢一条 `/ipfs/id/1.0.0` stream；复跑后 Android 日志已只剩 `identify skipped`，旧的 `peerStore.identify multistream select begin` 不再出现 |
| 2026-04-03 | 当前真正收口主线的是 `cmdSendDirect` 显式 seed 路径去掉 `peer_ready_deferred_to_dm_dial`：同一请求现在会先 `ensurePeerReadyViaExplicitAddresses`，拿到 `verifiedMuxer` 后直接走 `verified_live_muxer`；生产 UI token `ui_dm_exact_reuse_20260403_094328` 已成功，instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已闭环到 `path=verified_live_muxer -> dm_conn_live_select_ready -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success` |
| 2026-04-03 | 已把 tsnet listener `repair` 从“`ready < expected` 推测触发”收回成“route task 退出后显式请求 repair”的确定性状态机：`quicrelay` 新增 route 级 repair request，`runtime.listenerNeedsRepair/reconcileProxyListeners` 只消费这张表；复跑 token `ui_dm_repair_state_machine_20260403_100210` 后，本地节点不再在 `readLp begin` 窗口误 repair 自杀，`tailnet_status/local_info` 赛后继续秒回，真实失败重新前移成 Android `connect_exact 9788ms send_with_ack_timeout`，本地 `wait_dm` 为 `wait_dm_timeout` |
| 2026-04-03 | 继续收 `wait_send_ack` 现场时，新的本地真崩点已坐实在 [proxy.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/proxy.nim) `registerResolvedRemote -> refreshResolvedRemoteSnapshotsUnsafe -> validResolvedRemoteRegistration -> MultiAddress.init`；崩栈发生在 receiver 已进入 `/unimaker/dm/1.0.0` 之后，直接把 `19125` 打成 `socket closed while reading` |
| 2026-04-03 | 已把 `resolved remote` 这支的热路径彻底收回固定 `sharedKey`：`validResolvedRemoteRegistration`、snapshot 刷新、sanitize、lookup 都不再碰 `rawText/advertisedText` 做有效性判定或查找；新增回归 `resolved remote lookup ignores detached gc text fields` 后，`tests/testtsnetproxy.nim` 为 `18 OK`，本地 `tsnet_product_node` 用新二进制复跑后，这条 `registerResolvedRemote` 崩栈已不再复现 |
| 2026-04-03 | 带 `proxy` 新修复再跑生产 UI token `ui_dm_proxyfix_20260403_121948` 后，本地节点赛后继续存活，`tailnet_status` 可读到 `relayListeners[0].stage=incoming`、`local_info.listenAddrSource=relay_listeners`，说明本地 receiver 自杀链已被前推掉；当前新的唯一 blocker 变成 Android 主进程在 `verified_live_muxer` 发送窗口内 `ReferenceQueueDaemon -> libicu_jni MatcherNative_free -> SIGSEGV`，instrumentation 结果是 `Process crashed`，本地 `wait_dm` 为 `wait_dm_timeout` |
| 2026-04-03 | 已把 Android 显式 `/tsnet` 真实发送前的 `session_warm_join` 拿掉：发送前只 cancel 同 peer warm，不再把 warm session 当发送前置；这轮复跑把旧的 UI 前置干扰前推掉，receiver 真正重新暴露出本地 relay/transport 崩点 |
| 2026-04-03 | 已修 `quicrelay` 两条 receiver 生命周期真 bug：`finishQuicStream()` 不再尾部二次 `await` 多余的 async close；`udp listener bridge task` 现在由 `TsnetUdpRelayBridge.bridgeTask` 稳定持有到任务结束。旧的 `finishQuicStream` 崩溃和 future continuation 空指针都已前推掉 |
| 2026-04-03 | 已修 `tsnettransport.lookupAdvertisedListenerForRaw()`：listener mapping 改写地址时只认 `rawText/advertisedText` 稳定字符串，不再对 live `MultiAddress` 做 `$`；旧的 `lookupAdvertisedListenerForRaw -> MultiAddress.$ -> SIGSEGV` 已前推掉 |
| 2026-04-03 | 最新生产 UI token `ui_dm_listener_map_fix_20260403_163339` 已重新闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息；本地日志已走到 `direct message readLp done -> parse ok -> handler done -> direct message ack sent`，赛后 `tailnet_status/local_info` 继续秒回，当前主线重新稳定打通 |
| 2026-04-03 | 已把 `newStandardSwitchBuilder(transport=QUIC)` 收成理论正确的 QUIC-only 语义：不再给 builtin QUIC 挂假 `mplex`，也不再保留隐式 secure default；这条路径现在明确是 `MsQuicTransport -> MsQuicMuxer`，定向回归 `tests/test_msquic_builder.nim` 已验证 `upgrader.secureManagers.len == 0`、`MuxedUpgrade.muxers.len == 0`，`build/tsnet_product_node` 也重编通过 |
| 2026-04-03 | 已把显式 `/tsnet` seed 发送的 `peer_ready` 真相从“拿到 `verifiedMuxer`”收成“本次已建好的 DM session”：`warmSeededDirectSessionAsync` 先 fresh warm 出会话，再把 `sessionConnKey` 从 `bootstrap task` 贯穿到 `cmdSendDirect -> dmService.send(preferredConnKey)`，sender 不再对第一条业务流重做一次 live protocol select |
| 2026-04-03 | 带上上面这条修正后，生产 UI token `ui_dm_session_connkey_20260403_205629` 已再次闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已坐实 `path=warmed_session_conn -> dm_session_reuse -> dm_write_done -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success`；本地节点赛后仍可回 `tailnet_status/local_info` |
| 2026-04-03 | 已把 Android truth route 准备从 Compose `LaunchedEffect` 收回到 [MainActivity.onCreate/onNewIntent](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt)，不再让 `prepareTruthScene()` 受 UI 重组时机拖后；同时把 direct session warm attempt 的复用边界收成“同 peer + 同 seed key + 同活 job”才允许复用，旧 attempt 不再静默吞掉新 truth route |
| 2026-04-03 | 带上上面两条 Android 修正后，最新生产 UI token `ui_dm_warm_key2_20260403_213119` 已再次闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，sender 继续命中 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done`；当前成功路径约 `7153ms`，瓶颈仍主要在 `bootstrapSeedConnectExactAsync` 约 `4.3s` 的 connect/session warm，赛后 `tailnet_status/local_info` 仍会短暂退成 `relayListeners=[]` / `listenAddrSource=synthesized` |
| 2026-04-03 | 已把 `cmdSendDirect` 的 kickoff 终态写回收正：内层一旦拿到确定失败，就立即把 requestKey 对应的 kickoff state 写成 `done`，不再让真实 `connect_exact_failed ... live connection protocol select timeout` 被外层晚到一步包成假 `send_with_ack_timeout` |
| 2026-04-03 | 已把显式 `/tsnet` seed 的 bootstrap 再往前收成理论正确的一步：不再先 generic `connectMultiaddrExactAsync`，再补一层 `warmSession`；现在 `runBootstrapSeedConnectExactAsync` 直接用 `dmService.warmSessionDetailed(preferredAddrs=[addr], reuseVerifiedLiveConnection=false)` 产出 `sessionConnKey`，把“精确地址”和“DM session”合成一个权威步骤 |
| 2026-04-03 | 带上上面这条修正后，最新生产 UI token `ui_dm_session_exact_20260403_220040` 已再次闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已闭环到 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success`，用户可见发送耗时约 `6533ms` |
| 2026-04-03 | 这轮成功后本地节点仍存活，但赛后 `tailnet_status` 又退成 `relayListeners=[]`、`proxyListenersReady=false`，`local_info.listenAddrSource=synthesized`；说明 `connect_exact_failed` 已不再是主线，当前新剩余问题变成 success 之后的 listener published 稳态漂移 |
| 2026-04-03 | 已把 `bootstrapSeedConnectTasks` 从“按 `peerId` 单键”收成“按 `peerId + exact selectedAddrs` 键控”，并把 sender 判定 bootstrap 是否可复用的真相从 `future.finished()` 改成 live `sessionConnKey`；旧的 `joinedBootstrapTask=true` 但 `bootstrapTaskCompleted=false/sessionConnKey=0` 导致的假 `bootstrap direct session not warmed` 已前推掉 |
| 2026-04-03 | 带上上面这条修正后，生产 UI token `ui_dm_bootstrap_key_20260403_230320` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；Android logcat 已坐实 `warmSeededDirectSessionAsync warmOk=true connKey=532708546104 -> cmdSendDirect joined bootstrap establishment -> path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=6464` |
| 2026-04-03 | 最新赛后状态没有再完全掉回空集：`local_info.listenAddrSource=relay_listeners` 且保住了 `/ip4/100.64.148.187/udp/64275/...`；但 `tailnet_status` 仍前移成 `proxyListenersReady=false`、`relayListeners` 只剩单条 `/ip4 ... stage=known`，说明剩余问题已缩成 published listener 稳态投影/ready 计数偏移，不再是 bootstrap 归属语义或发送主线 |
| 2026-04-03 | 已把 `runtime.statusPayload()` 收成单次 relay 快照投影：`proxyListenerReady/proxyListenersReady/relayListeners` 现在都从同一份 `relayListenerStateSnapshots(runtimeId, routes)` 计算，不再先算一次 ready、再读一次 listeners 导致同一份 `tailnet_status` 自相矛盾 |
| 2026-04-03 | 带上上面这条修正重编本地节点后，fresh 节点起步状态已重新自洽：`tailnet_status` 能稳定给出 `proxyListenerReady=2`、`proxyListenersReady=true` 和双栈 `relayListeners[*].stage=incoming`，`local_info.listenAddrSource=relay_listeners` 也与之对齐 |
| 2026-04-03 | 继续用同一主包复跑时，发送主线没有继续前推，重新落回 Android 侧真实 `connect_exact 9790ms send_with_ack_timeout`；logcat 已坐实这次 `bootstrapSeedConnectExactAsync` 只拿到 `secureTimeoutMs=1901`，最后以内层真错 `bootstrap direct session not warmed` 结束。当前新主线已回到 Android bootstrap/session warm 预算分配 |
| 2026-04-04 | 已把 qrelay/runtime 的 published listener 投影收成两层确定性真相：qrelay 记录每条 route 的最后确定 stage，runtime 再持有自己的 `relayListenerProjectionCache`，没有显式 `dropped` 或 clear routes 就不允许因为瞬时读空把 published route 从状态里抹掉。`tests/testtsnetquicrelay.nim` 与 `build/tsnet_product_node` 已重编通过。 |
| 2026-04-04 | 带上上面这条修正后，本地 fresh `19125` 节点初始与赛后失败后都能稳定保持 `tailnet_status.proxyListenersReady=true`、双栈 `relayListeners` 非空，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续可读；这条 success 之后 published listener 掉空主线已被前推掉。 |
| 2026-04-04 | 继续复跑生产 UI token `ui_dm_projection_cache_20260404_003121` 时，新的唯一 blocker 已切回 Android sender bootstrap：本地 `wait_dm_timeout`，instrumentation 报 `connect_exact_failed 9800ms bootstrap direct session not warmed`；logcat 已坐实 `joinedBootstrapTask=true` 但 `bootstrapTaskCompleted=false/sessionWarmOk=false/sessionConnKey=0`。当前主线已从 published 投影漂移切回 direct session warm。 |
| 2026-04-04 | 已把 bootstrap exact-connect task 收成显式意图状态机：后台 warm 与前台发送分意图持有，前台发送不再继承后台 warm 的未完成 task；bootstrap 是否可复用也改成只认 live `sessionConnKey`，不再拿 `future.finished()` 当真相。`tests/testdirectdm.nim` 与 `build/tsnet_product_node` 已重编通过，Android 主包已重打并覆盖安装成功。 |
| 2026-04-04 | 带上上面这条修正后，生产 UI token `ui_dm_bootstrap_intent_20260404_052034` 已重新闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已坐实 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=4254`。 |
| 2026-04-04 | 同轮赛后本地 `19125` 继续稳定：`tailnet_status.proxyListenersReady=true`、双栈 `relayListeners` 保持 `serving/ready`，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续可读；当前主线已不再是 bootstrap task 归属或 published listener 投影，只剩继续把约 `4.25s` 的成功路径压回 `1-3s`。 |
| 2026-04-04 | 已把 Android truth route 的 direct-session warm 从“仅在 `draftText` 为空时才做”收正成“只要有 seedAddrs 就做”；`draftText` 只是预填消息，不能再屏蔽 session warm。带新主包复跑 token `ui_dm_draft_warm_20260404_052511` 仍为 instrumentation `OK (1 test)` 且本地 `wait_dm` 收到同名消息，主线未回退。 |
| 2026-04-04 | 已把 receiver social state 从“每次后处理都整份 `parseJson(social_state.json)`”收成“内存已解析快照 + clone-on-read + 原子发布”；`loadSocialStateUnsafe` 现在优先读 `socialStateParsedSnapshot`，`persistSocialStateUnsafe` 同步发布新快照，不再让 `finalizeReceivedDirectMessageAsync` 在赛中炸回 `loadSocialStateUnsafe -> parseJson -> Table.enlarge -> prepareDealloc`。 |
| 2026-04-04 | 带上上面这条修正后，生产 UI token `ui_dm_social_cache_20260404_054113` 已重新闭环成功：instrumentation `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 已闭环到 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=4721`。 |
| 2026-04-04 | 同轮赛后本地 `19125` 继续稳定：`tailnet_status.proxyListenersReady=true`、双栈 `relayListeners[*].stage=ready`，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续可读；这条“ACK 已发出但 receiver 后处理 parse social state 自杀，最终把 sender 拖成 dm_ack_wait”主线已被前推掉。 |
| 2026-04-04 | 已把 Android `sendConversationText()` 发送前那层 `matchingWarmAttempt.await()` 重复 gate 删掉；发送不再阻塞等待一份平行 warm 观察结果，唯一真相改成 native `sessionConnKey/path=warmed_session_conn`。这轮是架构收正，不是调参。 |
| 2026-04-04 | 带上上面这条修正后，生产 UI token `ui_dm_single_truth_20260404_055808` 继续 `OK (1 test)`，本地 `wait_dm` 收到同名消息；Android logcat 已闭环到 `observed_matching_warm -> path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=4227`。 |
| 2026-04-04 | 这轮把一个关键假快前推掉了：上一轮 `1167ms` 其实是“先在 UI 外面等 warm，再开始 send”的局部时间；现在 `4227ms` 才是诚实的 tap-to-sent 端到端时延。主线仍通，下一步只剩继续压真实 bootstrap/session warm 本体。 |
| 2026-04-04 | 已把 `cmdBootstrapSeedConnectExactKickoff` 提升进 priority queue，interactive direct session bootstrap 不再和后台普通命令抢队列；带新主包复跑 token `ui_dm_bootstrap_prio_20260404_060757` / `ui_dm_bootstrap_prio2_20260404_060925` 都为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，诚实时延从 `4227ms` 压到约 `3714ms/3720ms`。 |
| 2026-04-04 | 继续按理论边界把 `warmSessionDetailed()` 收正成“只建立 DM session，不再同步等待 reader fully ready”；receiver 的 ACK reader 仍由真实 send path 在首条写入后启动，不再把 reader attach 误算进 warm 完成条件。 |
| 2026-04-05 | 已把同节点路径统计文案收成稳定输出：即使当前还没有 direct/relay 样本，也会显示 `打洞 0% 0/0 · 直连DM -- · 中继DM --`，不再因为 `attempt=0/latency=-1` 整段消失。 |
| 2026-04-05 | 已自动用 `adb install -r /Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` 覆盖安装新主包；设备 `com.unimaker.native.debug` 的 `lastUpdateTime=2026-04-05 11:24:04`，前台已回到 `MainActivity`。 |
| 2026-04-05 | 生产 UI token `ui_dm_20260405_112419` 这轮仍停在 `send_with_ack_timeout`，但 instrumentation 已直接带回新的会话诊断串：`libp2p RELAY · tailnet DIRECT · 打洞 0% 0/0 · 直连DM -- · 中继DM -- · 发送失败 queued 9792ms send_with_ack_timeout`，说明同节点统计已真实接进生产 UI。 |
| 2026-04-04 | 上面这条 warm 边界收正后，新的真问题不是 sender budget，而是本地 `19125` 节点在 `idleWakeLoop -> sanitizeHostNetworkStatus/defaultHostNetworkStatus` 上再次崩回 `SIGSEGV`；`wait_dm` 直接 `socket closed while reading`，instrumentation 退回 `connect_exact 9785ms send_with_ack_timeout`。这次已坐实不是 warm 语义，而是运行态 `hostNetworkStatus` 用 `JsonNode` 现拼 JSON 的结构性错误。 |
| 2026-04-04 | 已把 `hostNetworkStatus/deferredHostNetworkStatus` 从运行态 `JsonNode` 改成值对象 `HostNetworkStatusSnapshot`；idle/public-ip-probe/side-effects 热路径现在只读结构体，只有 `currentHostNetworkStatus()` 和事件出参时才物化 JSON，不再在 `idleWakeLoop` 里反复 `%*` 构建对象。`tests/testdirectdm.nim` 与 `build/tsnet_product_node` 已重编通过。 |
| 2026-04-04 | 带上 `HostNetworkStatusSnapshot` 的新主包和新本地节点复跑后，生产 UI token `ui_dm_host_snapshot_20260404_062321` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；Android logcat 已闭环到 `path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=4123`。 |
| 2026-04-04 | 这轮赛后本地 `19125` 节点继续存活，`tailnet_status.proxyListenersReady=true`、`relayListeners` 保持 `incoming/ready`，`local_info.listenAddrSource=relay_listeners`；说明 `hostNetworkStatus` 这条“热路径现拼 JSON 把本地节点拖死”主线已被前推掉。 |
| 2026-04-04 | fresh 本地 `19125` 节点随后又前移暴露出新的真崩点：`noderesourceservice.collectMacResourceInfo -> safeExec -> osproc.execProcess -> envToCStringArray` 在 Mac 资源采集热路径里直接 `SIGSEGV`；这次已坐实不是 DM、不是 relay、也不是 Android。 |
| 2026-04-04 | 已把 [noderesourceservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/noderesourceservice.nim) 的 macOS `safeExec` 收成确定性边界：不再 `poUsePath + 继承环境`，改成绝对路径命令 `sw_vers/sysctl/vm_stat/df/system_profiler` + 显式空环境。`tests/testnoderesource_service.nim` 与 `build/tsnet_product_node` 已重编通过。 |
| 2026-04-04 | 带上上面这条修正后，fresh 本地节点重新拉起成功，`tailnet_status.proxyListenersReady=true`、双栈 `relayListeners[*].stage=ready`，`local_info.listenAddrSource=relay_listeners`，`pgrep` 也确认 `tsnet_product_node --rpcport=19125` 持续存活。 |
| 2026-04-04 | 新主包覆盖安装成功，设备 `lastUpdateTime=2026-04-04 07:37:13`；生产 UI token `ui_dm_direct_truth_20260404_074124` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息。Android logcat 已闭环到 `bootstrapSeedConnectExactAsync attempt:session_warm_done -> path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=4351`。 |
| 2026-04-04 | 同轮赛后本地 `19125` 继续可读：`tailnet_status.proxyListenersReady=true`、`relayListeners` 保持 `incoming/serving`，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续存在；`noderesourceservice/osproc` 这条本地生命周期崩点已被前推掉。 |
| 2026-04-04 | 已把 Android warm 结果里的 `sessionConnKey` 从 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 一路贯通到 [BridgeFacades.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt)、[ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt)、[libp2p_bridge.cpp](/Users/lbcheng/UniMaker/android/core-bridge/src/main/cpp/libp2p_bridge.cpp) 和 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim)；发送链现在具备“直接消费 ready session key”的权威通道，不再只能靠 bootstrap task 自己回填。 |
| 2026-04-04 | 带上上面这条贯通后，生产 UI token `ui_dm_session_key_passthrough_20260404_080109` 已再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息；但 Android logcat 也坐实当前真实发送仍是 `preferredSessionConnKey=0`，随后走 `bootstrapSeedConnectExactAsync attempt:session_warm_done -> path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=3946`，说明新的剩余时延头部仍在 native bootstrap/session warm，本轮不是 send/ack。 |
| 2026-04-04 | 上一轮成功后，本地 fresh `19125` 节点又前移暴露出新的赛后真崩点：`quicrelay.awaitPersistentIncomingWithRouteValidation -> probeRoutePublishedOnValidationClient -> relayRouteStatus -> rawAlloc`。现场已坐实 route 已经进入 `incoming/ready/bridge_attached/serving`，validation loop 却仍在跑第二套 `route_status` 真相源。 |
| 2026-04-04 | 已把 [quicrelay.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/quicrelay.nim) 的 validation 边界收正：一旦 route 已进入 `relayRouteServingStage`，`awaitPersistentIncomingWithRouteValidation()` 就不再继续 probe `route_status`；这时 published 真相只由 accept stream + keepalive 持有，不再保留第二条 validation client 路径。`tests/testtsnetquicrelay.nim` 与 `build/tsnet_product_node` 已重编通过。 |
| 2026-04-04 | 带上上面这条 validation 收正后，生产 UI token `ui_dm_validation_fix_20260404_080415` 已再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 `pgrep` 仍能看到 `tsnet_product_node --rpcport=19125` 存活，`tailnet_status.proxyListenersReady=true`、`relayListeners` 保持 `serving/ready`，`local_info.listenAddrSource=relay_listeners` 且双栈 `listenAddrs` 继续可读。这条 `validationClient/route_status` 赛后自杀链已被前推掉。 |
| 2026-04-04 | 已把 Android direct warm 的 ready 真相从 `peerId -> ownerKey 附着结果` 收成 `peerId + seedKey -> stable snapshot`，同时把 active warm 复用和取消判定也收成只认 `seedKey`；`ownerKey` 只再用于 UI 生命周期，不再污染网络会话真相。Android `:app:compileDebugKotlin`、`:app:assembleDebug` 和 `adb install -r` 都已通过。 |
| 2026-04-04 | 带上上面这条收正后，生产 UI token `ui_dm_peer_seed_truth_20260404_082630` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息；但 logcat 已坐实发送开始时仍是 `preferredSessionConnKey=0`，随后才走 `bootstrapSeedConnectExactAsync attempt:session_warm_done -> path=warmed_session_conn -> dm_session_reuse -> sendThreadMessage success durationMs=4247`。这说明当前 4 秒头部已经不是 owner 绑错，而是 warm 还没在点击前完成。 |
| 2026-04-04 | 已把 `bootstrapSeedConnectCanJoinUnfinishedTask()` 收成“只允许同意图 join”，并把 `runBootstrapSeedConnectExactAsync()` 的总预算改成必须服从调用方传入的 `safeSecureTimeoutMs`；前台发送不再继承未完成后台 warm，也不再让 bootstrap task 自己跑出 sender 的 10 秒 SLA。 |
| 2026-04-04 | 带上上面这条修正后，fresh 本地节点 + 新主包复跑 token `ui_dm_budget_truth_20260404_111708` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；Android logcat 已闭环到 `cmdSendDirect connectivity ... path=warmed_session_conn -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=8103`，旧的 `bootstrap direct session not warmed` 已不再出现。 |
| 2026-04-04 | 已把 `receivedDmLog` 从跨线程共享的 live `JsonNode` 收成 `ReceivedDmSnapshot` 值对象快照；接收侧只保存字符串/标量，`wait_dm`、game ingest、RPC 快照读取时再各自物化新 `JsonNode`，不再一边从 `seq[JsonNode]` 删除一边把同一批 ref 节点塞进新数组。 |
| 2026-04-04 | 带上上面这条修正后，`wait_dm` 已不再把本地 `19125` 节点打死：空跑会正常返回 `wait_dm_timeout`；随后生产 UI token `ui_dm_received_snapshot_20260404_113213` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息，Android logcat 闭环到 `bootstrapSeedConnectExactAsync attempt:session_warm_done -> path=warmed_session_conn -> dm_ack_done -> sendThreadMessage success durationMs=4348`。 |
| 2026-04-04 | `socialStateParsedSnapshot` 这轮又前移暴露出新的所有权真问题：receiver 在 `finalizeReceivedDirectMessageAsync -> persistSocialStateUnsafe -> cloneJson` 上直接 `SIGSEGV`，并把 sender 误拖成 `dm_ack_wait/send_with_ack_timeout`；这次已把 social state 正式收成“`socialStateMemLock` 下的单 owner live tree + 文本快照落盘”，`loadSocialStateUnsafe/persistSocialStateUnsafe` 不再对运行态树做整份 `cloneJson`。 |
| 2026-04-04 | 带上上面这条单 owner social-state 修正后，fresh 本地 `19125` 节点 + 新主包复跑 token `ui_dm_social_owner_tree_2_20260404_120905` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners[*].stage=serving`，`local_info.listenAddrSource=relay_listeners`。这条“消息收到后 social-state 后处理把本地节点拖死”的主线已被前推掉。 |
| 2026-04-04 | 已把 Android truth-route direct target 从“一次性 declare + send 里清掉再重建”收成“常驻 desired target + reconcile loop”；`NativeAppViewModel` 现在会持续重试 `declareNativeDirectSessionTargetNow()` 直到 native 接受，不再把 direct warm 绑成一次 fire-and-forget。 |
| 2026-04-04 | 同时已把发送路径的错边界删掉：truth-route direct target 未 ready 时，`sendConversationText()` 不再先 `clearNativeDirectSessionTargetNow()` 再走 relay；direct 和 relay 现在相互独立，relay fallback 不会再把后台 direct warm 自己打断。 |
| 2026-04-04 | 带上上面这条修正后，第一轮生产 UI token `ui_dm_direct_state_machine_20260404_142300` 已把旧的 `ensure_connectivity_failed 8086ms peer unreachable` 前推成 `send_begin 9793ms send_with_ack_timeout`；logcat 坐实 sender 已不再死在 connectivity，而是进入 `cmdSendDirect connectivity ... path=ready_wait`。 |
| 2026-04-04 | 紧接着第二轮 token `ui_dm_direct_state_machine_2_20260404_143100` 已重新 `OK (1 test)`，并且本地 `wait_dm` 收到同名消息；Android logcat 闭环到 `relay_fallback targetStage=running -> cmdSendDirect connectivity ... path=ready_wait -> dm_ack_done -> sendThreadMessage success durationMs=3724`。这说明“direct target 常驻、relay 独立 fallback”这条边界已经开始生效。 |
| 2026-04-04 | 当前发送成功链仍然是 `relay_fallback -> ready_wait -> dm_ack_done`，不是 `direct_target_ready -> warmed_session_conn`；新的唯一主线已经收窄成“让常驻 direct target 在点击前真正 ready，并在 ready 后自动切换为直连”。 |
| 2026-04-04 | 已把显式 truth-route send 的 native 选路从“send 自己 fresh dial/fallback”收成“direct/relay 并行状态机，send 只消费 ready session”：`executeSendDirectCommand()` 现在 direct ready 就走 `direct_ready_session`，否则等待 `peerRouteSession` 给出 `relay_ready_session`；send 本身不再把 explicit `/tsnet` truth-route 重新打回 `dm_fresh_dial_begin`。 |
| 2026-04-04 | 带上上面这条修正后，production UI token `ui_dm_dual_path_20260404_192151` 为 `OK (1 test)`；Android logcat 已坐实 `sendConversationText relay_fallback ... directReady=false -> cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=4206`。本地 receiver 日志已出现 `recordReceivedDm done` 和 `direct message ack sent`，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 节点继续存活。 |
| 2026-04-04 | 新一轮真实 blocker 不是 direct/relay 选路，而是 Android 后台 `networkDiscoverySnapshot` 观察线程直接读 live direct-target/bootstrap task，把主进程炸在 `libp2p_network_discovery_snapshot`；这次已把 direct-target 观测收回稳定快照，不再让 `latestOwnedDirectSessionTargetSnapshot()` 触碰 live `bootstrapSeedConnectTasks`。 |
| 2026-04-04 | 带上上面这条修正后，fresh 本地 `19125` 节点 + 新主包复跑 token `ui_dm_discovery_snapshot_fix_20260404_204711` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；`adb logcat -b crash` 里不再出现 `com.unimaker.native.debug` 的 `libp2p_network_discovery_snapshot` 崩溃，Android logcat 闭环到 `relay_fallback targetStage=pending -> bootstrapSeedConnectExactAsync ... session_warm_done -> path=relay_ready_session -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=4920`，赛后 `tailnet_status/local_info` 继续正常。 |
- 2026-04-04 21:01 左右，production UI token `ui_dm_direct_target_live_20260404_205527` 暴露出新真问题：不是 direct 不可用，而是 `dmservice` 还把后台 direct warm 和前台 relay route warm 锁在同一把 `peer+intent` 拨号锁里，sender 失败文案是 `connect_exact_failed 3330ms direct message dial lock timeout`。Android logcat 已坐实 `tailnet DIRECT`，但 `cmdSendDirect peer ready missing ... err=direct message dial lock timeout`。
- 我已在 [libp2p/protocols/dm/dmservice.nim](./libp2p/protocols/dm/dmservice.nim) 把 DM 拨号锁键从 `peer|intent` 收成 `peer|intent|direct_or_relay`；fresh 复跑 token `ui_dm_dial_lock_split_20260404_210705` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，说明 direct warm 不再把 relay route warm 卡死。
- 随后我又在 [examples/mobile_ffi/libnimlibp2p.nim](./examples/mobile_ffi/libnimlibp2p.nim) 把显式 `/tsnet` send 的选路收成“同时观察 direct/relay 两张 ready 快照，谁先 ready 用谁；direct 与 relay 同时 ready 时优先 direct”，不再先看一次 direct、再单向等 relay。
- 新逻辑 fresh 复跑 token `ui_dm_direct_relay_race_20260404_211303` 继续 `OK (1 test)`，本地 `wait_dm` 收到同名消息；sender 路径是 `path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=7375`。这说明这次点击时 direct 还没 ready，send 正确地走了 relay；不是又被错误锁死。
- 赛后 fresh 本地节点 `19125` 继续存活，[tailnet_status](./build/tsnet_product_rpc) 仍是 `proxyListenersReady=true`、`tailnetDirectRouteCount=0`、`tailnetPunchedRouteCount=0`，双栈 `relayListeners=ready`；[local_info](./build/tsnet_product_rpc) 仍是 `listenAddrSource=relay_listeners`。主线已经从“发送状态机错了”前推到“direct path 自身没在点击前 ready”。 
| 2026-04-04 | 已把 direct path 的尝试/成功/失败正式收成 owner 侧统计快照：`DirectSessionTargetSpec` 新增 `attemptCount/successCount/failureCount/lastAttemptAtMs/lastReadyAtMs/lastFailureAtMs/lastFailureReason`，并只在 published 状态迁移时更新；外部不再从日志、task finished 或 relay 结果反推“打洞成功率”。 |
| 2026-04-04 | 同时已把这些 direct 统计字段挂进 `buildPeerPathProjection()`，并补到 connectedPeers/networkDiscovery 输出链，保证节点列表、诊断页、发送决策都只读同一份路径真相。`tests/testdirectdm.nim`、`build/tsnet_product_node` 与 Android `:app:assembleDebug` 已重新通过。 |
| 2026-04-04 | fresh 本地 `19125` 节点 + 新主包复跑 token `ui_dm_direct_stats_20260404_213100` 继续 `OK (1 test)`，本地 `wait_dm` 收到同名消息；sender 路径仍是 `path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_ack_done -> sendThreadMessage success durationMs=8371`。这轮再次坐实：send 状态机已经正确，当前剩余问题就是 direct path 在点击前仍未 ready。 |
| 2026-04-04 | 继续往底层收后已钉死一个结构性真问题：`tsnet` 的 exact QUIC target 之前只有 `local -> relay` 两支，根本不会查 direct route。也就是说，即使底层已经学到 `punched_direct/direct` candidate，explicit `/tsnet` exact 路径也没机会消费它。 |
| 2026-04-04 | 已在 [runtime.nim](./libp2p/transports/tsnet/runtime.nim) 和 [tsnetprovidertypes.nim](./libp2p/transports/tsnetprovidertypes.nim) 把 exact target 收正成 `local -> direct -> relay`，并新增显式 `TsnetProxyDialMode.DirectRoute`。同时补了 [testtsnetproxy.nim](./tests/testtsnetproxy.nim) 回归：`exact udp dial can use direct route when exact target is direct-ready` 和 `exact udp dial target prefers direct route before relay`。 |
| 2026-04-04 | 新回归已通过：`nim c -r ... tests/testtsnetproxy.nim` 结果 `21 OK`；`tests/testdirectdm.nim` 和 `build/tsnet_product_node` 也重新编过。当前主线判断更收窄了：send 状态机和 exact 语义都已经正确，剩下的是“直连 candidate exchange / direct route 注册”本身还没在点击前完成。 |
| 2026-04-05 | 已把 [libnimlibp2p.nim](./examples/mobile_ffi/libnimlibp2p.nim) 的 `upsertDirectSessionTarget()` 收回 direct setter 正确边界：不再在声明 direct target 时同步触发 `reconcilePeerRouteSession()` 和 `ensurePeerRouteSessionReconcileLoop()`。带上这条修正后，manual deep link `manual_crash_probe_20260405_075437` 已不再把 `com.unimaker.native.debug` native 崩掉。 |
| 2026-04-05 | 随后又把 [ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt) 的 tsnet observation 链收成只读缓存，不再在 `networkDiscoverySnapshot/socialListDiscoveredPeers/getDiagnostics` 热路径里碰 live native；同时把 [ConversationRouteSendTest.kt](/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt) 的 composer 检测收正成读取输入框子树与 `消息输入框` 语义父节点。新主包 `lastUpdateTime=2026-04-05 08:05:40` 后，production UI token `ui_dm_20260405_081947` 与 `ui_dm_20260405_082037` 都已 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 fresh `19125` 节点继续可读，`tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，当前 fresh seed 为 `/ip4/100.64.148.187/udp/60659/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。 |
| 2026-04-05 | 已把“同一节点打洞成功率 + 直连/中继 DM 延时”收进同一份 peer 快照真相：native 在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里新增 peer 级 direct/relay DM 路径延时统计，按真实 `sendPath` 记账，并把 `directDmLatencyMs/relayDmLatencyMs/dmPathLatencyStats` 一起挂进 `buildPeerPathProjection()`、`connectedPeersInfo` 和 discovery 输出链；Android 侧在 [DomainModels.kt](/Users/lbcheng/UniMaker/android/core-model/src/main/kotlin/com/unimaker/nativeapp/core/model/DomainModels.kt)、[BridgeFacades.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt)、[NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)、[MainActivity.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt)、[NodesScreen.kt](/Users/lbcheng/UniMaker/android/feature-nodes/src/main/kotlin/com/unimaker/nativeapp/feature/nodes/NodesScreen.kt) 只做解析和展示，现在会话诊断与节点详情都会直接显示 `打洞 xx% a/b · 直连DM xxxms · 中继DM xxxms`。 |
| 2026-04-05 | 这轮验证结果：`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_latency_probe_only.nim` 编译通过，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过。`testmobile_ffi_latency_probe_only.nim` 的运行态仍被旧的本地 TCP 测试环境卡住：本机监听地址一直保持 `0.0.0.0/tcp/0`，连接阶段跑不进这轮新字段断言，因此当前已确认的是“代码与产品构建通过”，不是“这条本地测试运行态也已恢复”。 |
| 2026-04-05 | 又前推出一个新的 Android 热路径错边界：`directSessionTargetStatus()` 之前走 `withOperationalHandle()`，显式 `/tsnet` send 在 `prepare_route` 阶段先热读 direct status，就会为了“读状态”同步起 runtime/等 handle；再叠加 send 里“missing 就同步 declare direct target”，整条前置链会把 10 秒预算卡死在 `prepare_route`。现已把 [ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt) 的 `directSessionTargetStatus()` 收成纯快照读，并把 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 里的 send-time direct declare 改成后台异步请求。新主包 `lastUpdateTime=2026-04-05 09:01:15` 后，production UI token `ui_dm_20260405_090213` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名 DM；Android logcat 已闭环到 `triggerTruthRouteRuntimeActivation runtime_done -> declare=true -> syncDirectSessionTarget declared=true -> cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true -> dm_ack_done -> sendThreadMessage success durationMs=6367`。 |
| 2026-04-05 | 这轮把“direct/relay session 一直是 running”前推成真实 fresh dial 失败：先在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里给 direct/relay warm task 加上 `deadlineAtMs` 失效重拉，确认旧任务确实会吊死到 deadline 之外；再把 [dmservice.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim) 的 `establishOutboundSession()` fresh dial 收进 `awaitFutureWithin(...)`，让 `switch.dial(...)` 真正受 warm timeout 约束。新主包 `lastUpdateTime=2026-04-05 14:18:31` 后，production UI token `ui_dm_20260405_141928` 仍失败，但 logcat 已坐实 direct exact warm 与 relay route warm 都不再假死，而是共同返回 `direct message fresh dial timeout`：`warmSeededDirectSessionAsync ... err=direct message fresh dial timeout`、`bootstrapSeedConnectExactAsync failed ... lastError=direct message fresh dial timeout`、`runPeerRouteSessionWarmAsync warm_done ... err=direct message fresh dial timeout`。当前第一主因已收窄到 `switch.dial` 往 tsnet transport fresh dial 的真实卡点。 |
| 2026-04-05 | 已把 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `sendWithAckKickoff` 状态从“全局锁表 + 混读 `queuePending/runningKind`”收成“无锁 request slot + atomic latest snapshot”；`executeSendDirectCommand()` 不再把全局命令队列计数塞进 request payload。Android [BridgeFacades.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/nativeapp/core/bridge/BridgeFacades.kt) 的 timeout 诊断也改成只读 request-local `pathSelection/sendPath/directTargetStatus/relayTargetStatus`，不再打印全局 queue 假信号。`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 与 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 都通过。 |
| 2026-04-05 | 新主包 `lastUpdateTime=2026-04-05 22:59:20` 的 production UI token `ui_dm_20260405_225943` 仍失败，但错误串已经不再被全局 queue 污染；第一次 `sendDirectText:timeout_status` 只剩 request-local `status=running stage=envelope_ready`。继续给 Android timeout 读法补了 `300ms` 的 request-local settle 窗口后，新主包 `lastUpdateTime=2026-04-05 23:03:03` 的 token `ui_dm_20260405_230314` 已直接前推成 `connect_exact_failed 5177ms local tailnet provider not ready`。这轮 logcat 已坐实 fresh install 首发的真实时序是：`waitForDirectOrRelaySessionReady direct=pending relay=running -> reconcileDirectSessionTarget provider_wait_failed -> runPeerRouteSessionWarmAsync provider_wait_failed -> relay_failed/local tailnet provider not ready -> cmdSendDirect peer ready missing`。当前唯一主线已经从“sendWithAck 队列假真相”切回 fresh app 首发的 `provider_wait_failed` 时序。 |
| 2026-04-05 | 已把 [quicrelay.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/quicrelay.nim) 的 keepalive/validation 生命周期收正：`readJsonMessageWithTimeout`、`writeJsonMessageWithTimeout`、`awaitJsonFutureWithTimeout`、`awaitBridgeAttachWithTimeout` 超时后不再只 `cancelSoon()` 子 future，而是 `cancelAndWait()` 真正收口；`awaitPersistentIncomingWithRouteValidation()` 也改成在 finally 里把 `incomingFut/validationLoop/keepAliveLoop` 全部取消并 join 后再释放 client。 |
| 2026-04-05 | 带上这条修正后，新主包 `lastUpdateTime=2026-04-05 15:28:47` 的 production UI token `ui_dm_20260405_153022` 仍然失败，但本地 fresh `19125` 节点已经不再沿 `keepRelayConnectionAlive -> readJsonMessageWithTimeout -> readBinaryFrame -> msquicstream.read -> newObj` 这条链自杀。真实结果是：Android logcat 继续坐实 `warmSeededDirectSessionAsync`、`bootstrapSeedConnectExactAsync`、`runPeerRouteSessionWarmAsync` 都返回 `direct message fresh dial timeout`，instrumentation 仍停在 `local_tailnet_ready 9812ms send_with_ack_timeout runningKind=cmdNone queueQueued=0 queuePending=0`；与此同时本地节点赛后仍能继续回 `tailnet_status/local_info`，`relayListeners[*].stage=incoming`、`listenAddrSource=relay_listeners`，`wait_dm` 结果是 `wait_dm_timeout`。这说明 relay keepalive 崩链已被前推掉，当前第一主因重新收敛回 Android 侧 direct fresh dial 本体。 |
| 2026-04-05 | 已把 explicit `/tsnet` send 的架构正式收成“direct/relay 独立并行、send 只消费 ready session”的单一状态机：在 [tsnetprovidertypes.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnetprovidertypes.nim) 新增 `TsnetUdpExactDialPlan`，在 [runtime.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/runtime.nim) / [tsnetprovider*.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnetprovider.nim) 提供 pure exact-dial 规划，在 [tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 与 [dial*.nim](/Users/lbcheng/nim-libp2p/libp2p/dialer.nim) 增加 `directOnly` 拨号边界；同时在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增 `DirectPeerPathState`，把 direct reconcile 收成 owner 单飞 loop，`waitForDirectOrRelaySessionReady()` 只观察 direct/relay ready 快照，不再 send-time 自己 fresh dial 乱猜。`tests/testtsnetproxy.nim` 已扩到 `23 OK`，`build/tsnet_product_node` 与 Android `assembleDebug` 也都重新通过。 |
| 2026-04-05 | 随后又把 send 热路径里最后一条错边界前推掉：在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里，explicit `/tsnet` 的 `cmdSendDirect` 不再先卡 `ensureLocalTsnetReady()`；如果当前走的是 “只消费预热会话” 模式，就直接等待 `waitForDirectOrRelaySessionReady()`。带新主包 `lastUpdateTime=2026-04-05 16:59:38` 复跑 production UI token `ui_dm_20260405_170136` 后，失败阶段已从旧的 `local_tailnet_ready_wait` 前推成 `envelope_ready 9790ms send_with_ack_timeout`，说明 send 已经不再在 local tailnet gate 上烧预算。 |
| 2026-04-05 | Android 侧也按同一边界收了 direct desired：在 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 去掉 `declareNativeDirectSessionTargetNow()` 的 runtime-ready 前置判断，并把 truth route 入口改成页面一进来就 `declareNativeDirectSessionTarget(..., source=\"android_truth_route_prepare\")`；在 [ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt) 里，`directSessionTargetSet/Clear` 现在会先用上次成功的 `configJson` 补 `init(handle)`，但不 `start()`，保证“声明 desired”不再依赖 runtime 已经 started。 |
| 2026-04-05 | 带上上面两条继续复跑 production UI，主线真实状态已经前推清楚：新主包 `lastUpdateTime=2026-04-05 17:12:11`，fresh 本地节点 seed 仍是 `/ip4/100.64.148.187/udp/57229/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。token `ui_dm_20260405_170556` 与 `ui_dm_20260405_171226` 仍失败，错误稳定为 `send_with_ack_timeout stage=envelope_ready runningKind=cmdSendDirectKickoff queueQueued=0 queuePending=1`，本地 `wait_dm` 都是 `wait_dm_timeout`；同时 logcat 已坐实 `runPeerRouteSessionWarmAsync start peer=... source=android_truth_route_warm timeoutMs=4500`，说明 relay 状态机仍是独立并行启动的，且 sender 已不再退回 `local_tailnet_ready_wait`。当前唯一剩余 blocker 已经缩成：`cmdSendDirectKickoff -> executeSendDirectCommand -> waitForDirectOrRelaySessionReady / warmSessionDetailed(relayOnly=true)` 这条等待链没有在预算内真实收口。 |
| 2026-04-05 | 这轮继续把 blocker 前推成了更窄的两段时序问题。先在 [tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 把 `waitForProviderUdpDialReady()` 从“一次热读就失败”改成真正等待 `pending/connect/stream_open/ack_wait -> ready/failed`；production UI token `ui_dm_20260405_184825` 之后，relay bridge 已能在 send 过程中真实推进到 `connect -> stream_open -> ack_wait -> retrying(listener_busy)`，不再是假失败。随后在 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 把 truth-route direct desired 的 owner 侧 runtime 预热前推到页面进入，并在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 给 direct desired 的落表补回“立即触发 relay reconcile 第一拍”。最新验证是新主包 `lastUpdateTime=2026-04-05 18:58:26` 下的 token `ui_dm_20260405_185956`：`triggerTruthRouteRuntimeActivation runtime_done` 已在发送前约 1.5 秒出现，`runPeerRouteSessionWarmAsync start source=android_truth_route_activation` 已从原先 send 后更晚处前移到 send 后约 0.7 秒，`tsnet quic dial done` 也已经能在 send timeout 前出现。当前唯一剩余 blocker 已收敛成：relay warm 在 send deadline 前仍没把 `sessionConnKey` 发布成 ready，`waitForDirectOrRelaySessionReady` 超时时快照是 `directStatus=route_wait`、`relayStatus=running relayConnKey=0`，随后同一轮 warm 才以 `quic_dial_timeout` 收口。fresh 本地节点重启后 seed 现为 `/ip4/100.64.148.187/udp/63445/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，`19125` 赛后 `tailnet_status/local_info` 继续可读。 |
| 2026-04-05 | 当前新的本地节点崩点已钉死在 [connection.nim](/Users/lbcheng/nim-libp2p/libp2p/stream/connection.nim) 的 `Connection.readLp(maxSize)`：旧实现会按 `maxSize + 10` 直接申请整块读缓冲，`dmservice.runSession -> connectionReadLpAsync -> newObj` 这条崩栈正好对到入站 DM 首帧的大块分配。现已把它收成“前缀探测 + 按剩余长度分块读取”：先用 `64` 字节探测长度前缀，拿到总长度后再按最多 `16 KiB` 的块继续读，不再为 `maxMessageBytes=2 MiB` 先硬申请 `2 MiB` 缓冲。 |
| 2026-04-05 | 同时补了 [testconnection.nim](/Users/lbcheng/nim-libp2p/tests/testconnection.nim) 回归，验证 `readLp(2 * 1024 * 1024)` 也只会发起小块读请求，并且 coalesced replay 仍然正确。验证结果：`nim c -r tests/testconnection.nim` 为 `5 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetproxy.nim` 通过，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过。 |
| 2026-04-05 | 新一轮 production UI 失败已不再被 owner 外层 deadline 假盖成 `route_session_target_cancelled`。在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里，`runPeerRouteSessionWarmAsync()` 现在按 `warmBudgetMs` 自己轮询、超时后 `cancelAndWait()` 收口 warm future，并把 `lastWarmStage/lastWarmErr` 归一成真实失败阶段。带新主包复跑后，同一条链路已前推成 `connect_exact_failed ... quic_dial_timeout`，`runPeerRouteSessionWarmAsync` 不再把真因改写成 `route_session_target_cancelled`。 |
| 2026-04-05 | 随后又收掉了 app 本地 provider 的自锁：`ensureLocalTsnetReady()` 之前在 owner 线程里只是 `scheduleBackgroundTailnetWarm()`，等于在自己正占着的命令队列里再排一条 `cmdTailnetWarm`，导致 `providerReady` 永远起不来。现已在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增 `kickTailnetWarmOnOwnerThread()`，让 owner 路径直接 inline 启动 warm worker；`cmdTailnetWarm` 也复用同一 helper。fresh 本地 `19125` 节点现已稳定回到 `providerReady=true`、`proxyListenersReady=true`、`listenAddrSource=relay_listeners`，production UI 不再失败在 `local tailnet provider not ready`。 |
| 2026-04-05 | 在上面两条收正之后，最新 fresh 本地节点 seed 已更新为 `/ip4/100.64.148.187/udp/57290/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。production UI token `ui_dm_20260405_213542` 的真实失败已稳定收敛为 `connect_exact_failed 5196ms quic_dial_timeout`：logcat 已坐实 `reconcileDirectSessionTarget provider_wait_done`、`runPeerRouteSessionWarmAsync provider_wait_done`、`dm_fresh_dial_begin`、`dialer transport dial begin`、`tsnet dial target ready`、`dm_switch_dial_wait_begin`，随后 warm 在预算内收口成 `direct message fresh dial timeout`，sender 最终读到的是 `quic_dial_timeout`。这说明当前唯一剩余 blocker 已经前推到 `switch.dial -> tsnet transport -> upgrade/stream select` 这条真实 fresh dial 热路径。 |
| 2026-04-05 | 带上这条修正后，fresh 本地 `19125` 节点重新拉起，当前 seed 是 `/ip4/100.64.148.187/udp/55797/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`。production UI token `ui_dm_20260405_195634` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息；节点 PTY 里 `direct message readLp begin -> readLp done -> parse ok -> ack sent` 已完整闭环，不再出现之前那条 `dmservice.runSession -> connectionReadLpAsync -> newObj` 崩栈。赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 继续健康。 |
| 2026-04-06 | 这轮先把 relay worker 收成真正的后台状态机：在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里，direct desired 一落表就立刻 `reconcilePeerRouteSession()`，不再只等 `ensurePeerRouteSessionReconcileLoop()` 的下一拍；同时把 relay warm 尝试预算从硬编码 `4_500ms` 收成 `PeerRouteSessionWarmAttemptMs = 8_000`。新主包 `lastUpdateTime=2026-04-06 00:05:23` 后，production UI token `ui_dm_20260406_000601` 已不再死在 `quic_dial_timeout`，而是前推成 `connect_exact_failed 9023ms relay_session_path_mismatch`；logcat 坐实 `runPeerRouteSessionWarmAsync warm_done ... ok=true connKey=... pathKind=direct stage=fresh_dial_ready`，说明 relay worker 这次其实已经暖出了直连会话，只是 owner 还没把这份真相吸进去。 |
| 2026-04-06 | 随后把 direct 真相边界补全：只要 peer 上已经存在 live `direct/punched_direct/local` 会话，direct owner 就必须立刻吸收成 direct ready，不能再只认 direct worker 自己产出的 `sessionConnKey`。实现上在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增 `livePeerDirectSessionConnKey()`，并让 `liveDirectSessionTargetSnapshot()` 与 `reconcileDirectSessionTarget()` 都回落到这份 live peer truth。带新主包 `lastUpdateTime=2026-04-06 00:13:48` 复跑 fresh 本地节点 seed `/ip4/100.64.148.187/udp/62820/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 后，production UI token `ui_dm_20260406_001425` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已闭环到 `runPeerRouteSessionWarmAsync warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> cmdSendDirect success -> sendThreadMessage success durationMs=8504`。赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 继续健康，`adb logcat -b crash` 里没有 app 自己的 crash。 |
| 2026-04-06 | 这轮先把 `direct_session_target_set/clear` 的 owner 边界收正：在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 里，FFI 导出不再从调用线程直接改 `directSessionTargets/directPeerPathStates/peerRouteSessionTasks`，而是统一提交 `cmdDirectSessionTargetSet/cmdDirectSessionTargetClear` 给 owner 事件线程处理。第一次 fresh 复跑 token `ui_dm_20260406_052058` 直接把新错边界前推出来了：sender 失败串变成 `connect_exact_failed 9832ms direct and relay sessions not ready ... directTargetStatus=missing relayTargetStatus=missing`，这说明页面进入时的 direct desired 已经被 owner 命令链吞掉，不再是之前那种“假成功直接写表”。 |
| 2026-04-06 | 真根因随后钉死在同一文件的 `cmdDirectSessionTargetSet`：它还错误要求 `n.started=true`，导致 truth-route 页面进入时发出的 declare 在 runtime 已初始化但 switch 尚未 started 的窗口里全部被拒掉；于是 sender 最终只能看到 `directTargetStatus=missing`。现已删掉这条错误前置，让 direct desired 从 node event loop ready 开始就能合法落到 owner。 |
| 2026-04-06 | 带上这两条修正后，重新编过 `nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim`、`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetproxy.nim`，再通过 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 生成新主包，并自动 `adb install -r /Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk` 覆盖安装。设备 `com.unimaker.native.debug` 的 `lastUpdateTime` 已更新到 `2026-04-06 05:25:16`。 |
| 2026-04-06 | 用 fresh 本地节点 `/ip4/100.64.148.187/udp/56321/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 复跑 production UI token `ui_dm_20260406_052534` 后，instrumentation 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息。更关键的是时序已经收正：logcat 显示 `reconcilePeerRouteSession spawn peer=... source=android_truth_route_activation` 在 sender 前数秒启动，随后同一路 `runPeerRouteSessionWarmAsync warm_done ... pathKind=direct` 收口；真正点击发送时，sender 看到的是 `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=5918`。也就是说，这轮已经不再是“点击时 sender 自己补 warm”，而是页面进入后的 activation 路径先把直连会话准备好了。 |
| 2026-04-06 | 接着把 Android 侧 direct desired 收成单写入源：在 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 给 `DirectSessionTargetHandle` 增加 `source`，让 `syncDesiredDirectSessionTargets()` 成为唯一的 native declare 路径；truth-route prepare 只更新 desired state 并请求 runtime/tailnet warm，`processTruthRouteRuntimeActivation()` 不再二次 `directSessionTargetSet()` 覆盖 source。与此同时在 [MainActivity.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt) 删掉了 Compose `LaunchedEffect(initialTruthRoute, ...)` 里对同一条 route 的重复 `prepareTruthScene()`，避免同一路 truth route 被二次 prepare。 |
| 2026-04-06 | 带上这条整收后重新 `./gradlew :app:assembleDebug -x lint -x test --no-daemon`、自动 `adb install -r /Users/lbcheng/UniMaker/android/app/build/outputs/apk/debug/app-debug.apk`，设备 `lastUpdateTime` 更新到 `2026-04-06 06:16:39`。随后按同一 fresh `19125` 闭环重拉本地节点，当前 seed 为 `/ip4/100.64.148.187/udp/49974/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，production UI token `ui_dm_20260406_061746` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息；关键时序已经稳定收正为 `runPeerRouteSessionWarmAsync ... source=android_truth_route_prepare -> warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session`，`sendThreadMessage success durationMs=3477`。这轮相对上一版 `5674ms` 的 fresh 基线又压掉约 `2.2s`，且赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners[*].stage=incoming`，本地 `19125` 继续健康。 |
| 2026-04-06 | 这轮先把 [multistream.nim](/Users/lbcheng/nim-libp2p/libp2p/multistream.nim) 的 listener 收成了“coalesced replay 直接合并回包”：如果 v1 handshake 后首个 protocol 已经在 `pendingReadReplay`，就直接一次 write 发出 `response1 + protocol_ack`，不再先回 `response1` 再等第二次写；同时保留 `response1 + response2` 和 `response1 + na` 的同样边界。随后继续把 dialer 也收正：v1 `handshake + first protocol` 不再做两次 `writeLp`，而是一次写出 `response1_select_first_proto_coalesced`。 |
| 2026-04-06 | [testmultistream_stability.nim](/Users/lbcheng/nim-libp2p/tests/testmultistream_stability.nim) 已补齐两侧回归：`CoalescedListenerStream` 和 `PipelinedDialerStream` 现在都记录 `writes`，并断言 coalesced 输入下 listener 只写一次 combined frame，dialer 也只写一次 `lpFrame(\"/multistream/1.0.0\\n\") & lpFrame(\"/test/proto/1.0.0\\n\")`。`nim c -r /Users/lbcheng/nim-libp2p/tests/testmultistream_stability.nim` 继续为 `3 OK`。 |
| 2026-04-06 | 第一次带新主包自动 `adb install -r` 后，设备 `lastUpdateTime=2026-04-06 08:04:28`，fresh 本地节点 seed 为 `/ip4/100.64.148.187/udp/55925/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`；production UI token `ui_dm_20260406_080522` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`19125` 继续健康。 |
| 2026-04-06 | 那次实测也把真相前推出来了：真实生产流量当时还没命中 listener 的 coalesced replay 分支，节点日志仍是 `first_read=19 -> post_handshake read done elapsedMs=16 -> protocol_ack`。这说明 listener 优化本身是对的，但要让生产热路径吃到它，还得继续把 dialer 侧收成 single write。 |
| 2026-04-06 | 带上 dialer single-write 后重新 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 并自动 `adb install -r`，设备 `lastUpdateTime` 更新到 `2026-04-06 08:13:18`。fresh 本地节点 seed 变成 `/ip4/100.64.148.187/udp/59045/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`；production UI token `ui_dm_20260406_081333` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息，赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`。 |
| 2026-04-06 | 这次已经坐实真实生产流量命中了这条 combined path：Android logcat 先出现 `multistream dialer handshake write begin stage=response1_select_first_proto_coalesced`，随后节点日志直接出现 `multistream listener pending post-handshake content ... /unimaker/dm/1.0.0\\n -> response1_protocol_ack_coalesced`。也就是说，v1 protocol 的双边 single-flight 现在已经真的跑在 production UI 上了。 |
| 2026-04-06 | 但这刀并没有继续压总时延：同一轮里 `multistream dialer handshake read elapsedMs=742`、`select_first_proto read elapsedMs=1`，`sendThreadMessage success durationMs=3479`，几乎回到上一轮 `3477ms`。当前剩余大头已经不再是 protocol 二次写，而是 `dm_transport_connect_begin -> dm_transport_connect_done` 和 `handshake read` 本体。 |
| 2026-04-06 | 这轮先把“同一节点统计文案”的数据源边界收正了：在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 给 `DirectSessionTargetSnapshot` 补了 `directDmLatencyMs/relayDmLatencyMs`，`directSessionTargetStateJson()` 现在会把 `attempt/success/rate + direct/relay DM latency` 一次带回；在 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 里，`refreshConversationLatency()` 也不再只靠 `connectedPeersInfo`，而是把当前会话 seed 对应的 `queryNativeDirectSessionWarmStatus()` 直接并进 `ConversationLatencySnapshot`。本地回归 `nim c -r tests/testmobile_ffi_social_state_cache.nim`、产品节点重编、`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 都通过。 |
| 2026-04-06 | 但 fresh 真机验证在这轮被新的本地 `19125` 节点热崩链挡住了，不是旧的 `socialStatePath`，而是更前面的 `loadSocialStateUnsafe -> parseJson -> newObjNoInit`。fresh 节点 seed `/ip4/100.64.148.187/udp/60636/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，手发仍停在 `libp2p RELAY · tailnet DIRECT · 打洞 0% 0/0 · 直连DM -- · 中继DM -- · 发送中 wait_send_ack`；随后 production UI instrumentation `ui_dm_stats_verify_20260406_091235` 直接 `Process crashed`，本地节点日志坐实最新栈是 `finalizeReceivedDirectMessageAsync -> loadSocialStateUnsafe -> parseJson -> newObjNoInit -> SIGSEGV`。这说明“共享层不再缓存 `JsonNode`”还不够，`social_state` 的 parse 热路径本身也必须从 owner 热路拿掉，统计显示闭环才能继续。 |
| 2026-04-06 | 已把 `social_state` 的正确所有权彻底收正：在 [examples/mobile_ffi/libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增 owner 专用 `socialStateOwner` 内存态，并把首次载入前移到 `cmdStart`；`handleContentFeedNode`、`handleLanGroupMessage`、`finalizeReceivedDirectMessageAsync` 三条 owner 热路径现在都只改 owner 内存态后落盘，不再在业务消息后处理里 `loadSocialStateUnsafe -> parseJson`。同时 `libp2p_get_diagnostics_json` 也改回 `cmdDiagnostics`，调用线程不再旁路碰 `social_state`。 |
| 2026-04-06 | 带上这条修正后，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim` 为 `1 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过并自动 `adb install -r`。设备 `lastUpdateTime=2026-04-06 09:34:10`，fresh 本地 seed 为 `/ip4/100.64.148.187/udp/49453/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`；production UI token `ui_dm_social_owner_20260406_093421` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息，节点日志已闭环到 `handleDirectMessageNode socialState owner begin -> appendConversation done -> appendNotification done -> persistSocialState done -> postprocess done`，赛后 `tailnet_status/local_info` 继续正常，`adb logcat -b crash` 没有新的 app crash。 |
| 2026-04-06 | 这轮先在 [libp2p/protocols/dm/dmservice.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim) 把 direct warm 收正成“session ready 后异步挂 reader，但不等待 reader fully ready 才算 warm 完成”。当前 production 成功链大多已经是 `path=direct_ready_session`，所以 fresh dial payload pipeline 本来就不会落在点击热路径；真正该前移的是 reader attach，而不是 sender 再现挂一次。 |
| 2026-04-06 | 第一次带这条改动重跑 fresh `19125 + production UI` 时，新的第一主因不是 sender，而是本地节点在 `quicrelay.writeJsonMessageWithTimeout -> msquicStreamSend -> PendingStreamChunk.add` 崩掉；instrumentation `ui_dm_20260406_100118` 失败在 `connect_exact_failed 7719ms quic_dial_timeout`，本地 `wait_dm` 因节点自杀报 `socket closed while reading`。节点真栈已经坐实：`quicrelay.writeJsonMessage -> msquicStreamSend -> genericSeqAssign -> newObjNoInit -> SIGSEGV`。 |
| 2026-04-06 | 我随后在 [api_impl.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/nim-msquic/api/api_impl.nim) 收紧了 `HQUIC` 的 fast path：`getHandleFast()` 现在先经过 `gHandleRegistry` 判活，注册表成为唯一权威，已 release 的 handle 不再允许 raw cast 成 `StreamState/ConnectionState`。带上这条修正后，产品节点和 APK 重编、自动 `adb install -r` 成功，设备 `lastUpdateTime=2026-04-06 10:07:37`。 |
| 2026-04-06 | fresh 本地节点 seed `/ip4/100.64.148.187/udp/52387/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_100805` 已重新 `OK (1 test)`，本地 `wait_dm` 收到同名消息。Android logcat 已坐实新的正确时序：`runPeerRouteSessionWarmAsync ... stage=dm_transport_connect_done -> dm_protocol_select_done -> dm_reader_async_start -> warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=3485`。赛后 `tailnet_status.proxyListenersReady=true`、`relayListeners[*].stage=incoming`、`local_info.listenAddrSource=relay_listeners`，本地 `19125` 节点未再在 `quicrelay` ready 回包时自杀。 |
| 2026-04-06 | 这轮继续收 Android truth-route direct target 的调度边界：先尝试把 `prepare` 阶段的 desired target 直接 immediate sync 到 native owner，但 fresh 主包 `lastUpdateTime=2026-04-06 10:42:40` 的 production UI token `ui_dm_20260406_104319` 立即前推成 `connect_exact_failed 9819ms local tailnet provider pending`，说明这条 fast path 会把 direct declare 提前到 local tailnet provider 还没 ready 的窗口里，理论上不对，已经撤回。 |
| 2026-04-06 | 最终保留下来的结构修正只有一条：`syncDesiredDirectSessionTargets()` 统一经 [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 的 `directTargetFastPathScope` 串行执行，不再在公用 `Dispatchers.IO` 上和启动期其它重活抢调度。重新 `./gradlew :app:assembleDebug -x lint -x test --no-daemon`、自动 `adb install -r` 后，设备 `lastUpdateTime=2026-04-06 10:48:12`；fresh 本地 seed `/ip4/100.64.148.187/udp/64280/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260406_104908` 再次 `OK (1 test)`，本地 `wait_dm` 收到同名消息；logcat 已闭环到 `runPeerRouteSessionWarmAsync ... source=android_truth_route_prepare -> warm_done ... pathKind=direct -> cmdSendDirect connectivity ... path=direct_ready_session -> dm_ack_done -> sendThreadMessage success durationMs=3385`。当前还没到理论上界，但 cold start direct 主线已经稳定回到 `3.3s` 量级。 |
| 2026-04-06 | 这轮把 `ackRequested=true` 的 sender 预算边界正式收正了：在 [dmservice.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/dm/dmservice.nim) 提出 `DirectAuthoritativeAckTimeoutMs=3000` 作为 authoritative ACK 协议窗口，在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 把 `executeSendDirectCommand()` 的总预算拆成 `pre-send` 与 `post-ready ack` 两段；`waitForDirectOrRelaySessionReady()`、`ensureLocalTsnetReady()`、relay fallback 现在只消费扣除 ACK 预留后的预算，但即使剩余等待预算为 `0` 也仍会先读一次现成 ready session，不会误伤已经热好的 direct/relay。 |
| 2026-04-06 | 带上这条修正后，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_social_state_cache.nim`、`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 和 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 均通过；自动 `adb install -r` 后设备 `lastUpdateTime=2026-04-06 11:42:23`。fresh 本地节点 seed 为 `/ip4/100.64.148.187/udp/55840/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，production UI token `ui_dm_20260406_114312` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；Android logcat 已重新闭环到 `cmdSendDirect connectivity ... path=direct_ready_session relayOnly=false -> dm_ack_done -> sendThreadMessage success durationMs=4199`，之前“消息已到但 sender 假报 `ack timeout`”这条主因已经被前推掉。赛后 `tailnet_status.proxyListenersReady=true`、`local_info.listenAddrSource=relay_listeners`，`adb logcat -b crash` 里没有 `com.unimaker.native.debug` 的新 crash，仍只有华为输入法噪音。 |
| 2026-04-06 | 这轮把 relay route 退出与 published route 热读的边界彻底收正了：在 [quicrelay.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/quicrelay.nim) 新增 `clearRelayRouteStateLocked(ownerId, route)`，把 stale route 的 `incoming/ready/pending/awaiting/serving/known/dropped` 等 owner set 一次摘干净；`relayFinishRouteTask()` 改成只在“当前 route 没有 live replacement task”时才清 active/taskId/stage；同时 `relayRoutePublishedStage()` 不再碰 `relayListenerStates`，改成纯 owner set 投影。 |
| 2026-04-06 | 带上这条修正后，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquicrelay.nim`、定向回归 `tests/testtsnetquicrelay.nim 'stale listener routeId cannot overwrite replacement task stage' 'runtime listenerNeedsRepair repairs stale incoming udp route after route task exit'`、`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 和 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 均通过；自动完成华为安装器两步点击后，设备 `lastUpdateTime=2026-04-06 14:15:22`。fresh 本地 seed 为 `/ip4/100.64.148.187/udp/64211/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，production UI token `ui_dm_20260406_141728` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 `tailnet_status` 与 `local_info` 继续秒回，`proxyListenersReady=true`、`listenAddrSource=relay_listeners`，本地 `19125` 节点未再复现 `publishedListenerRoutes -> relayRoutePublishedStage -> relayListenerStates/getOrDefault -> SIGSEGV`。 |
| 2026-04-06 | 这轮继续把 runtime listener 注册真相从错误的 `seq[TsnetListenerRouteTarget]` 收成理论正确结构：在 [runtime.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/runtime.nim) 用稳定的 `tcp/udpListenerRoutes + tcp/udpListenerRawByRoute` 取代 `tcp/udpListenerRouteTargets`，`publishedListenerRoutes/runtimeListenerRoutes/listenerNeedsRepair/reconcileProxyListeners` 全部只读 route 列表和 `route->raw` 映射，不再经过 `listenerRouteTargetSnapshot -> newSeqOfCap[object]` 这条热读；[tests/testtsnetquicrelay.nim](/Users/lbcheng/nim-libp2p/tests/testtsnetquicrelay.nim) 也同步改成直接写 `udpListenerRawByRoute`，并补了 `publishedListenerRoutes() == @[route]` 断言。 |
| 2026-04-06 | 带上这条结构修正后，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnetquicrelay.nim` 通过，定向回归 `runtime local published udp stage counts as ready`、`runtime listenerNeedsRepair repairs stale incoming udp route after route task exit`、`runtime listenerNeedsRepair requests repair for known udp route without explicit flag`、`runtime reconcile repairs only explicitly requested route and keeps active listener` 为 `4 OK`；`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 和 `./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，自动 `adb install -r` 后设备 `lastUpdateTime=2026-04-06 16:00:10`。fresh 本地 seed 为 `/ip4/100.64.148.187/udp/58458/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL`，production UI token `ui_dm_20260406_160055` 为 `OK (1 test)`，本地 `wait_dm` 收到同名消息；赛后 `tailnet_status.proxyListenersReady=true`、双栈 `relayListeners[*].stage=ready`、`local_info.listenAddrSource=relay_listeners`，节点日志持续是 `listenerNeedsRepair=false reason=serving_stage`，`19125` 未再复现 `publishedListenerRoutes -> runtimeListenerRoutes -> listenerRouteTargetSnapshot -> nimNewSeqOfCap -> SIGSEGV`。 |
| 2026-04-06 | 按最新语义把 bridge-extra 的“直连真相”收成 IPv6-only：在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 删掉 `bridgePublicIpv4` 导出和自动 IPv4/LAN candidate 生成，只保留 `bridgePublicIpv6 + /ip6/.../quic-v1/p2p/...`；在 [quicrelay.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/quicrelay.nim) 让 `relayCandidatesForRaw()` 不再从 bridge-extra/hostNetwork 自动拼 `/ip4/...` 候选；在 [tsnet_product_node.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim) 启动时用 `getifaddrs` 只从非 `utun/tun/tap/tailscale` 接口探测本机全局 IPv6，并写进 `localIpv6/preferredIpv6/publicIpv6`。 |
| 2026-04-06 | 这轮验证结果：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `17 OK`，定向回归 `tests/testtsnetquicrelay 'relay candidates include public IPv6 and explicit hints from bridgeExtraJson'` 为 `1 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:/Users/lbcheng/nim-libp2p/build/tsnet_product_node /Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim` 通过，`cd /Users/lbcheng/UniMaker/android && ./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过，自动 `adb install -r` 后设备 `lastUpdateTime=2026-04-06 17:19:42`。fresh 本地 `19125` 节点的 `tailnet_status` 已不再包含 `bridgePublicIpv4`，而是导出 `bridgePublicIpv6=240e:33d:680e:1e00:808:1e06:a8b5:76ca`，`bridgeDirectCandidates` 也变成两条 `/ip6/240e:33d:680e:1e00:808:1e06:a8b5:76ca/udp/.../quic-v1/p2p/12D3KooWLgM...`；同一时刻本机 `ifconfig en0` 也坐实这条全局 IPv6 真在 `en0`，不是 `utun` VPN 地址。 |
| 2026-04-06 | 这轮先把 relay RTT 真值测出来了：fresh 本地 `19125` 节点上，`/Users/lbcheng/nim-libp2p/build/tsnet_product_rpc --pretty --port 19125 --op tailnet_ping --ping-target relay --sample-count 5 --timeoutMs 15000` 稳定返回 `minLatencyMs=1321`、`avgLatencyMs=1368`、`maxLatencyMs=1420`，所以当前网络条件下 tsnet relay DM 的理论下界就是约 `1.3s` 单 RTT 加极小 framing 开销，不可能比这条 relay RTT 更快。 |
| 2026-04-06 | 随后把 relay DM 基准链补齐了：在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 新增 `cmdPrepareRelaySession/cmdRelaySessionStatus` 与导出 `libp2p_prepare_relay_session/libp2p_relay_session_status`，在 [tsnet_product_node.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_node.nim) 新增 RPC `relay_session_prepare/relay_session_status`，在 [tsnet_product_rpc.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/tsnet_product_rpc.nim) 增加 `--session-conn-key`，让 relay 基准可以“先预热 session，再带 `sessionConnKey` 发 DM”，不再借用生产 `relayOnly` 语义。 |
| 2026-04-06 | relay 自测随之暴露了三条真实热路错误边界，并已逐层前推：1) [tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 的 `providerTailnetIp()` 以前会在 relay bridge dial 热路去拉整份 `statusPayload()`，现已改成只读 `tailnetIpTexts()` 轻量快照；2) [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `canonicalizeMultiaddrText()` 不再 `MultiAddress.init -> $` 往返分配，`mergeTsnetListenAddrsIntoPeerInfo()/refreshLanAwareAddrs()` 的 `tsnetOnly` 热路也不再把 published 文本重新 parse/整段赋给 `peerInfo.listenAddrs/addrs`；3) relay reconcile loop 以前只认 direct desired，`relay_session_prepare` 下一拍就会被当 stale 清掉，现已补成独立 `relaySessionTargets` 真相。 |
| 2026-04-06 | 当前 self relay benchmark 还没拿到成功 DM 样本，但 blocker 已前推到 relay 自测里的真实 transport 热路：`tsnet-quicrelay` 自测仍会在 `udp dial state lookup miss -> listener repair` 后把本地节点再次打回 `nim-msquic api_impl.applyAckFrame -> flushAllConnectionStreams`。针对这条最新崩链，现已把 [api_impl.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/nim-msquic/api/api_impl.nim) 从“ACK 热路复制 `seq[StreamState]` 快照再 flush”收回到“关闭只标记，循环后统一 prune”；这版已编过 `build/tsnet_product_node` 和 `build/tsnet_product_rpc`，等待下一次 fresh 自测确认。 |
| 2026-04-06 | 这轮继续把 relay lower-bound 自测链的热崩点往前推：1) [tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 已把 `publishLocalPeerInfo()` 从“动态 `JsonNode` merge/扩容”改成“初始化时一次解析 base fields，热路只拼固定字段 JSON 字符串”；2) [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 的 `ownedPeerRouteSessionConnKey()`/`latestPeerRouteSessionSnapshot()` 不再 fallback 扫描 peer 级 live session，也把 `peerRouteSessionSnapshotJson()` 收成手写固定字段字符串；3) [api_impl.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/nim-msquic/api/api_impl.nim) 的发送成功 diagnostics 已改成静态 note；4) [msquictransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/msquictransport.nim) 的 `awaitMsQuicDial()` 成功分支不再 `cancel()` timeout future；5) [tsnetprovider.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnetprovider.nim) 的 `listenerNeedsRepair/proxyListenersReady/proxyRouteCount/publishedAddrTexts` 改成锁内最小标量读，不再走 `providerReadSnapshot()` 整包拷贝。 |
| 2026-04-06 | 带上这批修正后，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 仍为 `17 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 多轮重编通过。fresh `19125` 上 `tailnet_ping --ping-target relay --sample-count 3` 继续稳定在 `min/avg/max = 1345/1349/1353 ms`，单次 fresh relay RTT 也回到 `1322 ms`，所以当前网络下 relay DM 理论下界仍是约 `1.3s`。最新 self `relay_session_prepare` 已不再回旧的 `published route / bridge-extra / provider snapshot / relay status JSON / awaitMsQuicDial cancelSoon` 崩链；当前新的唯一 top crash 已前推到 [runtime.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/runtime.nim) `udpDialState -> MultiAddress.$`，真栈是 `waitForProviderUdpDialReady -> providerUdpDialStateSafe -> tsnetprovider.udpDialState -> tsruntime.udpDialState -> multiaddress.$ -> vbuffer.toString -> SIGSEGV`。同时 fresh wrong-route 自测也已坐实真实协议错误从 `route_not_registered` 前推成 `listener_busy` / `direct message fresh dial connect timeout`；正确 route 复测还没拿到成功 prepare 样本。 |
| 2026-04-06 | 这轮 fresh relay prepare 已经把 `udpDialState/MultiAddress.$`、`latestPeerRouteSessionSnapshot/newObjNoInit`、`relayListenerStates/rawInsert` 这些 owner 热崩链全部收掉：`relayListenerStates` 已从 `owner -> route -> state` 收成单层 `routeKey -> state`，写侧优先原地更新；`openDialUdpProxy()` 也不再因为旧任务 cancel drain 期间再开第二个 raw proxy，而是先认 `route -> rawKey` 真相并复用同一条 dial proxy。 |
| 2026-04-06 | 最新 fresh 实测已明确前推到新的真实 blocker：`relay_session_prepare` 不再 `socket closed while reading`，节点也不再自杀；fresh `tailnet_ping --ping-target relay --sample-count 3 --timeoutMs 15000` 为 `min/avg/max = 1296/1330/1392 ms`，fresh `relay_session_prepare` 现在稳定回真实失败 `direct message fresh dial connect timeout`，`lastDialStage` 已从 `dm_transport_connect_begin` 前推到 `dm_session_warm_failed`。日志里第二轮 warm 已不再出现第二个 `udp dial proxy scheduled`，而是命中 `udp dial proxy reuse active route state ... raw=/ip4/127.0.0.1/udp/50128/quic-v1`；当前唯一剩余主因已经收窄成 `udp listener bridge attach failed ... err=nim_quic relay stream closed before a complete JSON frame was received`。 |
| 2026-04-06 | 这轮继续把 relay lower-bound 真相再收了一层：先在 [quicrelay.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnet/quicrelay.nim) 修掉 listener `bridge_attach/ready` 手写 JSON 的双重引号，直接收掉 `udp listener bridge attach failed ... stream closed before a complete JSON frame was received`；随后在 [connection.nim](/Users/lbcheng/nim-libp2p/libp2p/stream/connection.nim)、[relay.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/connectivity/relay/relay.nim)、[secure.nim](/Users/lbcheng/nim-libp2p/libp2p/protocols/secure/secure.nim)、[pnet.nim](/Users/lbcheng/nim-libp2p/libp2p/pnet.nim)、[msquictransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/msquictransport.nim)、[mplex.nim](/Users/lbcheng/nim-libp2p/libp2p/muxers/mplex/mplex.nim)、[yamux.nim](/Users/lbcheng/nim-libp2p/libp2p/muxers/yamux/yamux.nim)、[tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 给连接复制链补上 `relayPath` 真相，让 `relayOnly=true` 的 tsnet `RelayBridge` 不再在 DM session 里被误判成 `direct`。验证结果是实的：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 继续 `17 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；fresh `19125` 的 `tailnet_ping --ping-target relay --sample-count 3 --timeoutMs 15000` 为 `1337/1369/1398 ms`，fresh `relay_session_prepare` 首次真正成功返回 `status=ready sessionConnKey=4466060944 prepareLatencyMs=4069`，日志已坐实 `runPeerRouteSessionWarmAsync warm_done ... pathKind=relay`，节点持续存活、RPC 未断。 |
| 2026-04-06 | 主闭环打通后，下一层 blocker 也被前推到了“prepared session 复用写入”本体：同一节点上带 `sessionConnKey=4466060944` 且强制 `relayOnly=true` 跑 `send_dm`，发送链已经命中 `cmdSendDirect connectivity ... path=relay_ready_session -> dm_session_reuse -> dm_write_begin`，说明不再 fresh dial；但那次发送因为 session 已空放近 70 秒，最终在 `direct message session frame write begin -> direct message session frame write failed err=direct message write timeout` 卡满 `15002 ms`，`dm_rows` 为空。也就是说，现在 relay 常驻 session 的 prepare 已经正确，下一步只需要把“prepare 后立即复用发送”的 fresh benchmark 收成稳定样本，再看常驻 idle 是否还需要独立 keepalive/活性真相。 |
| 2026-04-06 | 这轮把 relay 常驻 session 的 transport 边界收正了：在 [tsnettransport.nim](/Users/lbcheng/nim-libp2p/libp2p/transports/tsnettransport.nim) 把 `TsnetTransportBuilderConfig.init()` 的默认 QUIC 配置改成 `handshakeIdleTimeoutMs=30000 / idleTimeoutMs=120000 / keepAliveIntervalMs=5000`，不再继续沿用 `MsQuicTransportConfig()` 那套 `idleTimeout=30s + keepAlive=0` 的默认边界；同时在 [testtsnettransport.nim](/Users/lbcheng/nim-libp2p/tests/testtsnettransport.nim) 补了一条 builder 默认值回归。验证结果：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 为 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过。fresh `19125` 新路由 `/ip4/100.64.148.187/udp/56068/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 的 `tailnet_ping --ping-target relay --sample-count 3 --timeoutMs 15000` 为 `1362/1613/1888 ms`；`relay_session_prepare` 返回 `status=ready sessionConnKey=4405177696 prepareLatencyMs=3869`。把同一条 session 空放 `75s` 后，`relay_session_status` 仍是 `ready/sessionConnKey=4405177696`，`send_dm` 直接成功返回 `stage=sent durationMs=1334`；日志已坐实发送链是 `path=relay_ready_session -> dm_session_reuse -> dm_write_done -> dm_ack_done`，消息帧复用的仍是同一 `connKey=4405177696`。这条“常驻 sessionConnKey 过 idle 后复用”主因已经收掉。 |
| 2026-04-07 | 这轮先把 Android production UI 的 relay-ready 真相显式抬到了界面与 instrumentation：在 [/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt)、[/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/MainActivity.kt) 和 [/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt](/Users/lbcheng/UniMaker/android/app/src/androidTest/kotlin/com/unimaker/nativeapp/ConversationRouteSendTest.kt) 增加 relay session 快照、`中继会话 READY` diagnostics、`waitRelayReady=true` 等待门闩；同时在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 让 `SendDirectKickoffTask` 真正携带 `preferredSessionConnKey`。这一步 fresh 主包 `lastUpdateTime=2026-04-07 05:19:12` 后，token `ui_dm_20260407_0520_connkey` 已 `OK (1 test)`，本地 `wait_dm` 收到同名消息，并且 logcat 已坐实 `preferredSessionConnKey=532601399152 -> cmdSendDirectKickoff dispatch ... sessionConnKey=532601399152 -> cmdSendDirect start ... sessionConnKey=532601399152`；但同一轮也把新的唯一 blocker 前推得很干净：发送路径虽然已经复用到 relay-prepared 的 key，`cmdSendDirect connectivity` 却仍被误记成 `path=direct_ready_session relayOnly=false`。 |
| 2026-04-07 | 随后我只收这一条真问题：在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 给 `executeSendDirectCommand()` 增加 `preferredSessionConnIsRelay`，把 relay-prepared `sessionConnKey` 的来源真相从 `usedRelayRouteSession` 解耦出来；只要当前复用的是 relay 准备好的 key，peer-ready stage、最终 `sendPath` 和 `sendRelayOnly` 都必须保持 `relay_ready_session/true`。验证结果是 fresh 闭环：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 继续 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过，`./gradlew :app:assembleDebug -x lint -x test --no-daemon` 通过；华为安装器两步确认后主包 `lastUpdateTime=2026-04-07 05:27:05`。fresh 本地 `19125` 节点 seed `/ip4/100.64.148.187/udp/49223/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，production UI token `ui_dm_20260407_0528_truthfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`、节点持续存活；[android_relay_ready3.log](/Users/lbcheng/nim-libp2p/build/android_relay_ready3.log) 已坐实 `relay ready before tap diagnostics=... 中继会话 READY -> sendConversationText relay_session_ready -> NativeLibp2pBridge preferredSessionConnKey=532539786816 -> cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true -> dm_session_reuse -> dm_write_done -> dm_ack_done -> sendThreadMessage success durationMs=719`。这条“relay-prepared key 到 send 时被假写成 direct”主因已经收掉。 |
| 2026-04-07 | 接着把 Android diagnostics 轮询里新的唯一 crash 也收掉了：旧链路 `libp2p_tailnet_status_json -> buildTailnetStatusPayload()` 还在 JNI 调用线程里直接构造 tailnet status，对 fresh benchmark 第二轮会把 app 打进 `libp2p_tailnet_status_json+80`。我在 [libnimlibp2p.nim](/Users/lbcheng/nim-libp2p/examples/mobile_ffi/libnimlibp2p.nim) 把 `libp2p_tailnet_status_json()` 改成统一提交 `cmdTailnetStatus`，只取 owner 线程产出的最终文本快照，失败时退回 `emptyTailnetStatusPayload()` 文本。验证结果：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testtsnettransport.nim` 继续 `18 OK`，`nim c -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin -o:build/tsnet_product_node examples/mobile_ffi/tsnet_product_node.nim` 通过；Android 主包重装后 `lastUpdateTime=2026-04-07 05:43:04`。fresh 本地 `19125` 节点 seed `/ip4/100.64.148.187/udp/59468/quic-v1/tsnet/p2p/12D3KooWLgMrytBuBxXyEjBsdjQyN7fhyV2c3bF7DpB7E3bdsMPL` 下，token `ui_dm_20260407_054359_tailfix` 为 `OK (1 test)`，本地 `wait_dm` 实收同名消息，赛后 `tailnet_status.proxyListenersReady=true`、节点持续存活；节点日志已坐实 `submitCommand cmdTailnetStatus -> statusPayload step=... -> runCommand completed kind=cmdTailnetStatus`，`adb logcat -b crash` 里不再出现 `com.unimaker.native.debug` 的 native `SIGSEGV`。 |
| 2026-04-07 | 在同一 fresh 节点上继续把“真实用户时序”前推：去掉 `waitRelayReady` 后，token `ui_dm_20260407_054359_nowait_tailfix` 已经 `OK (1 test)`，本地 `wait_dm` 实收同名消息，`adb logcat -b crash` 里仍然没有 `com.unimaker.native.debug` 的 crash。更关键的是，这不是前一轮 warm 污染出来的假成功：我随后先 `adb shell am force-stop com.unimaker.native.debug`，再跑冷启动 token `ui_dm_20260407_054359_nowait_cold`，结果仍是 `OK (1 test)`，本地 `wait_dm` 继续实收同名消息。两轮日志都把剩余真相暴露得很清楚：点击后先看到 `中继会话 RUNNING dm_transport_connect_begin`，随后 `runPeerRouteSessionWarmAsync` 前进到 `dm_protocol_select_begin`，最终命中 `cmdSendDirect connectivity ... path=relay_ready_session relayOnly=true`；`sendThreadMessage success durationMs` 分别是 `4398` 和 `4509`。这说明当前发送链已经对了，剩下唯一要收的是“把 relay 预热从 send-time 等待前移到点击前 ready”，让 fresh no-wait 样本回到接近 relay RTT 下界，而不是当前这类 `4.4s~4.5s`。 |
| 2026-04-08 | 这轮先把 shared WAN bootstrap join 的“整笔命令被拖成 20s 超时”主因收正了：在 [wanbootstrapservice.nim](/Users/lbcheng/nim-libp2p/libp2p/services/wanbootstrapservice.nim) 新增 `joinFuture`，把 `joinInProgress + waitOngoingJoinImpl()` 的 `100ms` 轮询改成单一 ongoing future 复用；同时给 `refreshFromRendezvousNamespaces()`、`requestBootstrapSnapshot()`、`bootstrapMountedKadDHTs()` 全部补上 `bootstrapPostConnectBudget()` 硬 deadline，避免 join 后半段被 `rendezvous/snapshot/kad` 长挂拖死。Android 侧在 [/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 新增 `hasUsableHostNetworkStatus()`，`transport_addrs/runtime_snapshot` 不再让 `networkType=none/reason=manual` 的空 placeholder 顶掉已拿到的真实 host truth。 |
| 2026-04-08 | 验证结果：`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testwanbootstrap_authoritative_autojoin.nim` 为 `1 OK`，`nim c -r -d:ssl -d:libp2p_msquic_experimental -d:libp2p_msquic_builtin tests/testmobile_ffi_wan_provider_seed_addrs.nim` 为 `2 OK`，`./gradlew :app:compileDebugKotlin -x lint -x test --no-daemon` 通过；Android 新包已用 `adb install -r -g` 装上，`lastUpdateTime=2026-04-08 06:07:26`，Harmony 新包已通过 `release_hongmeng.sh --target 3KN0224C18003262 --skip-nfc-smoke` 重装，`updateTime=1775599586706`。fresh 跑 `/Users/lbcheng/UniMaker/scripts/run_android_harmony_bootstrap_relay_dm.sh --skip-build --skip-install` 后，双端脚本已经跨过 `bootstrap_join` 跑到后续 `bootstrap_tick/DM`，旧的 `bootstrap_join_random_timeout` / `joinViaRandomBootstrapAsync_timeout` 不再是顶层 blocker；Android `transport_addrs` 现已稳定回真实 `hostNetworkStatus.networkType=cellular preferredIpv6=240e:43d:682c:222:a0b5:23ff:fe8e:a9d3`，Harmony `transport_addrs` 也为 `ok=true`。但真 discovery 仍未打通：`android-bootstrap_join.json` 内嵌 `joinResult` 还留着 `bootstrap_join_seeded_timeout`，两端 `candidateCount=1/connectedPeerCount=1/discoveredPayload.peerIds=[]`，都只看到 authoritative bootstrap；当前唯一正确主线已经前推成 provider gate：双端 `providerPolicy=wan_public_ip_quic`、`providerAdvertisable=true`，但 `providerReason` 仍是 `tsnet_publication_not_ready`，所以 bootstrap/overlay 还拿不到对端节点。 |
| 2026-04-08 | 继续往前推时，shared native 的 direct seed / advertised peer info 修正已经确认上车：`tests/testmobile_ffi_wan_provider_seed_addrs.nim` 现为 `5 OK`，日志已坐实 `syncAdvertisedPeerInfo reason=host_network_status changed=true ... addrs=/ip6/240e:43d:682c:222:a0b5:23ff:fe8e:a9d3/udp/4001/quic-v1`。随后 Android `app-debug.apk` 虽然已经 assemble 完成，但真正安装时被华为系统双层风控卡在 `com.android.packageinstaller/.InstallStaging -> .PackageInstallerActivity`；通过抓屏和 UI dump 精确点击两次 `继续安装` 后，Android 新包终于落地，`adb shell dumpsys package com.unimaker.native.debug | rg 'lastUpdateTime'` 前推到 `2026-04-08 07:01:55`。 |
| 2026-04-08 | 带上 Android 真新包 fresh 重跑 `/Users/lbcheng/UniMaker/scripts/run_android_harmony_bootstrap_relay_dm.sh --android-serial GBJ0222B24021692 --harmony-target 3KN0224C18003262 --skip-build --skip-install` 后，新的顶层 blocker 已经不在 shared native 发现逻辑，而是鸿蒙设备锁屏：脚本现场输出 `androidPeer=12D3KooWFAQPMC3LWnu9E3BoAuA1P5BKFWAh74AYdAowaeKcyob2`、`harmonyPeer=`，最终失败为 `peerId unresolved after bootstrap/runtime snapshot`。产物目录是 [/Users/lbcheng/UniMaker/build/android-harmony-bootstrap-relay-dm/20260408-070219](/Users/lbcheng/UniMaker/build/android-harmony-bootstrap-relay-dm/20260408-070219)。其中 [harmony-main-start.txt](/Users/lbcheng/UniMaker/build/android-harmony-bootstrap-relay-dm/20260408-070219/harmony-main-start.txt) 已坐实 `Error Code:10106102 The device screen is locked during the application launch`；`hidumper -s WindowManagerService -a '-a'` 也显示 `SCBScreenLock12` 仍是 `Focus window: 17`。所以这轮还没资格判断 Android/Harmony 谁发现不到谁，当前唯一必须先解决的是“把鸿蒙设备真正解锁并拉起 app”，然后再看 fresh 双端 discovery 结果。 |
